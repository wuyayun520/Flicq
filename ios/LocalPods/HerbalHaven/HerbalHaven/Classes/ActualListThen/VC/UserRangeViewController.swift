
//: Declare String Begin

/*: "Email Address" :*/
fileprivate let str_liveTouchValue:[Character] = ["E","m","a","i","l"," "]
fileprivate let str_subValue:String = "Addresssay left self view as"

/*: "Phone number" :*/
fileprivate let str_labMakeValue:[Character] = ["P","h","o","n","e"," ","n","u","m"]
fileprivate let str_timeContent:String = "beuser"

/*: "Enter the email code sent to" :*/
fileprivate let str_contentMaxBagValue:String = "Enter tclear add"
fileprivate let str_countEqualColorName:[Character] = ["h","e"," ","e","m","a","i","l"," ","c","o","d","e"," ","s","e","n","t"," ","t","o"]

/*: "Enter the phone code sent to" :*/
fileprivate let str_resignData:[Character] = ["E","n","t","e","r"," ","t","h","e"," ","p","h","o","n","e"," ","c"]
fileprivate let str_messageImageName:String = "self as content modelode s"
fileprivate let str_rankValue:[Character] = ["e","n","t"," ","t","o"]

/*: "(+ :*/
fileprivate let str_topData:[Character] = ["(","+"]

/*: ) :*/
fileprivate let str_screenText:[Character] = [")"]

/*: "8075F5" :*/
fileprivate let str_shareValue:[Character] = ["8","0","7","5","F","5"]

/*: "F4F4F4" :*/
fileprivate let str_tapData:[Character] = ["F","4","F","4","F","4"]

/*: "Resend verification email" :*/
fileprivate let str_viewCornerName:String = "Reseelse data"
fileprivate let str_clearName:String = "minimizefminimizecat"
fileprivate let str_targetText:String = "color head skinion e"

/*: "Tips: If you can't receive the verification code, please check whether the message is in spam or not" :*/
fileprivate let str_blockShareValue:[UInt8] = [0x4f,0x72,0x6b,0x68,0x21,0x3b,0x52,0x7d,0x3b,0x62,0x74,0x6e,0x3b,0x78,0x7a,0x75,0x3c,0x6f,0x3b,0x69,0x7e,0x78,0x7e,0x72,0x6d,0x7e,0x3b,0x6f,0x73,0x7e,0x3b,0x6d,0x7e,0x69,0x72,0x7d,0x72,0x78,0x7a,0x6f,0x72,0x74,0x75,0x3b,0x78,0x74,0x7f,0x7e,0x37,0x3b,0x6b,0x77,0x7e,0x7a,0x68,0x7e,0x3b,0x78,0x73,0x7e,0x78,0x70,0x3b,0x6c,0x73,0x7e,0x6f,0x73,0x7e,0x69,0x3b,0x6f,0x73,0x7e,0x3b,0x76,0x7e,0x68,0x68,0x7a,0x7c,0x7e,0x3b,0x72,0x68,0x3b,0x72,0x75,0x3b,0x68,0x6b,0x7a,0x76,0x3b,0x74,0x69,0x3b,0x75,0x74,0x6f]

/*: "Bind Email succeed" :*/
fileprivate let str_partyRemoteData:[Character] = ["B","i","n","d"," ","E","m","a","i","l"]
fileprivate let str_searchName:[Character] = [" ","s","u","c","c","e","e","d"]

/*: "Bind Mobile Phone succeed" :*/
fileprivate let str_hiddenText:String = "message model player returnBind M"
fileprivate let str_textSizeName:String = "e Phoneself if"
fileprivate let str_appFirstTransformText:String = "if remove selected succeed success su"
fileprivate let str_frontData:String = "ccmodelmodeld"

/*: "Resend verification email (%@s)" :*/
fileprivate let str_layerValue:[UInt8] = [0x3b,0xc,0x1a,0xc,0x7,0xd,0x49,0x1f,0xc,0x1b,0x0,0xf,0x0,0xa,0x8,0x1d,0x0,0x6,0x7,0x49,0xc,0x4,0x8,0x0,0x5,0x49,0x41,0x4c,0x29,0x1a,0x40]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UserRangeViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/1.
//

//: import UIKit
import UIKit

//: class TalkingLoginBindInputCodeVC: TalkingBaseViewController {
class UserRangeViewController: ToControllerPositionBaseRecognizerDelegate {
	var sizeCount: Int = 57
	var liveName: String = "domain"
	var onArray: [AnyHashable] = []
	var labelDictionary: [AnyHashable: String] = [:]
	var burnQuantity: Int = 79
	var recommendAreaName: String = "user"
	var eigenvalueOfASquareMatrixArray: [AnyHashable] = []
	var rankDictionary: [AnyHashable: String] = [:]

    //: public var  phoneOrEmailStr: String = ""  // 邮箱或者Email
    public var phoneOrEmailStr: String = "" // 邮箱或者Email
    //: public var  areaCodeNum: String = ""
    public var areaCodeNum: String = ""
    //: public var  codeStr: String = ""
    public var codeStr: String = ""
    //: public var  viewType: TalkingBindVCType?
    public var viewType: SoundMeBindType?
    //: var isBack: Bool!
    var isBack: Bool!
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.setupSubviews()
        self.message()
        //: self.setupSubViewsConstraint()
        self.name()
        //: self.bindInteraction()
        self.observer()
        //: func_starCodeTime()
        redGiant()
    
		if var riseValue = self.isBack { 
	            if (inputLabel.layer.contentsRect.size.width != 1) && (inputLabel.alignmentRectInsets.bottom == 16) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let backgroundLet = CypherView()
	            backgroundLet.fromClose = riseValue
	            backgroundLet.awakeQuantity = { [self] userInterval in
	            self.sizeCount = userInterval
	            
	            var backgroundLet = codeInputView.codeNumber
	                backgroundLet += 1
	                if Int(backgroundLet) > -98 {
	                    backgroundLet = backgroundLet - 1
	                }
	            if backgroundLet < self.sizeCount {
	                self.sizeCount = backgroundLet
	            }
	            
	            return self.sizeCount
	            }
	            backgroundLet.lineBeforeContent = { [self] viewTitle in
	            self.liveName = viewTitle
	            
	            var backgroundLet = codeStr
	            backgroundLet.append(backgroundLet[backgroundLet.index(after: backgroundLet.startIndex)])
	            if backgroundLet.hasPrefix(self.liveName) {
	                self.liveName = backgroundLet
	            }
	            
	            let obtrude = self.liveName.dropLast(1)
	            if obtrude.count == 5 {
	                self.liveName.remove(at: self.liveName.startIndex)
	            }
	            return self.liveName
	            }
	            backgroundLet.modelPremiumArray = { [self] willArray in
	            self.onArray = willArray
	            
	            guard var value = self.onArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            backgroundLet.fogDictionary = { [self] finishSuccessDictionary in
	            self.labelDictionary = finishSuccessDictionary
	            
	            guard var value = self.labelDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                inputLabel.addSubview(backgroundLet)
	            }
	
		}
	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: func_endCodeTime()
        clear()
    
		if var pageValue = self.isBack { 
	            if (codeInputView.bounds.origin.y == 17.96) && (codeInputView.inputViewController != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let fromLimit = CypherView(frame: codeInputView.bounds)
	            fromLimit.fromClose = pageValue
	            fromLimit.awakeQuantity = { [self] userInterval in
	            self.burnQuantity = userInterval
	            
	            var fromLimit = self.codeInputView.codeNumber
	            fromLimit ^= fromLimit ^ 3
	            if fromLimit > self.burnQuantity {
	                self.burnQuantity = fromLimit
	            }
	            
	            return self.burnQuantity
	            }
	            fromLimit.lineBeforeContent = { [self] viewTitle in
	            self.recommendAreaName = viewTitle
	            
	            var fromLimit = self.areaCodeNum
	            fromLimit.removeAll(where: { $0.isWhitespace })
	            if fromLimit.hasPrefix(self.recommendAreaName) {
	                self.recommendAreaName = fromLimit
	            }
	            
	            self.recommendAreaName = String(repeating: self.recommendAreaName.lowercased() + "view", count: self.recommendAreaName.count)
	            return self.recommendAreaName
	            }
	            fromLimit.modelPremiumArray = { [self] willArray in
	            self.eigenvalueOfASquareMatrixArray = willArray
	            
	            guard var value = self.eigenvalueOfASquareMatrixArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            fromLimit.fogDictionary = { [self] finishSuccessDictionary in
	            self.rankDictionary = finishSuccessDictionary
	            
	            guard var value = self.rankDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                codeInputView.addSubview(fromLimit)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    // MARK: - Lazy Load

    //: lazy var tipsLabel: UILabel = {
    lazy var tipsLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .segment()
        //: if self.viewType == .BindEmail {
        if self.viewType == .BindEmail {
            //: label.text = "Email Address".localized
            label.text = (String(str_liveTouchValue) + String(str_subValue.prefix(7))).localized
            //: } else {
        } else {
            //: label.text = "Phone number".localized
            label.text = (String(str_labMakeValue) + str_timeContent.replacingOccurrences(of: "user", with: "r")).localized
        }
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: return label
        return label
        //: }()
    }()

    //: lazy var titleLabel: UILabel = {
    lazy var titleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
        label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
        //: label.textColor = .appTitleColor()
        label.textColor = .segment()

        //: if self.viewType == .BindEmail {
        if self.viewType == .BindEmail {
            //: label.text = "Enter the email code sent to".localized
            label.text = (String(str_contentMaxBagValue.prefix(7)) + String(str_countEqualColorName)).localized
            //: } else {
        } else {
            //: label.text = "Enter the phone code sent to".localized
            label.text = (String(str_resignData) + String(str_messageImageName.suffix(5)) + String(str_rankValue)).localized
        }
        //: return label
        return label
        //: }()
    }()

    //: lazy var inputLabel: UILabel = {
    lazy var inputLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 16)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 16)
        //: label.textColor = .appThemeColor()
        label.textColor = .viewColor()
        //: label.text = (self.viewType == .BindEmail ? "\(phoneOrEmailStr)" : "(+\(areaCodeNum))\(phoneOrEmailStr)")
        label.text = (self.viewType == .BindEmail ? "\(phoneOrEmailStr)" : "(+\(areaCodeNum))\(phoneOrEmailStr)")
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: return label
        return label
        //: }()
    }()

    //: lazy var codeInputView: TalkingCodeInputView = {
    lazy var codeInputView: ListInputView = {
        //: let codeView = TalkingCodeInputView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 70), codeNumber: 6, labelTextColor: .white, mainColor: UIColor(hex: "8075F5")!, normalColor: UIColor(hex: "F4F4F4")!, margin: 12)
        let codeView = ListInputView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: 70), codeNumber: 6, labelTextColor: .white, mainColor: UIColor(hex: (String(str_shareValue)))!, normalColor: UIColor(hex: (String(str_tapData)))!, margin: 12)
        //: if LabelToReactiveCompatible.shared.direction == .rightToLeft {
        if LabelToReactiveCompatible.shared.direction == .rightToLeft {
            //: codeView.layer.transform =  CATransform3DMakeRotation(CGFloat(Double.pi), 0, 1, 0)
            codeView.layer.transform = CATransform3DMakeRotation(CGFloat(Double.pi), 0, 1, 0) // 镜像
        }
        //: return codeView
        return codeView
        //: }()
    }()

    //: lazy var commitButton: UIButton = {
    lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 18)
        //: btn.setTitle("Resend verification email".localized, for: .normal)
        btn.setTitle((String(str_viewCornerName.prefix(4)) + "nd ver" + str_clearName.replacingOccurrences(of: "minimize", with: "i") + String(str_targetText.suffix(5)) + "mail").localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.imageSize(colors: UIColor.appColorVideo(), size: CGSize(width: kLet_itemData - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        //: label.textColor = .pullUpColor()
        label.textColor = .pullUpColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let str = "Tips: If you can't receive the verification code, please check whether the message is in spam or not".localized
        let str = String(bytes: str_blockShareValue.map{$0^27}, encoding: .utf8)!.localized
        //: let range = NSRange(location: 5, length: str.count-5)
        let range = NSRange(location: 5, length: str.count - 5)
        //: let attributedString = NSMutableAttributedString(string: str)
        let attributedString = NSMutableAttributedString(string: str)
        //: attributedString.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 12), range: range)
        attributedString.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 12), range: range)
        //: label.attributedText = attributedString
        label.attributedText = attributedString
        //: return label
        return label
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginBindInputCodeVC {
extension UserRangeViewController {
    //: func func__bindEmailAction() {
    func failure() {
        //: self.view.endEditing(true)
        self.view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        TotalerpretationProgressHUD.iconView(superView: view)
        //: if viewType == .BindEmail {
        if viewType == .BindEmail {
            //: TalkingLoginRequestTool.req_bindEmailCode(emailCode: codeStr) { succeed, result, errorModel in
            ActualListThen.domain(emailCode: codeStr) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: if succeed {
                if succeed {
                    //: RecordAppManager.share.loginUserMode.email = self.phoneOrEmailStr
                    RecordAppManager.share.loginUserMode.email = self.phoneOrEmailStr
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Bind Email succeed".localized)
                    self.moveAir(showMsg: (String(str_partyRemoteData) + String(str_searchName)).localized)
                    //: let count: Int = (self.navigationController?.viewControllers.count)!
                    let count: Int = (self.navigationController?.viewControllers.count)!
                    //: if count >= 3 {
                    if count >= 3 {
                        //: let count = count - 3
                        let count = count - 3
                        //: let vc = self.navigationController?.viewControllers[count]
                        let vc = self.navigationController?.viewControllers[count]
                        //: self.navigationController?.popToViewController(vc!, animated: true)
                        self.navigationController?.popToViewController(vc!, animated: true)
                    }
                    //: } else {
                } else {
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.icon()
                    //: self.codeInputView.startEdit()
                    self.codeInputView.woodsiness()
                }
            }
            //: } else {
        } else {
            //: TalkingLoginRequestTool.req_MobileBind(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, vcode: self.codeStr) { [self]  succeed, result, errorModel in
            ActualListThen.sawLog(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, vcode: self.codeStr) { [self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: if !succeed {
                if !succeed {
                    //: self.codeInputView.startEdit()
                    self.codeInputView.woodsiness()
                    //: return
                    return
                }
                //: RecordAppManager.share.loginUserMode.mobile = self.phoneOrEmailStr
                RecordAppManager.share.loginUserMode.mobile = self.phoneOrEmailStr
                //: self.func__showStatusBarSuccessMsg(showMsg: "Bind Mobile Phone succeed".localized)
                self.moveAir(showMsg: (String(str_hiddenText.suffix(6)) + "obil" + String(str_textSizeName.prefix(7)) + String(str_appFirstTransformText.suffix(3)) + str_frontData.replacingOccurrences(of: "model", with: "e")).localized)
                //: for index in 0..<(self.navigationController?.viewControllers.count)! {
                for index in 0 ..< (self.navigationController?.viewControllers.count)! {
                    //: if self.navigationController?.viewControllers[index] .isKind(of: TalkingSettingsUniversalVC.self) == true {
                    if self.navigationController?.viewControllers[index].isKind(of: TipContextRecognizerDelegate.self) == true {
                        //: let vc = (self.navigationController?.viewControllers[index] as? TalkingSettingsUniversalVC)!
                        let vc = (self.navigationController?.viewControllers[index] as? TipContextRecognizerDelegate)!
                        //: self.navigationController?.popToViewController(vc, animated: true)
                        self.navigationController?.popToViewController(vc, animated: true)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func func__resendEmailCodeAction() {
    func push() {
        //: self.view.endEditing(true)
        self.view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        TotalerpretationProgressHUD.iconView(superView: view)
        //: if viewType == .BindEmail {
        if viewType == .BindEmail {
            //: TalkingLoginRequestTool.req_getBindEmailCode(emailStr: self.phoneOrEmailStr, isBind: true) { succeed, result, errorModel in
            ActualListThen.dismissCompletion(emailStr: self.phoneOrEmailStr, isBind: true) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: if succeed {
                if succeed {
                    //: self.func_starCodeTime()
                    self.redGiant()
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.icon()
                }
                //: self.codeInputView.startEdit()
                self.codeInputView.woodsiness()
            }
            //: } else {
        } else {
            //: TalkingLoginRequestTool.req_bindPhoneGetCode(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, completion: { succeed, result, errorModel in
            ActualListThen.make(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, completion: { succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: if succeed {
                if succeed {
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.icon()
                    //: self.func_starCodeTime()
                    self.redGiant()
                }
                //: self.codeInputView.startEdit()
                self.codeInputView.woodsiness()
                //: })
            })
        }
    }

    //: func func_starCodeTime() {
    func redGiant() {
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: var timeCount = 300
        var timeCount = 300
        //: timerDispose = Observable<Int>.interval(.seconds(1), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(1), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: timeCount -= 1
                timeCount -= 1
                //: self.commitButton.isEnabled = (timeCount <= 0)
                self.commitButton.isEnabled = (timeCount <= 0)
                //: if timeCount > 0 {
                if timeCount > 0 {
                    //: self.commitButton.setTitle("Resend verification email (%@s)".localizedArguments(timeCount), for: .normal)
                    self.commitButton.setTitle(String(bytes: str_layerValue.map{$0^105}, encoding: .utf8)!.localizedArguments(timeCount), for: .normal)
                    //: } else {
                } else {
                    //: self.func_endCodeTime()
                    self.clear()
                    //: self.commitButton.setTitle("Resend verification email".localized, for: .normal)
                    self.commitButton.setTitle((String(str_viewCornerName.prefix(4)) + "nd ver" + str_clearName.replacingOccurrences(of: "minimize", with: "i") + String(str_targetText.suffix(5)) + "mail").localized, for: .normal)
                }
                //: })
            })
    }

    //: func func_endCodeTime() {
    func clear() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }
}

// MARK: - Layout

//: extension TalkingLoginBindInputCodeVC {
extension UserRangeViewController {
    // 添加视图
    //: private func setupSubviews() {
    private func message() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(tipsLabel)
        view.addSubview(tipsLabel)
        //: view.addSubview(titleLabel)
        view.addSubview(titleLabel)
        //: view.addSubview(inputLabel)
        view.addSubview(inputLabel)
        //: view.addSubview(codeInputView)
        view.addSubview(codeInputView)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
        //: view.addSubview(desLab)
        view.addSubview(desLab)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func name() {
        //: tipsLabel.snp.makeConstraints { make in
        tipsLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.top.equalTo(34)
            make.top.equalTo(34)
        }
        //: titleLabel.snp.makeConstraints { make in
        titleLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(tipsLabel.snp.bottom).offset(40)
            make.top.equalTo(tipsLabel.snp.bottom).offset(40)
        }
        //: inputLabel.snp.makeConstraints { make in
        inputLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(titleLabel.snp.bottom)
            make.top.equalTo(titleLabel.snp.bottom)
        }
        //: codeInputView.snp.makeConstraints { make in
        codeInputView.snp.makeConstraints { make in
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(70)
            make.height.equalTo(70)
            //: make.top.equalTo(inputLabel.snp.bottom).offset(24)
            make.top.equalTo(inputLabel.snp.bottom).offset(24)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(codeInputView.snp.bottom).offset(30)
            make.top.equalTo(codeInputView.snp.bottom).offset(30)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(commitButton.snp.bottom).offset(36)
            make.top.equalTo(commitButton.snp.bottom).offset(36)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func observer() {
        //: commitButton.rx.controlEvent(.touchUpInside)
        commitButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__resendEmailCodeAction()
                self.push()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: codeInputView.codeBlock = { [weak self] (codeStr: String) -> Void in
        codeInputView.codeBlock = { [weak self] (codeStr: String) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if codeStr.count == 6 {
            if codeStr.count == 6 {
                //: self.codeStr = codeStr
                self.codeStr = codeStr
                //: self.func__bindEmailAction()
                self.failure()
            }
            //: return
        }
    }
}
