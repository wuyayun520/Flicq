
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_liveText:[UInt8] = [0x7b,0x7c,0x7b,0x66,0x3a,0x71,0x7d,0x76,0x77,0x60,0x28,0x3b,0x32,0x7a,0x73,0x61,0x32,0x7c,0x7d,0x66,0x32,0x70,0x77,0x77,0x7c,0x32,0x7b,0x7f,0x62,0x7e,0x77,0x7f,0x77,0x7c,0x66,0x77,0x76]

private func bottomDismiss(layer num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let str_withStatusReturnText:String = "top disappear cover leftTalki"
fileprivate let str_aspectName:String = "contentup"
fileprivate let str_directionName:[Character] = ["C","h","a","t","T","e","x","t","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_picData:[Character] = ["T","a","l","k","i","n","g","C"]
fileprivate let str_routeTitle:String = "button userhatIm"
fileprivate let str_managerRowContent:String = "make cell capacitysgCe"

/*: "PublishingCompanyMsgCell" :*/
fileprivate let str_makeText:String = "Talkimage video view make"
fileprivate let str_maleContent:String = "upChatequal table bottom regular"
fileprivate let str_chooseValue:String = "gCellmake self camera"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_rowName:String = "Talkinself model"
fileprivate let str_addTimeData:String = "style var else all statusgChat"
fileprivate let str_mobileText:String = "MsgCebottom source yet size"

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_managerTitle:String = "TSysline segment make tool use"
fileprivate let str_purchaseLeadingGreenText:String = "to to background screensageCel"
fileprivate let str_touchData:String = "color rightl_Reu"

/*: "roomId" :*/
fileprivate let str_screenData:String = "section make toroomId"

/*: "type" :*/
fileprivate let str_sexValue:[UInt8] = [0x7,0xc,0x3,0xf8]

fileprivate func blockMark(top num: UInt8) -> UInt8 {
    let value = Int(num) - 147
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let str_phoneIconRequestValue:String = "viewtra"

/*: "jumpKey" :*/
fileprivate let str_recordingData:[UInt8] = [0x1e,0x1,0x19,0x4,0x3f,0x11,0xd]

/*: "Send Gift" :*/
fileprivate let str_sectionData:[Character] = ["S","e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let str_shareIndexText:String = "View iself as content sex"
fileprivate let str_writeText:String = "reasono"

/*: "Report" :*/
fileprivate let str_colorMakeText:String = "Reportcell add user"

/*: "uid" :*/
fileprivate let str_sizeSystemData:[UInt8] = [0x35,0x29,0x24]

fileprivate func modelRet(key num: UInt8) -> UInt8 {
    let value = Int(num) - 192
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "name" :*/
fileprivate let str_somethingName:[UInt8] = [0xba,0xb5,0xb9,0xb1]

/*: "Can't send blank message" :*/
fileprivate let str_managerName:[Character] = ["C","a","n","\'","t"," ","s"]
fileprivate let str_appValue:String = "E"
fileprivate let str_restoreValue:String = "collection addnd b"
fileprivate let str_micTitle:String = "MESSAGE"

/*: "user" :*/
fileprivate let str_exitData:String = "makeser"

/*: "content" :*/
fileprivate let str_enterKeyValue:[UInt8] = [0x70,0x7c,0x7d,0x67,0x76,0x7d,0x67]

private func specialContent(top num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "msgType" :*/
fileprivate let str_makeNeedData:String = "list send equal addmsgTyp"
fileprivate let str_intervalTimeData:String = "E"

/*: "txt" :*/
fileprivate let str_infoCellTitle:String = "layerxlayer"

/*: "isGroup" :*/
fileprivate let str_thanFillValue:String = "view extensionisGroup"

/*: "jumps" :*/
fileprivate let str_actualGiftValue:String = "jumpcolor"

/*: "message" :*/
fileprivate let str_fromUnderData:[UInt8] = [0xa3,0x9b,0xa9,0xa9,0x97,0x9d,0x9b]

fileprivate func aShare(block num: UInt8) -> UInt8 {
    let value = Int(num) - 54
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let str_birthValue:String = "extension to leadership toTxtMsg"

/*: "toUid" :*/
fileprivate let str_tableShowValue:String = "toUidadd text"

/*: "msgInfo" :*/
fileprivate let str_delayValue:String = "biology equalmsgInfo"

/*: "params" :*/
fileprivate let str_leadingData:[UInt8] = [0x3e,0x2f,0x3c,0x2f,0x23,0x3d]

/*: "contentType" :*/
fileprivate let str_talkTapData:[UInt8] = [0x57,0x5b,0x5a,0x40,0x51,0x5a,0x40,0x60,0x4d,0x44,0x51]

private func facePage(size num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "ImgMsg" :*/
fileprivate let str_imageColorModelData:String = "snap video version view selfImgMsg"

/*: "img" :*/
fileprivate let str_replyName:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_dismissValue:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let str_whiteText:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let str_theNameContent:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_pictureLockContent:String = "ttitlea"

/*: "giftId" :*/
fileprivate let str_priceData:String = "model user color successgiftId"

/*: "giftNum" :*/
fileprivate let str_greetValue:[Character] = ["g","i","f","t","N"]
fileprivate let str_listContent:[Character] = ["u","m"]

/*: "pkgItemsetId" :*/
fileprivate let str_customDirectionValue:[Character] = ["p","k","g","I","t","e"]
fileprivate let str_makeTitle:String = "table"
fileprivate let str_taskActionData:String = "let in self modelsetId"

/*: "msg" :*/
fileprivate let str_scaleStatusData:String = "soundsg"

/*: "tips" :*/
fileprivate let str_shareTitle:String = "ticell"

/*: "gift" :*/
fileprivate let str_biologyTitle:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let str_hiddenData:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let str_offData:[UInt8] = [0xe6,0xeb,0xef,0xea,0xde,0xe7,0xed]

private func pathOrigin(user num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "All Numbers" :*/
fileprivate let str_mapLabTitle:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let str_tabText:[Character] = ["s","e","n","d","N","u","m"]

/*: "toUser" :*/
fileprivate let str_managerUpValue:String = "toUsercell center share"

/*: "msgIncome" :*/
fileprivate let str_biologyName:String = "msgIncname total top container"
fileprivate let str_voiceValue:String = "everye"

/*: "isConnection" :*/
fileprivate let str_sizeLabManagerName:String = "isContip text"
fileprivate let str_screenToContent:[Character] = ["n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CircleControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class CircleControllerDelegate: ToControllerPositionBaseRecognizerDelegate {
	var actionOn: Bool = true
	var packageMagnitude: Double = -93.6
	var shadowArray: [AnyHashable] = []
	var halogenDictionary: [AnyHashable: String] = [:]
	var userFromQuantity: Int = 99
	var beginEnable: Bool = false
	var punchCardTotal: Int = 36
	var appearDictionary: [AnyHashable: String] = [:]
	var pathOn: Bool = true
	var textTotal: Int = 26
	var incomeDictionary: [AnyHashable: String] = [:]

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = ManagerErrorTransformable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = DistinctConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = ManagerErrorTransformable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_liveText.map{bottomDismiss(layer: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RecordAppManager.share.appConfigMode.disableShootScreen {
            if RecordAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TopShieldView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        capture()
        //: createUIConstraints()
        dataLive()
        //: bindInteraction()
        screen()
        //: addTapGestureRecognizer()
        recognizer()
        //: func__chatRoom_insertTipMsg()
        outcomeList()
        //: chatSettings()
        timeViewSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            picWith()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    
            if (giftTrackView.constraintsAffectingLayout(for: .vertical).count == 78) && (giftTrackView.constraints.count == 152) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let rawData = MovementView(frame: giftTrackView.bounds.intersection(CGRect(x: CGFloat(122.65), y: CGFloat(66), width: CGFloat(0), height: CGFloat(0))))
            rawData.recapitulationOff = animated
            rawData.ownerSum = msgInputView.giftView.keyborHeight
            
            rawData.textClose = { [self] showTitleSwitch in
            self.pathOn = showTitleSwitch
            
            var rawData = infoModel.isOwner
            rawData = false
            if rawData != self.pathOn {
                self.pathOn = rawData
            }
            
            self.pathOn = false
            return self.pathOn
            }
            rawData.fromMagnitude = { [self] digitiserCount in
            self.textTotal = digitiserCount
            
            var rawData = msgInputView.giftView.keyborHeight
                rawData += 1
                if rawData < 65 {
                    rawData = rawData - 1
                }
            if rawData < self.textTotal {
                self.textTotal = rawData
            }
            
            return self.textTotal
            }
            rawData.ofDictionary = { [self] upDictionary in
            self.incomeDictionary = upDictionary
            
            guard var value = self.incomeDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                giftTrackView.addSubview(rawData)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    
            if (msgInputView.bottomView.constraintsAffectingLayout(for: .vertical).count == 78) && (msgInputView.bottomView.constraints.count == 152) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let rawData = MovementView(frame: msgInputView.bottomView.bounds.intersection(CGRect(x: CGFloat(122.65), y: CGFloat(66), width: CGFloat(0), height: CGFloat(0))))
            rawData.recapitulationOff = animated
            rawData.ownerSum = conversationData.userStatus
            
            rawData.textClose = { [self] showTitleSwitch in
            self.beginEnable = showTitleSwitch
            
            var rawData = giftEffectView.isPlayAnimat
            rawData = false
            if rawData != self.beginEnable {
                self.beginEnable = rawData
            }
            
            self.beginEnable = false
            return self.beginEnable
            }
            rawData.fromMagnitude = { [self] digitiserCount in
            self.punchCardTotal = digitiserCount
            
            var rawData = infoModel.identity
                rawData += 1
                if rawData < 65 {
                    rawData = rawData - 1
                }
            if rawData < self.punchCardTotal {
                self.punchCardTotal = rawData
            }
            
            return self.punchCardTotal
            }
            rawData.ofDictionary = { [self] upDictionary in
            self.appearDictionary = upDictionary
            
            guard var value = self.appearDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                msgInputView.bottomView.addSubview(rawData)
            }

	}

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.fireConstraint(join: false)
        //: self.destroyTipsTimer()
        self.dataConverter()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(GroupMessageCell.self, forCellReuseIdentifier: (String(str_withStatusReturnText.suffix(5)) + "ngGr" + str_aspectName.replacingOccurrences(of: "content", with: "o") + String(str_directionName)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(ShowFocusViewCell.self, forCellReuseIdentifier: (String(str_picData) + String(str_routeTitle.suffix(5)) + "ageM" + String(str_managerRowContent.suffix(4)) + "llID"))
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(PublishingCompanyMsgCell.self, forCellReuseIdentifier: (String(str_makeText.prefix(4)) + "ingGro" + String(str_maleContent.prefix(6)) + "GiftMs" + String(str_chooseValue.prefix(5))))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(MemorabiliaView.self, forCellReuseIdentifier: (String(str_rowName.prefix(6)) + String(str_addTimeData.suffix(5)) + "Tips" + String(str_mobileText.prefix(5)) + "llID"))
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_managerTitle.prefix(4)) + "temMes" + String(str_purchaseLeadingGreenText.suffix(7)) + String(str_touchData.suffix(5)) + "seId"))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: CommitViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = CommitViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: ResolutionView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ResolutionView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: ListTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = ListTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: LocationItemThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = LocationItemThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: TerraceView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = TerraceView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func timeViewSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ItemChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        fireConstraint(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func fireConstraint(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(str_screenData.suffix(6))): self.conversationData.groupID, String(bytes: str_sexValue.map{blockMark(top: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: LiveRowRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        LiveRowRequestTool.menuInSub(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TMessageControllerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension CircleControllerDelegate: TMessageControllerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        //: if data.isKind(of: InfoCellData.self) {
        if data.isKind(of: InfoCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = GroupMessageCell(style: .default, reuseIdentifier: (String(str_withStatusReturnText.suffix(5)) + "ngGr" + str_aspectName.replacingOccurrences(of: "content", with: "o") + String(str_directionName)))
            //: let cellData = data as! InfoCellData
            let cellData = data as! InfoCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.voiceUid(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: UserCellData.self) {
        if data.isKind(of: UserCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = ShowFocusViewCell(style: .default, reuseIdentifier: (String(str_picData) + String(str_routeTitle.suffix(5)) + "ageM" + String(str_managerRowContent.suffix(4)) + "llID"))
            //: let cellData = data as! UserCellData
            let cellData = data as! UserCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ProgressToCellData.self) {
        if data.isKind(of: ProgressToCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = PublishingCompanyMsgCell(style: .default, reuseIdentifier: (String(str_makeText.prefix(4)) + "ingGro" + String(str_maleContent.prefix(6)) + "GiftMs" + String(str_chooseValue.prefix(5))))
            //: let cellData = data as! ProgressToCellData
            let cellData = data as! ProgressToCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: SinceCellData.self) {
        if data.isKind(of: SinceCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = MemorabiliaView(style: .default, reuseIdentifier: (String(str_rowName.prefix(6)) + String(str_addTimeData.suffix(5)) + "Tips" + String(str_mobileText.prefix(5)) + "llID"))
            //: let cellData = data as! SinceCellData
            let cellData = data as! SinceCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(RecordAppManager.share.loginUid) {
            if userModel.uid == Int(RecordAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                voiceUid(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                nettUp(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ShowFocusViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! UserCellData
            let imageCellData = cell.messageData as! UserCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(kLet_successErrData)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = BackgroundHandyJSON()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = MemoryHandyJSON()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [BackgroundHandyJSON] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = SinceLiveReactiveCompatible(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.greetAt()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: GroupMessageCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! GroupMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_phoneIconRequestValue.replacingOccurrences(of: "view", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_recordingData.map{$0^116}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NewspaperMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EnvelopeSinceAdministratorThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EnvelopeSinceAdministratorThen.share.instalment(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: MemorabiliaView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! MemorabiliaView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_phoneIconRequestValue.replacingOccurrences(of: "view", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_recordingData.map{$0^116}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NewspaperMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EnvelopeSinceAdministratorThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EnvelopeSinceAdministratorThen.share.instalment(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        photoSend(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func dataConverter() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: ConsumerModel) {
    private func nettUp(user: ConsumerModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TagDataSource(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.toCurrent(cellTitleList: [(String(str_sectionData)).localized, "@", (String(str_shareIndexText.prefix(6)) + str_writeText.replacingOccurrences(of: "reason", with: "nf")).localized, (String(str_colorMakeText.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.userList(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.valueView(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.voiceUid(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.fileAllocationTableFromPracticalApplication(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: ConsumerModel) {
    private func userList(user: ConsumerModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.toAll()
        //: let model = TalkingChatRoomMemberModel()
        let model = TopMeasurable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.hitModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: ConsumerModel) {
    private func valueView(user: ConsumerModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: str_sizeSystemData.map{modelRet(key: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: str_somethingName.map{$0^212}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.titleTip(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func voiceUid(uid: Int) {
        //: EnvelopeSinceAdministratorThen.share.func__pushToUserDetailVC(uid: String(uid))
        EnvelopeSinceAdministratorThen.share.alongUid(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func fileAllocationTableFromPracticalApplication(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = ConditionListReactiveCompatible(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.toView(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func blockModel(msgStr: String, quoteModel _: CopernicanSystemJsonModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            talkTask(showMsg: (String(str_managerName) + str_appValue.lowercased() + String(str_restoreValue.suffix(4)) + "lank " + str_micTitle.lowercased()).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            for uid in msgInputView.atArr {
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: str_recordingData.map{$0^116}, encoding: .utf8)!: (str_exitData.replacingOccurrences(of: "make", with: "u")), String(bytes: str_sizeSystemData.map{modelRet(key: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: str_enterKeyValue.map{specialContent(top: $0)}, encoding: .utf8)!: temStr, (String(str_makeNeedData.suffix(6)) + str_intervalTimeData.lowercased()): (str_infoCellTitle.replacingOccurrences(of: "layer", with: "t")), (String(str_thanFillValue.suffix(7))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(str_actualGiftValue.replacingOccurrences(of: "color", with: "s"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: str_fromUnderData.map{aShare(block: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: str_sexValue.map{blockMark(top: $0)}, encoding: .utf8)!: (String(str_birthValue.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_screenData.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(str_tableShowValue.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_delayValue.suffix(7))): msgInfo, String(bytes: str_leadingData.map{$0^78}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        photoSend(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func miniImg(img: UIImage) {
        //: let imagePath = UserCellData.getChatImagePath(image: img)
        let imagePath = UserCellData.labAdd(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: str_talkTapData.map{facePage(size: $0)}, encoding: .utf8)!: (String(str_imageColorModelData.suffix(6))), (String(str_makeNeedData.suffix(6)) + str_intervalTimeData.lowercased()): (String(str_replyName)), String(bytes: str_dismissValue.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(str_whiteText)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: str_sexValue.map{blockMark(top: $0)}, encoding: .utf8)!: (String(str_imageColorModelData.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_screenData.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_delayValue.suffix(7))): msgInfo, String(bytes: str_leadingData.map{$0^78}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        photoSend(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func photoSend(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            talkTask(showMsg: kLet_homeText)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(str_whiteText))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(str_whiteText))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(str_whiteText))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: LiveRowRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        LiveRowRequestTool.textualMatterCapacity(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.quantityroduction(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(str_delayValue.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(str_theNameContent))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((str_pictureLockContent.replacingOccurrences(of: "title", with: "ot") + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(str_pictureLockContent.replacingOccurrences(of: "title", with: "ot") + "lMfCoin")] as! NSNumber
                //: RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func quantityroduction(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard RecordAppManager.share.loginUserMode.status != 1 else {
            guard RecordAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    talkTask(showMsg: errorStr)
                }
                //: return
                return
            }

            //: EnvelopeSinceAdministratorThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            EnvelopeSinceAdministratorThen.share.colorFailure(clickEvent: kLet_voiceText, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.disoblige()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            fauna(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: EnvelopeSinceAdministratorThen.share.func__pushToSubscribeAlert()
            EnvelopeSinceAdministratorThen.share.dismissBy()

            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                talkTask(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension CircleControllerDelegate: HiddenViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func root(giftModel _: RecordTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func nameAll() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func giftBy(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = SharedColorThen()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.titleTip(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func removeName(actionType: ActualCustomReflectable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            sprechstimmePhotos()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func queryed(giftModel _: RecordTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func detailCall(giftModel: RecordTransformable, giftNum: String, model: TopMeasurable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            talkTask(showMsg: kLet_homeText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(str_tableShowValue.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(str_priceData.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_greetValue) + String(str_listContent))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(str_screenData.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_customDirectionValue) + str_makeTitle.replacingOccurrences(of: "table", with: "m") + String(str_taskActionData.suffix(5)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: LiveRowRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        LiveRowRequestTool.rowShow(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.quantityroduction(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.equalNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.sharedTo(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func wrapImage(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func ageExecute(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func utiliserRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.utiliserRecognizer(tap)
    }

    //: func func__starRecordBtnDown() {}
    func statusDown() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func pan(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension CircleControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func sprechstimmePhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        HiddenToolReactiveCompatible.signatureRoom(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = DistinctReactiveCompatible.squatVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: EnvelopeSinceAdministratorThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                EnvelopeSinceAdministratorThen.share.viewDoing()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.miniImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func makeRequest() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        HiddenToolReactiveCompatible.playerHead(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.miniImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func accumulationEvent(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(str_screenData.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    //: func func__TipsNotification() {
    func sizeCurrent() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(createRequest(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_firstTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func createRequest(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: SinceCellData  = userinfo["msg"] as! SinceCellData
        let celldata: SinceCellData = userinfo[(str_scaleStatusData.replacingOccurrences(of: "sound", with: "m"))] as! SinceCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func fauna(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: str_enterKeyValue.map{specialContent(top: $0)}, encoding: .utf8)!: tipStr, (String(str_makeNeedData.suffix(6)) + str_intervalTimeData.lowercased()): (str_shareTitle.replacingOccurrences(of: "cell", with: "ps"))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func outcomeList() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard RecordAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard RecordAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: RecordAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.fauna(tipStr: RecordAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func sharedTo(extral: [String: Any?], model: TopMeasurable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_delayValue.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(str_thanFillValue.suffix(7)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(str_makeNeedData.suffix(6)) + str_intervalTimeData.lowercased())] = (String(str_biologyTitle))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_delayValue.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: str_sizeSystemData.map{modelRet(key: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(str_hiddenData)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: str_offData.map{pathOrigin(user: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(str_mapLabTitle)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(str_biologyTitle))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(str_biologyTitle))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(str_hiddenData))] = "\(giftdic[(String(str_tabText))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(str_managerUpValue.prefix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_biologyName.prefix(6)) + str_voiceValue.replacingOccurrences(of: "every", with: "om"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_biologyName.prefix(6)) + str_voiceValue.replacingOccurrences(of: "every", with: "om"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_theNameContent))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        bestowment(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((str_pictureLockContent.replacingOccurrences(of: "title", with: "ot") + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(str_pictureLockContent.replacingOccurrences(of: "title", with: "ot") + "lMfCoin")] as! NSNumber
            //: RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RecordAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.observerCoin(needReload: false, mf_coin: RecordAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.currentMe()
    }

    //: func initCachEffectData() {
    func picWith() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.equalMedium(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func bestowment(giftDic: [String: Any], model: TopMeasurable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.family(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EqualGiftManager.share.liveOf() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [InfoMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.dealModel(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.ofPlay(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func bridge(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EqualGiftManager.share.liveOf() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [InfoMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.dealModel(model: giftModel as! PartAnimatModel)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.modelArr(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension CircleControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func capture() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func dataLive() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_timeScreenValue)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(kLet_timeScreenValue)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - kLet_messageTopText)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func screen() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = SharedColorThen()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TUIMessageCellData).isKind(of: InfoCellData.self) {
                    if (cellData as! TUIMessageCellData).isKind(of: InfoCellData.self) {
                        //: let groupCellData = cellData as! InfoCellData
                        let groupCellData = cellData as! InfoCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(str_theNameContent))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: SharedConversationListener.shared.rx
        SharedConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(str_sizeLabManagerName.prefix(5)) + String(str_screenToContent)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
