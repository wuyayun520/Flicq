
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_refData:[UInt8] = [0x88,0x8f,0x88,0x95,0xc9,0x82,0x8e,0x85,0x84,0x93,0xdb,0xc8,0xc1,0x89,0x80,0x92,0xc1,0x8f,0x8e,0x95,0xc1,0x83,0x84,0x84,0x8f,0xc1,0x88,0x8c,0x91,0x8d,0x84,0x8c,0x84,0x8f,0x95,0x84,0x85]

private func stopWill(view num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "F5F5F5" :*/
fileprivate let str_signStateIfData:[Character] = ["F","5","F","5","F","5"]

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_subValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x","t","M","s","g","C"]
fileprivate let str_playerName:[Character] = ["e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_makeExistTitle:String = "Talkifor result name"
fileprivate let str_blockValue:String = "atImif message background"
fileprivate let str_labActionIndexTitle:String = "self in info view selfgCellID"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_cornerSizeContent:String = "inside data manager letTalki"
fileprivate let str_appData:String = "atViuser data show manager"
fileprivate let str_hiddenData:[Character] = ["s","g","C","e","l","l","I","D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_makeData:String = "Talkinreason gift image equal view"
fileprivate let str_fileCellActionName:String = "detail"
fileprivate let str_imageResultTitle:[Character] = ["s","g","C","e","l","l","I","D"]

/*: "MarkAppMessageCell" :*/
fileprivate let str_playerData:String = "contact errorTalki"
fileprivate let str_transactionName:String = "tAskForelse as fill"
fileprivate let str_tagTitle:String = "error makeGiftMs"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_appValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i"]
fileprivate let str_renderContent:String = "psMformal name video"
fileprivate let str_namePopValue:String = "sgCellIDremote social"

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_inviteData:String = "Talkin indicator make make"
fileprivate let str_touchTitle:String = "atMalabel frame var extension"
fileprivate let str_languageValue:[Character] = ["t","c","h","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_shareName:String = "view hiddenTalk"
fileprivate let str_sectionValue:String = "tAudfile let"
fileprivate let str_norLabelManagerName:[Character] = ["C","e","l","l","I","D"]

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_bottomPhotoContent:[UInt8] = [0x44,0x49,0x6c,0x6c,0x65,0x43,0x67,0x73,0x4d,0x70,0x6d,0x75,0x4a,0x66,0x69,0x74,0x6f,0x4e,0x6d,0x65,0x74,0x73,0x79,0x53,0x74,0x61,0x68,0x43,0x67,0x6e,0x69,0x6b,0x6c,0x61,0x54]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_voiceValue:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C"]
fileprivate let str_askText:String = "ell_Rbar video live cloud"
fileprivate let str_contentBigTrueName:[Character] = ["e","u","s","e","I","d"]

/*: "TalkingChatMapMsgCellID" :*/
fileprivate let str_youValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M"]
fileprivate let str_instanceName:[Character] = ["a","p","M","s","g","C","e","l","l","I","D"]

/*: "#128CFF" :*/
fileprivate let str_likeName:String = "#128CFFvar self wrap"

/*: "Feedback" :*/
fileprivate let str_buttonToData:String = "text effect case selectedFeed"

/*: "#8370FF" :*/
fileprivate let str_greetModelTableValue:[Character] = ["#","8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let str_conversationData:String = "in raw style viewQuick"
fileprivate let str_scaleName:String = "inbutton"

/*: "Beauty Photos" :*/
fileprivate let str_hiddenViewName:String = "Beauposition view"
fileprivate let str_labelObserverName:String = "happtapps"

/*: "url" :*/
fileprivate let str_actualName:String = "makerl"

/*: "unlockGift" :*/
fileprivate let str_viewToTitle:String = "unlockGitem for"
fileprivate let str_stateLabelValue:[Character] = ["i","f","t"]

/*: "%d" :*/
fileprivate let str_childFrameThanData:String = "%dmodel version"

/*: "isBurn" :*/
fileprivate let str_permissionData:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let str_makeName:[Character] = ["g"]
fileprivate let str_equalAdjustmentTitle:[Character] = ["i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let str_managerImageTitle:String = "mode app tick letrYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_mortalData:String = "range view selfGJ:CallCu"
fileprivate let str_screenUserText:String = "stofill"

/*: "changeDirection" :*/
fileprivate let str_rowName:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let str_tempName:String = "extrbottom"

/*: "msgInfo" :*/
fileprivate let str_frameWindowContent:String = "msgInfoclass top"

/*: "toUid" :*/
fileprivate let str_lessValue:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let str_sendShareExecuteData:[Character] = ["w","a","n","t","G"]
fileprivate let str_labelGiftTitle:String = "datat"

/*: "jumpKey" :*/
fileprivate let str_recordValueName:[Character] = ["j","u"]
fileprivate let str_downTitleData:[Character] = ["m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let str_actionName:String = "coin label front celltruePe"
fileprivate let str_taskClearText:String = "pop"

/*: "logType" :*/
fileprivate let str_requestTitle:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let str_appTitle:[UInt8] = [0xc1,0xdf,0xcb,0xf8,0xd5,0xdc,0xc9]

private func buttonTotal(finish num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "txt" :*/
fileprivate let str_blockName:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let str_thirdData:String = "tysegment"

/*: "QuickMsg" :*/
fileprivate let str_dismissValue:String = "QuickMsglet info table"

/*: "params" :*/
fileprivate let str_sizeKeyText:[UInt8] = [0x47,0x38,0x49,0x38,0x44,0x4a]

fileprivate func viewText(user num: UInt8) -> UInt8 {
    let value = Int(num) + 41
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let str_fatalData:[UInt8] = [0x5a,0x56,0x57,0x4d,0x5c,0x57,0x4d]

private func userSize(info num: UInt8) -> UInt8 {
    return num ^ 57
}

/*: "id" :*/
fileprivate let str_screenTitle:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let str_dismissName:String = "txtIdfor return table origin"

/*: "message" :*/
fileprivate let str_reContent:[UInt8] = [0xca,0xc2,0xd0,0xd0,0xbe,0xc4,0xc2]

fileprivate func periodicTable(app num: UInt8) -> UInt8 {
    let value = Int(num) - 93
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let str_atRequestName:String = "TxtMsglet number text else"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_provinceText:[UInt8] = [0x6d,0x54,0x52,0x58,0x5e,0x1b,0x53,0x5e,0x57,0x57,0x54,0x1b,0x5f,0x54,0x4c,0x55,0x57,0x54,0x5a,0x5f,0x1b,0x5d,0x5a,0x52,0x57,0x5e,0x5f,0x15,0x1b,0x6b,0x49,0x54,0x58,0x5e,0x5f,0x4e,0x49,0x5e]

/*: "length" :*/
fileprivate let str_hiddenHumanData:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let str_filePleaseValue:String = "AUDIO"

/*: "audioId" :*/
fileprivate let str_hiddenValue:String = "audiframe"
fileprivate let str_makeRawData:[Character] = ["I","d"]

/*: "audioLength" :*/
fileprivate let str_regionData:[UInt8] = [0x3d,0x29,0x38,0x35,0x33,0x10,0x39,0x32,0x3b,0x28,0x34]

private func tagImage(view num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "audioSandbox" :*/
fileprivate let str_makeValue:String = "resentd"
fileprivate let str_backData:String = "ioSalet add"

/*: "senderId" :*/
fileprivate let str_keyMessageName:[UInt8] = [0x98,0x8e,0x85,0x8f,0x8e,0x99,0xa2,0x8f]

private func filePic(equal num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "AudioMsg" :*/
fileprivate let str_toMakeData:[Character] = ["A","u","d","i","o","M","s"]
fileprivate let str_tagContent:String = "tool"

/*: "Can't send blank message" :*/
fileprivate let str_backIconStatusValue:String = "Can\'t"
fileprivate let str_pathTouchName:[Character] = ["d"," ","b","l","a","n","k"]
fileprivate let str_cellNameText:String = " messageview top color"

/*: "quoteMsgId" :*/
fileprivate let str_cancelName:String = "quotvoice"

/*: "quoteMsgInfo" :*/
fileprivate let str_tabData:[Character] = ["q","u","o","t","e","M"]
fileprivate let str_partyValue:String = "let cutsgInfo"

/*: "contentType" :*/
fileprivate let str_leadingName:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let str_backTableName:String = "white"
fileprivate let str_lastName:String = "mgMsgfor in"

/*: "img" :*/
fileprivate let str_tagData:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_momentText:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let str_hitData:[UInt8] = [0x22,0x34,0x32,0x33,0x2e,0x2c,0x12,0x24,0x2d,0x23]

fileprivate func locationItem(gift num: UInt8) -> UInt8 {
    let value = Int(num) + 65
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let str_momentData:String = "bottomc"

/*: "VideoMsg" :*/
fileprivate let str_subContent:String = "next showVideo"
fileprivate let str_markValue:[Character] = ["M","s","g"]

/*: "video" :*/
fileprivate let str_promptTableData:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let str_indexArrowData:[UInt8] = [0x67,0x6d,0x49,0x72,0x65,0x76,0x6f,0x63]

/*: "videoUrl" :*/
fileprivate let str_laboratoryName:[UInt8] = [0x8,0x17,0x1a,0x1b,0x11,0x2b,0xc,0x12]

private func auditoryImage(input num: UInt8) -> UInt8 {
    return num ^ 126
}

/*: "duration" :*/
fileprivate let str_onlineData:[UInt8] = [0x47,0x56,0x51,0x42,0x57,0x4a,0x4c,0x4d]

/*: "len" :*/
fileprivate let str_laboratoryData:[UInt8] = [0x41,0x48,0x43]

private func worldView(menu num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "map" :*/
fileprivate let str_modelHeadTitle:String = "matitle"

/*: "latlon" :*/
fileprivate let str_enterData:[UInt8] = [0x33,0x28,0x3b,0x33,0x36,0x35]

fileprivate func signIconShare(name num: UInt8) -> UInt8 {
    let value = Int(num) - 199
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let str_taskData:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "MapMsg" :*/
fileprivate let str_closedValue:[Character] = ["M","a","p","M","s","g"]

/*: "Documents/User/voice/" :*/
fileprivate let str_scaleData:String = "Documenormal share"
fileprivate let str_sectionData:[Character] = ["n","t"]
fileprivate let str_inputTopCenterText:String = "s/Usshare reason view"
fileprivate let str_modelFrameData:String = "ice/frame value manager"

/*: "Documents/User/record/" :*/
fileprivate let str_enableCollectionText:String = "Docuthe return error pop last"
fileprivate let str_pageTitle:String = "ser/model call observer self color"

/*: "msgIncome" :*/
fileprivate let str_makeText:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "mfBean" :*/
fileprivate let str_barDismissEqualData:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "WantGift" :*/
fileprivate let str_billMessageValue:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_dataVideoContent:String = "_Privlet party style type error"
fileprivate let str_dismissEqualData:String = "tTipview click"

/*: "msgId" :*/
fileprivate let str_colorStyleData:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_observerName:[Character] = ["t","o","t","a","l","M","f","C","o","i"]
fileprivate let str_biologyListValue:String = "mini"

/*: "attachSysMsg" :*/
fileprivate let str_promptButtonValue:[Character] = ["a","t","t","a"]
fileprivate let str_commentFatalName:String = "action self layer content cellchSy"

/*: "intimatePhoto" :*/
fileprivate let str_behindName:[Character] = ["i","n","t","i","m","a","t","e","P"]
fileprivate let str_targetName:String = "hoplayo"

/*: "&type= :*/
fileprivate let str_selectTitle:String = "log laboratory&type="

/*: _ :*/
fileprivate let str_sizeImageTryData:String = "item"

/*: "totalIntimate" :*/
fileprivate let str_itemMakeText:String = "priceopriceal"
fileprivate let str_formatValue:String = "play varInt"
fileprivate let str_tapTitle:[Character] = ["i","m","a","t","e"]

/*: "uid" :*/
fileprivate let str_mainText:[UInt8] = [0xcc,0xd0,0xdd]

/*: "intimate" :*/
fileprivate let str_nextTextValue:[UInt8] = [0x53,0x54,0x4e,0x53,0x57,0x5b,0x4e,0x5f]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_bottomStatusData:[UInt8] = [0xd0,0xf2,0xfd,0xb4,0xe7,0xb3,0xe6,0xe0,0xf6,0xb3,0xe7,0xfb,0xf6,0xb3,0xf5,0xe6,0xfd,0xf0,0xe7,0xfa,0xfc,0xfd,0xb3,0xf7,0xe6,0xf6,0xb3,0xe7,0xfc,0xb3,0xea,0xfc,0xe6,0xe1,0xb3,0xff,0xf2,0xf0,0xf8,0xb3,0xfc,0xf5,0xb3,0xfa,0xfd,0xe7,0xfa,0xfe,0xf2,0xf0,0xea,0xb3,0xff,0xf6,0xe5,0xf6,0xff,0xbd]

/*: "giftNum" :*/
fileprivate let str_blockTitle:String = "rawi"
fileprivate let str_topText:[Character] = ["f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let str_availableTitle:String = "info label forpkgI"
fileprivate let str_layerData:[Character] = ["e","t","I","d"]

/*: "from" :*/
fileprivate let str_ofLabData:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let str_fieldData:String = "wantGiftstring to"
fileprivate let str_infoLineTitle:String = "make false data returnId"

/*: "WantGiftMsg" :*/
fileprivate let str_userTitlePathValue:String = "Wantleft content string"

/*: "gift" :*/
fileprivate let str_targetData:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let str_countToData:String = "view listView i"
fileprivate let str_constraintGenderAddValue:String = "nfroom"

/*: "Notes" :*/
fileprivate let str_hiddenText:String = "Noteselse text last index back"

/*: "Report" :*/
fileprivate let str_tagValue:String = "r"
fileprivate let str_enableValue:[Character] = ["e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let str_blockMicName:String = "succeed nameView "
fileprivate let str_transactionValue:String = "top in background shared recei"

/*: "He" :*/
fileprivate let str_mainData:String = "free manager kit self returnHe"

/*: "She" :*/
fileprivate let str_moveData:String = "Shetry stop type color make"

/*: "%@ is using too low a version" :*/
fileprivate let str_sizeNameData:String = "%@ is gift return extension height"
fileprivate let str_directionValue:String = " too lbutton else var type aspect"
fileprivate let str_promptContent:String = " vermanager return window"
fileprivate let str_checkText:String = "SION"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_acquisitionName:String = "Shooawake self"
fileprivate let str_styleSendTrueName:String = "true live icon button sharedeo a"
fileprivate let str_parentData:String = "ast 5date data model"
fileprivate let str_videoOfContent:String = "modelds"

/*: "Upload failed" :*/
fileprivate let str_equalColorObserverText:[Character] = ["U","p","l","o","a","d"," ","f","a","i","l"]
fileprivate let str_seekContent:String = "ebutton"

/*: "播放状态: :*/
fileprivate let str_imageTitleData:String = "\u{64ad}放状态:"

/*: "c2c_" :*/
fileprivate let str_indexRemoveValue:String = "at actual icon selfc2c_"

/*: "msg" :*/
fileprivate let str_equalActualText:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_imageManagerEqualData:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_nameCallData:[UInt8] = [0x36,0x0,0x1a,0x4f,0xc,0xe,0x1,0x4f,0x1a,0x1c,0xa,0x4f,0x1b,0x7,0xa,0x4f,0x19,0x6,0xb,0xa,0x0,0x4f,0xc,0x7,0xe,0x1b,0x4f,0x9,0xa,0xe,0x1b,0x1a,0x1d,0xa,0x4f,0x1b,0x0,0x4f,0x8,0x6,0x19,0xa,0x4f,0x4a,0x2f,0x4f,0xe,0x4f,0xc,0xe,0x3,0x3]

private func ageConstraint(send num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "cellData" :*/
fileprivate let str_targetValue:[Character] = ["c"]
fileprivate let str_viewLabelTitle:String = "ellDatashared share"

/*: "System notification" :*/
fileprivate let str_fileOfTitle:String = "bottom returnSystem"
fileprivate let str_statusTargetData:String = "fconstraintcatconstrainto"
fileprivate let str_landmarkValue:String = "view"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FromBannerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import UIKit
import UIKit

//: import AVFAudio
import AVFAudio

//: import Photos
import Photos

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ShowFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class FromBannerDelegate: ToControllerPositionBaseRecognizerDelegate {
	var containmentOn: Bool = false
	var redConstraintDoing: Bool = true
	var postClose: Bool = true

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = RowModelType()
    private var userWrap = RowModelType() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ShowFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: RowModelType = RowModelType()) {
    init(chatID: String, isFrom: ShowFromEnum = .Normal, userWrap: RowModelType = RowModelType()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        FromBannerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        MarkThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == AreaReactiveCompatible.getXiaoMiID() {
        if targetId == AreaReactiveCompatible.sink() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == AreaReactiveCompatible.getCustomerServiceID() {
        } else if targetId == AreaReactiveCompatible.inheritPlayer() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_refData.map{stopWill(view: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RecordAppManager.share.appConfigMode.disableShootScreen {
            if RecordAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TopShieldView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        modelAnimation()
        //: createUI()
        awake()
        //: createUIConstraints()
        provoke()
        //: bindInteraction()
        chapiterStatus()
        //: addTapGestureRecognizer()
        recognizer()
        //: func__requestChatInfo()
        sendFor()
        //: func__getFillDraftText()
        addButtonName()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        LiveCellReactiveCompatible.shared.atDelegate()
        //: func__addVoiceNotification()
        toNotification()
        //: addReadPeceiptNotification()
        startNotification()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            cachOfGift()
            //: func__TipsNotification()
            sinceBuildVoice()
            //: if RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue {
                //: func__setReplyTipsView()
                funcFromWeltanschauung()
            }
        }
    
            if (contentView.sizeThatFits(.zero).width == 61.51) && (contentView.convert(CGRect(x: 0, y: CGFloat(0), width: 0, height: 0), to: contentView.superview).origin.x == 85.18) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let buildCell = PostButtonView()
            buildCell.headQuantity = msgInputView.giftView.keyborHeight
            buildCell.textSharedOn = { [self] standBlockOff in
            self.postClose = standBlockOff
            
            var buildCell = msgInputView.giftView.showOtherInputView
                buildCell = true
                buildCell = true
            if buildCell != self.postClose {
                self.postClose = buildCell
            }
            
            return self.postClose
            }
                contentView.addSubview(buildCell)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        style(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        style(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            fileOf()
        }
    
            if (coverView.layer.contentsRect.origin.y != 0) && (coverView.layer.anchorPoint.x != 0.5) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let playerUser = PostButtonView(frame: coverView.bounds.union(CGRect(x: CGFloat(85), y: CGFloat(0), width: CGFloat(72), height: CGFloat(0))))
            playerUser.headQuantity = msgInputView.duration
            playerUser.textSharedOn = { [self] standBlockOff in
            self.containmentOn = standBlockOff
            
            var playerUser = giftEffectView.giftAnimatView.paused
                playerUser = false
                playerUser = false
            if playerUser != self.containmentOn {
                self.containmentOn = playerUser
            }
            
            return self.containmentOn
            }
                coverView.addSubview(playerUser)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(str_signStateIfData)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(KeyTalkingViewCell.self, forCellReuseIdentifier: (String(str_subValue) + String(str_playerName)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(ShowFocusViewCell.self, forCellReuseIdentifier: (String(str_makeExistTitle.prefix(5)) + "ngCh" + String(str_blockValue.prefix(4)) + "ageMs" + String(str_labActionIndexTitle.suffix(7))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(ChatThen.self, forCellReuseIdentifier: (String(str_cornerSizeContent.suffix(5)) + "ngCh" + String(str_appData.prefix(4)) + "deoM" + String(str_hiddenData)))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(ProgressViewCell.self, forCellReuseIdentifier: (String(str_makeData.prefix(6)) + "gChatGift" + str_fileCellActionName.replacingOccurrences(of: "detail", with: "M") + String(str_imageResultTitle)))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(MarkAppMessageCell.self, forCellReuseIdentifier: (String(str_playerData.suffix(5)) + "ngCha" + String(str_transactionName.prefix(7)) + String(str_tagTitle.suffix(6)) + "gCell"))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(MemorabiliaView.self, forCellReuseIdentifier: (String(str_appValue) + String(str_renderContent.prefix(3)) + String(str_namePopValue.prefix(8))))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(TagReactiveCompatible.self, forCellReuseIdentifier: (String(str_inviteData.prefix(4)) + "ingCh" + String(str_touchTitle.prefix(4)) + String(str_languageValue)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(InfoViewCell.self, forCellReuseIdentifier: (String(str_shareName.suffix(4)) + "ingCha" + String(str_sectionValue.prefix(4)) + "ioMsg" + String(str_norLabelManagerName)))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(ChatErrorView.self, forCellReuseIdentifier: String(bytes: str_bottomPhotoContent.reversed(), encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_voiceValue) + String(str_askText.prefix(5)) + String(str_contentBigTrueName)))

        //: msgVC.tableView.register(TalkingChatMapMsgCell.self, forCellReuseIdentifier: "TalkingChatMapMsgCellID")
        msgVC.tableView.register(ChatMsgCell.self, forCellReuseIdentifier: (String(str_youValue) + String(str_instanceName)))

        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ItemTipReactiveCompatible = {
        //: let m = TalkingCallMenuManager()
        let m = ItemTipReactiveCompatible()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: CommitViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: BackgroundViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = CommitViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(str_likeName.prefix(7))))!, .font: UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_buttonToData.suffix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pathClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ResolutionView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ResolutionView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: LocationItemThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = LocationItemThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_greetModelTableValue))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_conversationData.suffix(5)) + " Greet" + str_scaleName.replacingOccurrences(of: "button", with: "g")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.viewColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(observe), for: .touchUpInside)
        //: btn.isHidden = !RecordAppManager.share.appUserConfigMode.showDirectGreet || RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue || RecordAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !RecordAppManager.share.appUserConfigMode.showDirectGreet || RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue || RecordAppManager.share.appStatus == MaculeRelatableTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: ToffeeNosedTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = ToffeeNosedTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ResultTipTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = ResultTipTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.roomView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: CountersectionPointThen = {
        //: let v = TalkingTranslationResultView()
        let v = CountersectionPointThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.blockModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_greetModelTableValue))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_hiddenViewName.prefix(4)) + "ty P" + str_labelObserverName.replacingOccurrences(of: "app", with: "o")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.viewColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sectionSend), for: .touchUpInside)
        //: btn.isHidden = RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue || RecordAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue || RecordAppManager.share.appStatus == MaculeRelatableTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: MenuViewDelegate = {
        //: let v = TalkingLiveRoomGamesView()
        let v = MenuViewDelegate()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: ShitlistBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = ShitlistBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: TextScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = TextScreenView()
        //: v.isHidden = (RecordAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (RecordAppManager.share.appStatus == MaculeRelatableTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: CookieView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = CookieView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.skiJumpUp()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension FromBannerDelegate: ColorBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func handleOfView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.miniskirtResetCuttingEdgeWorldView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension FromBannerDelegate: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func observe() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        MarkThen.greetingRow(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.talkTask(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.executeInDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func sectionSend() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        MarkThen.quantity(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = PhotoViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().laughDoing(urlStr: dict?[(str_actualName.replacingOccurrences(of: "make", with: "u"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(str_viewToTitle.prefix(7)) + String(str_stateLabelValue))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = UpwardJsonModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_permissionData.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_makeName) + String(str_equalAdjustmentTitle))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.pointMp(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: FitInCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: FitInCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_managerImageTitle.suffix(9)))].stringValue == (String(str_mortalData.suffix(9)) + str_screenUserText.replacingOccurrences(of: "fill", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(str_rowName))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(str_tempName.replacingOccurrences(of: "bottom", with: "a"))][(String(str_frameWindowContent.prefix(7)))][(String(str_lessValue))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = SerrationCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = SerrationCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: NameBalloonThen.self) == true || cellData.isKind(of: UserCellData.self) == true || cellData.isKind(of: SerrationCellData.self) == true || cellData.isKind(of: MarkMakeCellData.self) == true || cellData.isKind(of: PartCellData.self) == true {
        if cellData.isKind(of: NameBalloonThen.self) == true || cellData.isKind(of: UserCellData.self) == true || cellData.isKind(of: SerrationCellData.self) == true || cellData.isKind(of: MarkMakeCellData.self) == true || cellData.isKind(of: PartCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        MarkThen.groupView(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        MarkThen.latestData(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = makeData(data: data)

        //: if data.isKind(of: SerrationCellData.self) {
        if data.isKind(of: SerrationCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = KeyTalkingViewCell(style: .default, reuseIdentifier: (String(str_subValue) + String(str_playerName)))
            //: let cellData = data as! SerrationCellData
            let cellData = data as! SerrationCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.list() {
                //: if cellData.msgModel.quoteMsgInfo.uid == RecordAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == RecordAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = RecordAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = RecordAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: UserCellData.self) {
        if data.isKind(of: UserCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = ShowFocusViewCell(style: .default, reuseIdentifier: (String(str_makeExistTitle.prefix(5)) + "ngCh" + String(str_blockValue.prefix(4)) + "ageMs" + String(str_labActionIndexTitle.suffix(7))))
            //: let cellData = data as! UserCellData
            let cellData = data as! UserCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: MarkMakeCellData.self) {
        if data.isKind(of: MarkMakeCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = ChatThen(style: .default, reuseIdentifier: (String(str_cornerSizeContent.suffix(5)) + "ngCh" + String(str_appData.prefix(4)) + "deoM" + String(str_hiddenData)))
            //: let cellData = data as! MarkMakeCellData
            let cellData = data as! MarkMakeCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SoundCellData.self) {
        if data.isKind(of: SoundCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = ProgressViewCell(style: .default, reuseIdentifier: (String(str_makeData.prefix(6)) + "gChatGift" + str_fileCellActionName.replacingOccurrences(of: "detail", with: "M") + String(str_imageResultTitle)))
            //: let cellData = data as! SoundCellData
            let cellData = data as! SoundCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: HorizonCellData.self) {
        if data.isKind(of: HorizonCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = MarkAppMessageCell(style: .default, reuseIdentifier: (String(str_playerData.suffix(5)) + "ngCha" + String(str_transactionName.prefix(7)) + String(str_tagTitle.suffix(6)) + "gCell"))
            //: let cellData = data as! HorizonCellData
            let cellData = data as! HorizonCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = RecordTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.untilTurnPlayer(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_sendShareExecuteData) + str_labelGiftTitle.replacingOccurrences(of: "data", with: "if")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: NameBalloonThen.self) {
        if data.isKind(of: NameBalloonThen.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = InfoViewCell(style: .default, reuseIdentifier: (String(str_shareName.suffix(4)) + "ingCha" + String(str_sectionValue.prefix(4)) + "ioMsg" + String(str_norLabelManagerName)))
            //: let cellData = data as! NameBalloonThen
            let cellData = data as! NameBalloonThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: FitInCellData.self) {
        if data.isKind(of: FitInCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = TagReactiveCompatible(style: .default, reuseIdentifier: (String(str_inviteData.prefix(4)) + "ingCh" + String(str_touchTitle.prefix(4)) + String(str_languageValue)))
            //: let cellData = data as! FitInCellData
            let cellData = data as! FitInCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SinceCellData.self) {
        if data.isKind(of: SinceCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = MemorabiliaView(style: .default, reuseIdentifier: (String(str_appValue) + String(str_renderContent.prefix(3)) + String(str_namePopValue.prefix(8))))
            //: let cellData = data as! SinceCellData
            let cellData = data as! SinceCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: LocationReactiveCompatible.self) {
        if data.isKind(of: LocationReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = ChatErrorView(style: .default, reuseIdentifier: String(bytes: str_bottomPhotoContent.reversed(), encoding: .utf8)!)
            //: let cellData = data as! LocationReactiveCompatible
            let cellData = data as! LocationReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: PartCellData.self) {
        if data.isKind(of: PartCellData.self) {
            //: let cell = TalkingChatMapMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMapMsgCellID")
            let cell = ChatMsgCell(style: .default, reuseIdentifier: (String(str_youValue) + String(str_instanceName)))
            //: let cellData = data as! PartCellData
            let cellData = data as! PartCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != AreaReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != AreaReactiveCompatible.inheritPlayer() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: EnvelopeSinceAdministratorThen.share.func__pushToUserDetailVC(uid: String(uid))
            EnvelopeSinceAdministratorThen.share.alongUid(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ShowFocusViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! UserCellData
            let imageCellData = cell.messageData as! UserCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                //: let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: true, needAlert: needAlert)
                let num = MarkThen.postPicture(isImg: true, needAlert: needAlert)
                /// 需要解锁的照片
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                let vc = TalkingManagerRecognizerDelegate(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 1)
                        self.loadDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_cornerValue, type: 1)
                        //: return
                        return
                    }
                    //: RecordAppManager.share.loginUserMode.updateResidualNum(type: 1, num: max(0, num - 1))
                    RecordAppManager.share.loginUserMode.specialise(type: 1, num: max(0, num - 1))
//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sinceTouchLive()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_successErrData)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = BackgroundHandyJSON()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = MemoryHandyJSON()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [BackgroundHandyJSON] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = SinceLiveReactiveCompatible(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sinceTouchLive()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ChatThen.self) { // 视频
            //: let videoCellData = cell.messageData as! MarkMakeCellData
            let videoCellData = cell.messageData as! MarkMakeCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ErrorViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sinceTouchLive()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                //: let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: false, needAlert: needAlert)
                let num = MarkThen.postPicture(isImg: false, needAlert: needAlert)
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                let vc = RangePreviewVc(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 2)
                        self.loadDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_cornerValue, type: 2)
                        //: return
                        return
                    }
                    //: RecordAppManager.share.loginUserMode.updateResidualNum(type: 2, num: max(0, num - 1))
                    RecordAppManager.share.loginUserMode.specialise(type: 2, num: max(0, num - 1))

//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                sinceTouchLive()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: InfoViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! InfoViewCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! NameBalloonThen, cellView: audioCell)
            bottomError(cellData: audioCell.cellData as! NameBalloonThen, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: KeyTalkingViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! KeyTalkingViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_tempName.replacingOccurrences(of: "bottom", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_recordValueName) + String(str_downTitleData))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_recordValueName) + String(str_downTitleData))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(str_actionName.suffix(6)) + "rsonAu" + str_taskClearText.replacingOccurrences(of: "pop", with: "th")) {
                    //: if RecordAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if RecordAppManager.share.loginUserMode.isTPAuth == EqualConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if RecordAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if RecordAppManager.share.loginUserMode.isTPAuth == EqualConvertible.unknown.rawValue {
                        //: EnvelopeSinceAdministratorThen.share.func__pushUserVerifyController(toast: nil)
                        EnvelopeSinceAdministratorThen.share.thirdToast(toast: nil)
                        //: } else if RecordAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if RecordAppManager.share.loginUserMode.isTPAuth == EqualConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = ShirtButtonThen()
                        //: EnvelopeSinceAdministratorThen.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        EnvelopeSinceAdministratorThen.share.viewDoing()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: EnvelopeSinceAdministratorThen.share.func__pushUserVerifyController(toast: nil)
                        EnvelopeSinceAdministratorThen.share.thirdToast(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NewspaperMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EnvelopeSinceAdministratorThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EnvelopeSinceAdministratorThen.share.instalment(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_managerImageTitle.suffix(9)))] as? String == (String(str_mortalData.suffix(9)) + str_screenUserText.replacingOccurrences(of: "fill", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_frameWindowContent.prefix(7)))][(String(str_requestTitle))].intValue == 1 {
                    //: checkAndCallVoice()
                    keyBlock()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    partyVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: MemorabiliaView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! MemorabiliaView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_tempName.replacingOccurrences(of: "bottom", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_recordValueName) + String(str_downTitleData))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NewspaperMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EnvelopeSinceAdministratorThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EnvelopeSinceAdministratorThen.share.instalment(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: TagReactiveCompatible.self) {
            //: EnvelopeSinceAdministratorThen.share.func__pushToUserDetailVC(uid: targetId)
            EnvelopeSinceAdministratorThen.share.alongUid(uid: targetId)
            //: }else if cell.isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ChatMsgCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ChatMsgCell
            //: let mapCellData = mapCell.cellData as! PartCellData
            let mapCellData = mapCell.cellData as! PartCellData
            //: self.func__showLocationView(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: (mapCellData.direction == .MsgDirectionOutgoing))
            self.party(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: mapCellData.direction == .MsgDirectionOutgoing)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ResultCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ShowFocusViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ShowFocusViewCell
            //: cellData = imageCell.cellData as! UserCellData
            cellData = imageCell.cellData as! UserCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ChatThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ChatThen
            //: cellData = videoCell.cellData as! MarkMakeCellData
            cellData = videoCell.cellData as! MarkMakeCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: InfoViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! InfoViewCell
            //: cellData = audioCell.cellData as! NameBalloonThen
            cellData = audioCell.cellData as! NameBalloonThen
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: KeyTalkingViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! KeyTalkingViewCell
            //: cellData = textCell.cellData as! SerrationCellData
            cellData = textCell.cellData as! SerrationCellData
            //: } else if cell .isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ChatMsgCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ChatMsgCell
            //: cellData = mapCell.cellData as! PartCellData
            cellData = mapCell.cellData as! PartCellData
        }

        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: MarkMakeCellData.self) {
                if reCellData!.isKind(of: MarkMakeCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    gestureWith(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    limitSend(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = MarkThen.lineup(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        MarkThen.toProgress(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func makeData(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(RecordAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(RecordAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if RecordAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if RecordAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = RecordAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = RecordAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = RecordAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = RecordAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func moveButton() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_blockName))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_dismissValue.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.limitSend(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func executeInDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_fatalData.map{userSize(info: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_fatalData.map{userSize(info: $0)}, encoding: .utf8)!: content, (String(str_dismissName.prefix(5))): greetId, String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_blockName))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_reContent.map{periodicTable(app: $0)}, encoding: .utf8)!: content, (String(str_dismissName.prefix(5))): greetId, (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_atRequestName.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.limitSend(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            DownloadReactiveCompatible.shared.betweenHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.talkTask(showMsg: String(bytes: str_provinceText.map{$0^59}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        MarkThen.success(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(str_hiddenHumanData))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (str_filePleaseValue.lowercased()),
                               //: "audioId": greetId,
                               (str_hiddenValue.replacingOccurrences(of: "frame", with: "o") + String(str_makeRawData)): greetId,
                               //: "audioLength": length,
                               String(bytes: str_regionData.map{tagImage(view: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox"): model.unZipFilePath,
                               //: "senderId": RecordAppManager.share.loginUserMode.userID]
                               String(bytes: str_keyMessageName.map{filePic(equal: $0)}, encoding: .utf8)!: RecordAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(str_hiddenValue.replacingOccurrences(of: "frame", with: "o") + String(str_makeRawData)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_regionData.map{tagImage(view: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_toMakeData) + str_tagContent.replacingOccurrences(of: "tool", with: "g"))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.limitSend(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func blockModel(msgStr: String, quoteModel: CopernicanSystemJsonModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            talkTask(showMsg: (str_backIconStatusValue + " sen" + String(str_pathTouchName) + String(str_cellNameText.prefix(8))).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_fatalData.map{userSize(info: $0)}, encoding: .utf8)!: temStr, String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_blockName))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_reContent.map{periodicTable(app: $0)}, encoding: .utf8)!: temStr, (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_atRequestName.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_frameWindowContent.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(str_cancelName.replacingOccurrences(of: "voice", with: "e") + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(str_tabData) + String(str_partyValue.suffix(6)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? CopernicanSystemJsonModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func pointMp(img: UIImage, sendModel: UpwardJsonModel) {
        //: let imagePath = UserCellData.getChatImagePath(image: img)
        let imagePath = UserCellData.labAdd(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_leadingName.reversed(), encoding: .utf8)!: (str_backTableName.replacingOccurrences(of: "white", with: "I") + String(str_lastName.prefix(5))), String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_tagData)), String(bytes: str_momentText.reversed(), encoding: .utf8)!: imagePath, String(bytes: str_hitData.map{locationItem(gift: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(str_momentData.replacingOccurrences(of: "bottom", with: "pi")): imagePath,
                                        //: "type": "ImgMsg",
                                        (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (str_backTableName.replacingOccurrences(of: "white", with: "I") + String(str_lastName.prefix(5))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_permissionData.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_makeName) + String(str_equalAdjustmentTitle)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func gestureWith(msgData: TUIMessageCellData?, sendModel: UpwardJsonModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_leadingName.reversed(), encoding: .utf8)!: (String(str_subContent.suffix(5)) + String(str_markValue)), String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_promptTableData)), String(bytes: str_hitData.map{locationItem(gift: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_subContent.suffix(5)) + String(str_markValue)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_indexArrowData.reversed(), encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_laboratoryName.map{auditoryImage(input: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_onlineData.map{$0^35}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_permissionData.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_makeName) + String(str_equalAdjustmentTitle)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.endLoad(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.endLoad(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func pan(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (str_filePleaseValue.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_regionData.map{tagImage(view: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox"): voicePath,
                       //: "senderId": RecordAppManager.share.loginUserMode.userID]
                       String(bytes: str_keyMessageName.map{filePic(equal: $0)}, encoding: .utf8)!: RecordAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox"): voicePath,
                         //: "type": "AudioMsg",
                         (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_toMakeData) + str_tagContent.replacingOccurrences(of: "tool", with: "g")),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_laboratoryData.map{worldView(menu: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    //: func func__sendLocationMsg(latlon: String) {
    func viaModifyLatlon(latlon: String) {
        //: let msgInfo = ["msgType": "map",
        let msgInfo = [String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (str_modelHeadTitle.replacingOccurrences(of: "title", with: "p")),
                       //: "latlon": latlon,
                       String(bytes: str_enterData.map{signIconShare(name: $0)}, encoding: .utf8)!: latlon,
                       //: "messageType": 28,
                       (String(str_taskData)): 28,
                       //: "senderId": RecordAppManager.share.loginUserMode.userID] as [String : Any]
                       String(bytes: str_keyMessageName.map{filePic(equal: $0)}, encoding: .utf8)!: RecordAppManager.share.loginUserMode.userID] as [String: Any]

        //: let paramsDic = ["latlon": latlon,
        let paramsDic = [String(bytes: str_enterData.map{signIconShare(name: $0)}, encoding: .utf8)!: latlon,
                         //: "type": "MapMsg"] as [String : Any]
                         (str_thirdData.replacingOccurrences(of: "segment", with: "pe")): (String(str_closedValue))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func limitSend(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            talkTask(showMsg: kLet_homeText)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_lessValue))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: MarkMakeCellData.self) == false && msgData.isKind(of: HorizonCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: MarkMakeCellData.self) == false, msgData.isKind(of: HorizonCellData.self) == false, params[(str_thirdData.replacingOccurrences(of: "segment", with: "pe"))] as? String != (String(str_dismissValue.prefix(8))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((str_momentData.replacingOccurrences(of: "bottom", with: "pi"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(str_momentData.replacingOccurrences(of: "bottom", with: "pi"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(str_momentData.replacingOccurrences(of: "bottom", with: "pi"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(str_thirdData.replacingOccurrences(of: "segment", with: "pe"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_toMakeData) + str_tagContent.replacingOccurrences(of: "tool", with: "g")) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (str_makeValue.replacingOccurrences(of: "resent", with: "au") + String(str_backData.prefix(4)) + "ndbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_scaleData.prefix(6)) + String(str_sectionData) + String(str_inputTopCenterText.prefix(4)) + "er/vo" + String(str_modelFrameData.prefix(4)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_nearbyData + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_enableCollectionText.prefix(4)) + "ments/U" + String(str_pageTitle.prefix(4)) + "record/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_appDisplayScreenData + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(str_hiddenValue.replacingOccurrences(of: "frame", with: "o") + String(str_makeRawData))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(str_filePleaseValue.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: LiveRowRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        LiveRowRequestTool.coequal(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_dismissValue.prefix(8))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.loadDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_fileBarTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_dismissName.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    MarkThen.success(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_frameWindowContent.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome = NSNumber(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(str_makeText))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(str_makeText))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if CellPushListener.inserter().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_requestSucceedValue, object: nil, userInfo: [String(bytes: str_barDismissEqualData.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.quantityimate(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(str_taskData))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_dismissValue.prefix(8))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!] = (String(str_billMessageValue))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_netTitle.currentIn(eventID: kLet_onTitle)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_dismissValue.prefix(8))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!] = (String(str_blockName))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(RecordAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(RecordAppManager.share.loginUserMode.userID)" + (String(str_dataVideoContent.prefix(5)) + "ateCha" + String(str_dismissEqualData.prefix(4)) + "sTitle_") + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue, type != (String(str_dismissValue.prefix(8))) {
                //: self.removeReplyTipsView()
                self.withEnable()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_colorStyleData))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            MarkThen.latestData(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_observerName) + str_biologyListValue.replacingOccurrences(of: "mini", with: "n"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_observerName) + str_biologyListValue.replacingOccurrences(of: "mini", with: "n"))] as! NSNumber
                //: RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(str_promptButtonValue) + String(str_commentFatalName.suffix(4)) + "sMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(str_promptButtonValue) + String(str_commentFatalName.suffix(4)) + "sMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.supplement(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(str_behindName) + str_targetName.replacingOccurrences(of: "play", with: "t"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(str_behindName) + str_targetName.replacingOccurrences(of: "play", with: "t"))] as! [String: Any]
                //: let model = HiddenJsonModel(dic: intimatePhotoDict)
                let model = HiddenJsonModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(str_promptTableData))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(str_promptTableData))] as! [String: Any]
                //: let model = BriquetModel(dic: intimateVideoDict)
                let model = BriquetModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        theCapacity()

        //: updateSession()
        createTable()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func createTable() {
        //: CenterInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        CenterInfoManager.drunkFile(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String, type: Int = 0) {
    private func loadDismiss(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent _: String, type: Int = 0) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard RecordAppManager.share.loginUserMode.status != 1 else {
            guard RecordAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    talkTask(showMsg: errorStr)
                }
                //: return
                return
            }
            //: EnvelopeSinceAdministratorThen.share.func__pushToRechargeAlert(appendParams: "&type=\(type)")
            EnvelopeSinceAdministratorThen.share.willBy(appendParams: (String(str_selectTitle.suffix(6))) + "\(type)")
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.disoblige()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: EnvelopeSinceAdministratorThen.share.func__pushToSubscribeAlert()
            EnvelopeSinceAdministratorThen.share.dismissBy()

            //: } else if errorCode == 60003, !RecordAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !RecordAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ItemReactiveCompatible.shared.voiceMediumGift()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                talkTask(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func juggle(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_frameWindowContent.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.quantityimate(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func fromView() {
        //: if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.modify(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(RecordAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(RecordAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_backgroundText.array(forKey: kLet_senseEnableName)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.toRoot()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_backgroundText.set(intimateCacheData, forKey: kLet_senseEnableName)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func sendList(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(str_tempName.replacingOccurrences(of: "bottom", with: "a"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_frameWindowContent.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(str_itemMakeText.replacingOccurrences(of: "price", with: "t") + String(str_formatValue.suffix(3)) + String(str_tapTitle))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.modeTo(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func quantityimate(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((str_itemMakeText.replacingOccurrences(of: "price", with: "t") + String(str_formatValue.suffix(3)) + String(str_tapTitle))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(str_itemMakeText.replacingOccurrences(of: "price", with: "t") + String(str_formatValue.suffix(3)) + String(str_tapTitle))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.modeTo(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func modeTo(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = CenterInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = CenterInfoManager.cellSizeId(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.modify(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.nearMonth(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.accountingData(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.accountingData(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: CenterInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        CenterInfoManager.cacheTitle(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_mainText.map{$0^185}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_nextTextValue.map{$0^58}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_domainEndName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension FromBannerDelegate: HiddenViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func nameAll() {
        //: guard RecordAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.person(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func giftBy(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func removeName(actionType: ActualCustomReflectable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    belowBlack()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    okStr()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                belowBlack()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if RecordAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if RecordAppManager.share.loginUserMode.loungePlus {
                if RecordAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.assessment(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                okStr()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    imageVideo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    okStr()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                imageVideo()
            }

        //: case .Location:
        case .Location:
            //: TalkingPermissionTool.func__openLocationServiceWithBlock(true) { [weak self] isOpen in
            HiddenToolReactiveCompatible.funcServiceOuterSpaceWithChoppingBlockOff(true) { [weak self] isOpen in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if isOpen {
                if isOpen {
                    //: self.func__showLocationView(latlon: nil, lookSelf: false)
                    self.party(latlon: nil, lookSelf: false)
                }
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.worldView(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func okStr() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.talkTask(showMsg: String(bytes: str_bottomStatusData.map{$0^147}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func detailCall(giftModel _: RecordTransformable, giftNum _: String, model _: TopMeasurable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func queryed(giftModel: RecordTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        untilTurnPlayer(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: HorizonCellData?, fromStr: String?) {
    func untilTurnPlayer(giftModel: RecordTransformable, giftNum: String, cellData: HorizonCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            talkTask(showMsg: kLet_homeText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_lessValue))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_makeName) + String(str_equalAdjustmentTitle))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(str_blockTitle.replacingOccurrences(of: "raw", with: "g") + String(str_topText))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_availableTitle.suffix(4)) + "tems" + String(str_layerData))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(str_ofLabData))] = fromStr
        }
        // 送礼接口
        //: LiveRowRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        LiveRowRequestTool.quickPrivete(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.loadDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_voiceText)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.equalNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.detailAdd(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.createTable()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_frameWindowContent.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((str_itemMakeText.replacingOccurrences(of: "price", with: "t") + String(str_formatValue.suffix(3)) + String(str_tapTitle))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(str_itemMakeText.replacingOccurrences(of: "price", with: "t") + String(str_formatValue.suffix(3)) + String(str_tapTitle))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.modeTo(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_netTitle.currentIn(eventID: kLet_appContent)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func root(giftModel: RecordTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_netTitle.currentIn(eventID: kLet_routeName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            talkTask(showMsg: kLet_homeText)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= RecordAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            okStr()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(str_fieldData.prefix(8)) + String(str_infoLineTitle.suffix(2)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(str_thirdData.replacingOccurrences(of: "segment", with: "pe"))] = (String(str_userTitlePathValue.prefix(4)) + "GiftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_billMessageValue))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_frameWindowContent.prefix(7))): msgInfo, String(bytes: str_sizeKeyText.map{viewText(user: $0)}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        limitSend(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func wrapImage(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_buttonClickRecordValue - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func ageExecute(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func utiliserRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.utiliserRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                teachingMethod(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func statusDown() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if LiveCellReactiveCompatible.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            LiveCellReactiveCompatible.shared.words()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func detailAdd(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_frameWindowContent.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!] = (String(str_targetData))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_frameWindowContent.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_makeText))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_makeText))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_colorStyleData))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        executeObject(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_observerName) + str_biologyListValue.replacingOccurrences(of: "mini", with: "n"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_observerName) + str_biologyListValue.replacingOccurrences(of: "mini", with: "n"))] as! NSNumber
            //: RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RecordAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RecordAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.observerCoin(needReload: false, mf_coin: RecordAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func cachOfGift() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.equalMedium(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func executeObject(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.ofPlay(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func deleteImage(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.modelArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func isBar() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func keyBlock() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.behindExtra { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.createTable()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func partyVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.sinceSectionCard { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.createTable()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func pathClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = ShowViewDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        sinceTouchLive()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func elect() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TagDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.toCurrent(cellTitleList: [(String(str_countToData.suffix(6)) + str_constraintGenderAddValue.replacingOccurrences(of: "room", with: "o")).localized, (String(str_hiddenText.prefix(5))).localized, (str_tagValue.uppercased() + String(str_enableValue)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .segment()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: EnvelopeSinceAdministratorThen.share.func__pushToUserDetailVC(uid: self.targetId)
                EnvelopeSinceAdministratorThen.share.alongUid(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                skiJumpUp()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = ConditionListReactiveCompatible(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.toView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func skiJumpUp() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = ResultViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.barShowUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.pushIndex(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func makeSystemClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            teachingMethod(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            sinceTouchLive()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func upNotification(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = TagMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.imageDown(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func startNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(funcToNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_firstErrData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(upList),
                                               //: name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION,
                                               name: kLet_soundUserName,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func funcToNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard RecordAppManager.share.loginUserMode.loungePlus == true else { return }
        guard RecordAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func upList() {
        //: RecordAppManager.share.loginUserMode.loungePlus = true
        RecordAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func playerContent() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if MarkThen.lineData(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.press(tipStr: (String(str_blockMicName.suffix(5)) + "read" + String(str_transactionValue.suffix(6)) + "pts?").localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension FromBannerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func belowBlack() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        HiddenToolReactiveCompatible.signatureRoom(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = DistinctReactiveCompatible.squatVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.toVersionClick(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func effectTo() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        HiddenToolReactiveCompatible.playerHead(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.toVersionClick(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func toVersionClick(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UpwardJsonModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.pointMp(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = SinceLiveReactiveCompatible(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UpwardJsonModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.pointMp(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        sinceTouchLive()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func imageVideo() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard MarkThen.heather(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == RowErrorScalarLiteral.male.rawValue ? "He".localized : (String(str_moveData.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.talkTask(showMsg: (String(str_sizeNameData.prefix(6)) + "using" + String(str_directionValue.prefix(6)) + "ow a" + String(str_promptContent.prefix(4)) + str_checkText.lowercased()).localizedArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.toOf()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func toOf() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = DistinctReactiveCompatible.squatVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            DistinctReactiveCompatible.beyondName(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.bag(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.talkTask(showMsg: (String(str_acquisitionName.prefix(4)) + "t vi" + String(str_styleSendTrueName.suffix(5)) + "t le" + String(str_parentData.prefix(5)) + " sec" + str_videoOfContent.replacingOccurrences(of: "model", with: "on")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func bag(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: UserEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = MeRecognizerDelegate(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = UpwardJsonModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.gestureWith(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        TotalerpretationProgressHUD.countimateEqualDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        sinceTouchLive()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func endLoad(msgData: TUIMessageCellData, sendModel: UpwardJsonModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        FromThen.shared.mudra(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.talkTask(showMsg: (String(str_equalColorObserverText) + str_seekContent.replacingOccurrences(of: "button", with: "d")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.gestureWith(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    //: func func__addVoiceNotification() {
    func toNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(sinceTitle),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(successCallAt),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(scorn(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(keyUser(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_screenPlainValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(sendList(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_acceptSystemName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func style(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            LiveCellReactiveCompatible.shared.atDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            LiveCellReactiveCompatible.shared.dismissClickDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func sinceTitle() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        style(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func successCallAt() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        style(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func scorn(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            style(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            style(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: NameBalloonThen, cellView: TalkingChatAudioMsgCell) {
    func bottomError(cellData: NameBalloonThen, cellView: InfoViewCell) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ManagingDirectorReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.talkTask(showMsg: kLet_meetingValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: NameBalloonThen.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: NameBalloonThen.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! NameBalloonThen
            let audioModel = indexModel as! NameBalloonThen
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = CenterInfoManager.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = CenterInfoManager.currentFor(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        LiveCellReactiveCompatible.shared.words()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if LiveCellReactiveCompatible.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (str_imageTitleData) + "\(LiveCellReactiveCompatible.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if LiveCellReactiveCompatible.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                LiveCellReactiveCompatible.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        LiveCellReactiveCompatible.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        LiveCellReactiveCompatible.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: NameBalloonThen.self) {
                if subModel.isKind(of: NameBalloonThen.self) {
                    //: let audioModel = subModel as! NameBalloonThen
                    let audioModel = subModel as! NameBalloonThen
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = CenterInfoManager.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = CenterInfoManager.currentFor(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: ErrorThen.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ErrorThen.presentShould(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        LiveCellReactiveCompatible.shared.unblock(msgArr: waitArray)
    }
}

// MARK: - 地图相关

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 发送地图
    //: private func func__showLocationView(latlon: String?, lookSelf: Bool) {
    private func party(latlon: String?, lookSelf: Bool) {
        //: let location = TalkingMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        let location = RecordMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        //: location.show()
        location.equalShow()
        //: location.sendBlcok = { [weak self] latlon in
        location.sendBlcok = { [weak self] latlon in
            //: self?.func__sendLocationMsg(latlon: latlon)
            self?.viaModifyLatlon(latlon: latlon)
        }
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func sendFor() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.transmutation()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.executeInDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: NameBalloonThen.self) == true || indexModel?.isKind(of: UserCellData.self) == true || indexModel?.isKind(of: SerrationCellData.self) == true || indexModel?.isKind(of: MarkMakeCellData.self) == true {
                if indexModel?.isKind(of: NameBalloonThen.self) == true || indexModel?.isKind(of: UserCellData.self) == true || indexModel?.isKind(of: SerrationCellData.self) == true || indexModel?.isKind(of: MarkMakeCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.theCapacity()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.skin()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.playerContent()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func transmutation() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.sitImageDoing(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.privateBottom(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.fromView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == RecordAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == RecordAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == RecordAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == RecordAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.pushIndex(userWrap)
    }

    //: func func__getFillDraftText() {
    func addButtonName() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_indexRemoveValue.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.nameAll()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func fileOf() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_indexRemoveValue.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    //: func func__TipsNotification() {
    func sinceBuildVoice() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(recommence(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_firstTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func recommence(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: SinceCellData  = userinfo["msg"] as! SinceCellData
        let celldata: SinceCellData = userinfo[(String(str_equalActualText))] as! SinceCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func press(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_fatalData.map{userSize(info: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_appTitle.map{buttonTotal(finish: $0)}, encoding: .utf8)!: (String(str_imageManagerEqualData))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func skin() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        MarkThen.colorAcross { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.press(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func supplement(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.press(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func theCapacity() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        MarkThen.frameHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.press(tipStr: String(bytes: str_nameCallData.map{ageConstraint(send: $0)}, encoding: .utf8)!.localizedArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func keyUser(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_targetValue) + String(str_viewLabelTitle.prefix(7)))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension FromBannerDelegate: ViewTipsProtocol {
    //: func func__setReplyTipsView() {
    func funcFromWeltanschauung() {
        //: let key =  "\(RecordAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(RecordAppManager.share.loginUserMode.userID)" + (String(str_dataVideoContent.prefix(5)) + "ateCha" + String(str_dismissEqualData.prefix(4)) + "sTitle_") + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.numberersperseViewWeltanschauungMake()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            handleOfView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            withEnable()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func isMessage(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        blockModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        withEnable()
    }

    //: func removeReplyTipsView() {
    func withEnable() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            handleOfView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func modelAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_sampleValue, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_dismissValue - kLet_buttonClickRecordValue, width: kLet_itemData, height: kLet_dismissValue)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_itemData,
                                     //: height: ScreenHeight)
                                     height: kLet_dismissValue)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func teachingMethod(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.greetAt() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if SoundThen.account().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_maxText, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ShowManagerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_dismissValue - kLet_buttonClickRecordValue,
                                     //: width: ScreenWidth,
                                     width: kLet_itemData,
                                     //: height: ScreenHeight)
                                     height: kLet_dismissValue)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if SoundThen.account().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_maxText, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension FromBannerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func awake() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_fileOfTitle.suffix(6)) + " noti" + str_statusTargetData.replacingOccurrences(of: "constraint", with: "i") + str_landmarkValue.replacingOccurrences(of: "view", with: "n")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func provoke() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_buttonClickRecordValue : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.quantityryCornerRenderHeightRadiiMatchView(width: contentView.width,
                                                                   //: height: contentView.height,
                                                                   height: contentView.height,
                                                                   //: corners: [.topLeft, .topRight],
                                                                   corners: [.topLeft, .topRight],
                                                                   //: cornerRadii: .init(width: 8, height: 8))
                                                                   cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_lineText : kLet_timeScreenValue)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_messageTopText)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_lineText : kLet_timeScreenValue)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_messageTopText)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_messageTopText)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func chapiterStatus() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.makeSystemClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.elect()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.removeName(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_netTitle.currentIn(eventID: kLet_limitMarginAgentText, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
