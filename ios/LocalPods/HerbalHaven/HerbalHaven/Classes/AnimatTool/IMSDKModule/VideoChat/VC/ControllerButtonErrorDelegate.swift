
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_equalEventValue:[UInt8] = [0x45,0x42,0x45,0x58,0x4,0x4f,0x43,0x48,0x49,0x5e,0x16,0x5,0xc,0x44,0x4d,0x5f,0xc,0x42,0x43,0x58,0xc,0x4e,0x49,0x49,0x42,0xc,0x45,0x41,0x5c,0x40,0x49,0x41,0x49,0x42,0x58,0x49,0x48]

private func videoClass(index num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "uid" :*/
fileprivate let str_roomImageData:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let str_labelValue:String = "bottom map range case locationheadPic"

/*: "nickname" :*/
fileprivate let str_finishText:String = "nresourceckname"

/*: "birthday" :*/
fileprivate let str_makeData:String = "biritemhday"

/*: "videoUrl" :*/
fileprivate let str_scaleName:String = "vidcustom"

/*: "cover" :*/
fileprivate let str_dismissValue:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let str_regularData:String = "lfill"
fileprivate let str_makeName:[Character] = ["a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_centerSinceTitle:[UInt8] = [0x87,0x89,0x80]

private func userLogActual(number num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "refuseCall" :*/
fileprivate let str_pageNameValue:String = "in viewrefuseC"
fileprivate let str_presentationContent:String = "aheadhead"

/*: "data" :*/
fileprivate let str_kitData:[UInt8] = [0xf4,0xf1,0x4,0xf1]

fileprivate func appRow(reason num: UInt8) -> UInt8 {
    let value = Int(num) - 144
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let str_styleBackNameValue:String = "Callname self text"
fileprivate let str_designValue:[Character] = ["p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let str_mapSubAllContent:String = "stopCallimage var super max"

/*: "logId" :*/
fileprivate let str_lastData:[UInt8] = [0x75,0x78,0x70,0x52,0x6d]

fileprivate func pageRange(make num: UInt8) -> UInt8 {
    let value = Int(num) - 9
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_norValue:String = "clabelncel"

/*: "No answer" :*/
fileprivate let str_modelModeData:String = "to coin center rawNo a"

/*: "duration" :*/
fileprivate let str_labPresentData:String = "duratgendern"

/*: "She has hung up" :*/
fileprivate let str_restaurantValue:String = "She make round data make"
fileprivate let str_succeedContent:[Character] = [" ","u","p"]

/*: "cancelSex" :*/
fileprivate let str_targetName:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let str_nameIndexTitle:[Character] = ["H","e"," ","h","a","s"," ","h","u","n"]
fileprivate let str_fatalName:String = "g upmessage height err half"

/*: "endType" :*/
fileprivate let str_userDescriptionValue:[Character] = ["e"]
fileprivate let str_plusData:[Character] = ["n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let str_playerData:String = "number home normal pressstopSex"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_labelBalanceData:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x74,0x69,0x64,0x65,0x72,0x63,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let str_reasonName:String = "else letThe o"
fileprivate let str_styleSexTitle:String = "let gift hun"
fileprivate let str_normalData:String = "g up tbutton view height"

/*: "type" :*/
fileprivate let str_fromCanViewTitle:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_loadAppMName:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let str_intervalBottomContent:String = "name style appearance dataCance"
fileprivate let str_roomStopInName:String = "L"

/*: "Purchase" :*/
fileprivate let str_labelData:String = "Purcfree to"

/*: "&type=3" :*/
fileprivate let str_screenData:String = "&type=3return if container button"

/*: "addBean" :*/
fileprivate let str_makeValue:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_neverTitle:String = "refof"

/*: "onAcceptCall" :*/
fileprivate let str_managerName:[Character] = ["o","n","A","c","c","e"]
fileprivate let str_actionData:String = "ptCallquote image view point"

/*: "acceptCall" :*/
fileprivate let str_mainValue:String = "amept"
fileprivate let str_appTitle:String = "background mode moment returnCall"

/*: "Calls are cancelled" :*/
fileprivate let str_localValue:String = "Calluser model"
fileprivate let str_onlineData:String = "equal user can"
fileprivate let str_meReceiveData:String = "cmodelled"

/*: "The call has ended" :*/
fileprivate let str_equalAppName:[Character] = ["T","h","e"," ","c","a","l"]
fileprivate let str_thirdNameData:[Character] = ["l"," ","h"]
fileprivate let str_topName:[Character] = ["a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ControllerButtonErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_systemTitle = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class ControllerButtonErrorDelegate: ToControllerPositionBaseRecognizerDelegate {
	var showReplacementContent: String = "home"
	var coordinateContent: String = "view"
	var userTitle: String = "circle"

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ActualChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CommitReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: TagView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: WaftErrorScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagingDirectorReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagingDirectorReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_equalEventValue.map{videoClass(index: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! LabelHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.valueBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            colouring()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            calling(logId: self.chatModel?.logId)
        }
    
		if var usufructuaryValue = chatModel?.iHeadPic { 
			if var eliteValue = self.chatModel?.iUid { 
				if var dataValue = videoManager.bgVideoView { 
			            if (dataValue.canResignFirstResponder != true) && (dataValue.layer.contentsRect.size.width != 1) {
			                //: SWIFT_CUSTOM_DANGER_File_Call
			                let picTotal = PicView()
			            picTotal.loadTitle = eliteValue
			            
			            
			            picTotal.imageName = { [self] indexContent in
			            self.showReplacementContent = indexContent
			            
			            var picTotal = usufructuaryValue
			            let from = picTotal.prefix(upTo: picTotal.startIndex).count
			            picTotal.reserveCapacity(from - picTotal.suffix(5).count)
			            if picTotal.contains(self.showReplacementContent) {
			                self.showReplacementContent = picTotal
			            }
			            
			            return self.showReplacementContent
			            }
			                dataValue.addSubview(picTotal)
			            }
			
				}
			}
		}
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.themeData()
    
		if var loadAppearValue = player?.filePath { 
			if var contextValue = videoManager.trtcRemotUid { 
		            if (interactionView.giftEffectView.giftAnimatView.layer.position.x == 34.74) && (interactionView.giftEffectView.giftAnimatView.intrinsicContentSize.height == 264.75) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let liveModel = PicView(frame: interactionView.giftEffectView.giftAnimatView.bounds.intersection(CGRect(x: CGFloat(53), y: CGFloat(67), width: CGFloat(0), height: CGFloat(98))))
		            liveModel.loadTitle = contextValue
		            
		            
		            liveModel.imageName = { [self] indexContent in
		            self.coordinateContent = indexContent
		            
		            var liveModel = loadAppearValue
		            let page = liveModel.suffix(from: liveModel.startIndex).isEmpty
		            liveModel.removeAll(keepingCapacity: page)
		            if liveModel.contains(self.coordinateContent) {
		                self.coordinateContent = liveModel
		            }
		            
		            return self.coordinateContent
		            }
		                interactionView.giftEffectView.giftAnimatView.addSubview(liveModel)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        subviewsPath()
        //: setupSubViewsConstraint()
        form()
        //: initNotificationObserver()
        cornerChange()
        //: req_callGetUserInfo()
        statuteTitle()
        //: timeoutRules()
        destroyBlock()
        //: setUpBGMPlayer()
        assemblage()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.startGift()
        }
    
            if (interactionView.giftTrackView.layer.position.x == 34.74) && (interactionView.giftTrackView.intrinsicContentSize.height == 264.75) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let liveModel = PicView(frame: interactionView.giftTrackView.bounds.intersection(CGRect(x: CGFloat(53), y: CGFloat(67), width: CGFloat(0), height: CGFloat(98))))
            liveModel.loadTitle = interactionView.giftView.chatRoomID
            
            
            liveModel.imageName = { [self] indexContent in
            self.userTitle = indexContent
            
            var liveModel = interactionView.giftView.chatRoomID
            let page = liveModel.suffix(from: liveModel.startIndex).isEmpty
            liveModel.removeAll(keepingCapacity: page)
            if liveModel.contains(self.userTitle) {
                self.userTitle = liveModel
            }
            
            return self.userTitle
            }
                interactionView.giftTrackView.addSubview(liveModel)
            }

	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RecordAppManager.share.appConfigMode.disableShootScreen {
            if RecordAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TopShieldView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        ManagingDirectorReactiveCompatible.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.observerMake()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ContextVideoView = {
        //: let v = TalkingBGVideoView()
        let v = ContextVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.weight(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: BalloonReactiveCompatible = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = BalloonReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ConditionReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = ConditionReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_itemData - 15 - actualWidth(w: 125)
        //: if LabelToReactiveCompatible.shared.direction == .rightToLeft {
        if LabelToReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_pathErrData + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.enablelineView()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: DirectorChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = DirectorChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func statuteTitle() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        DirectorChatManager.infoWithCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_roomImageData.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_labelValue.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_finishText.replacingOccurrences(of: "resource", with: "i"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_makeData.replacingOccurrences(of: "item", with: "t"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(str_scaleName.replacingOccurrences(of: "custom", with: "e") + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(str_dismissValue))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_regularData.replacingOccurrences(of: "fill", with: "oc") + String(str_makeName))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.course()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.televisionInStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func televisionInStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ConditionCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ConditionCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.finishAcross(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.activityCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func colouring() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        HiddenToolReactiveCompatible.terminateColor { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.talkTask(showMsg: kLet_replaceText)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_pageNameValue.suffix(7)) + str_presentationContent.replacingOccurrences(of: "head", with: "l")), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_roomImageData.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.textCollection()
                //: return
                return
            }

            //: self.startPreview()
            self.startGift()
            //: self.stopBGM()
            self.targetButton()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CommitReactiveCompatible.destroyType(type: .Response)
            //: self.startBGM()
            self.valueBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func startGift() {
        //: self.videoManager.startPreview()
        self.videoManager.rangeEvaluate()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func observerMake() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.targetButton()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TalkingNameAdministratorDanmuManager.danmuAvailable()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ManagingDirectorReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ManagingDirectorReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func textCollection(animated: Bool = true) {
        //: self.stopBGM()
        self.targetButton()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func enablelineView() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.nameTitle()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.televisionInStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func individual() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.createPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.televisionInStatus()
        // 展示投诉说明飘屏（男性）
//        self.show_videoFloatScreenAnimation()
    }

    /// 展示投诉说明飘屏（男性）
//    private func show_videoFloatScreenAnimation() {
//        guard reportView == nil else { return }
//
//        reportView = WaftErrorScreenView()
//        view.addSubview(reportView!)
//        reportView?.show_animation()
//    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func cornerChange() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(reply(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(self.notification(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_netRecordName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(comment(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_mineData,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_systemTitle.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func reply(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            textCollection()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func comment(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ManagingDirectorReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagingDirectorReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagingDirectorReactiveCompatible.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.takeHome()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func notification(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.televisionInStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension ControllerButtonErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.talkTask(showMsg: (String(str_styleBackNameValue.prefix(4)) + " interru" + String(str_designValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_mapSubAllContent.prefix(8))), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (str_norValue.replacingOccurrences(of: "label", with: "a") + "Call"), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_pageNameValue.suffix(7)) + str_presentationContent.replacingOccurrences(of: "head", with: "l")), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_roomImageData.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.disregardYearEndConversation()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            textCollection()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func assemblage() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? BackBinaryCount.Call : BackBinaryCount.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CommitReactiveCompatible.destroyType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func valueBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.executeWith()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func targetButton() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.analogDigitalConverterConversation()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func destroyBlock() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_statusMaxText : self.chatModel?.sharedBorder()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            objectAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.objectAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func objectAction() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !ManagingDirectorReactiveCompatible.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.talkTask(showMsg: (String(str_modelModeData.suffix(4)) + "nswer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (str_norValue.replacingOccurrences(of: "label", with: "a") + "Call"), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_pageNameValue.suffix(7)) + str_presentationContent.replacingOccurrences(of: "head", with: "l")), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_roomImageData.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ManagingDirectorReactiveCompatible.shared.levelFront(info: socketDict)
        //: popCurrentViewController()
        textCollection()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func equalData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_labPresentData.replacingOccurrences(of: "gender", with: "io"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.chemicalElement(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.placeStage()
            //: createTalkRoom()
            individual()
            //: self.stopBGM()
            self.targetButton()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func calling(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.nameTitle()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.weight(show: false)
        //: self.stopBGM()
        self.targetButton()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.placeStage()
        //: self.createTalkRoom()
        self.individual()
    }
}

// MARK: - ResultCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension ControllerButtonErrorDelegate: ResultCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func socketUp(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_restaurantValue.prefix(4)) + "has hung" + String(str_succeedContent)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_targetName))].stringValue == RowErrorScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_nameIndexTitle) + String(str_fatalName.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        textCollection()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func technicality(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        calling(logId: json[String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func loadCell(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_userDescriptionValue) + String(str_plusData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_restaurantValue.prefix(4)) + "has hung" + String(str_succeedContent)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_playerData.suffix(7)))].stringValue == RowErrorScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_nameIndexTitle) + String(str_fatalName.prefix(4))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_labelBalanceData.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_reasonName.suffix(5)) + "ther" + String(str_styleSexTitle.suffix(4)) + String(str_normalData.prefix(6)) + "he phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.starDismiss()
        //: popCurrentViewController()
        textCollection()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func lose(msg: String, data: [String: Any]) {
        //: guard RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_fromCanViewTitle))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_loadAppMName))] as? String else { return }
            //: let httpsUrl = RecordAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RecordAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BackConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EnvelopeSinceAdministratorThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EnvelopeSinceAdministratorThen.share.caseConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            BackgroundAlertShow.openConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(str_intervalBottomContent.suffix(5)) + str_roomStopInName.lowercased()).localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_labelData.prefix(4)) + "hase").localized)
            {
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: } rightBlock: {
            } rightBlock: {
                //: EnvelopeSinceAdministratorThen.share.func__pushToRechargeAlert(appendParams: "&type=3")
                EnvelopeSinceAdministratorThen.share.willBy(appendParams: (String(str_screenData.prefix(7))))
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func cropArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.stateEqual(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func fromData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_makeValue))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.modifyAddName(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.freeSpokenPoint(point: addBean)
        }
    }
}

// MARK: - ActualObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ControllerButtonErrorDelegate: ActualObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func react(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_restaurantValue.prefix(4)) + "has hung" + String(str_succeedContent)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_neverTitle.replacingOccurrences(of: "of", with: "u") + "seSex")].stringValue == RowErrorScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_nameIndexTitle) + String(str_fatalName.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        textCollection()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func currency(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        calling(logId: json[String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - TopObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension ControllerButtonErrorDelegate: TopObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func clickError(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(str_pageNameValue.suffix(7)) + str_presentationContent.replacingOccurrences(of: "head", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.talkTask(showMsg: (String(str_modelModeData.suffix(4)) + "nswer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.disregardYearEndConversation()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            textCollection()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_managerName) + String(str_actionData.prefix(6))) && errorNo == 101 { // 接听通话时，余额不足
            //: EnvelopeSinceAdministratorThen.share.func__pushToRechargeAlert(appendParams: "&type=3")
            EnvelopeSinceAdministratorThen.share.willBy(appendParams: (String(str_screenData.prefix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_managerName) + String(str_actionData.prefix(6))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.talkTask(showMsg: msg)
            //: popCurrentViewController()
            textCollection()
        }
    }
}

// MARK: - ModelObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension ControllerButtonErrorDelegate: ModelObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func observerCall() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (str_mainValue.replacingOccurrences(of: "m", with: "cc") + String(str_appTitle.suffix(4))), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_roomImageData.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func observerState() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.talkTask(showMsg: (String(str_localValue.prefix(4)) + "s are" + String(str_onlineData.suffix(4)) + str_meReceiveData.replacingOccurrences(of: "model", with: "el")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (str_norValue.replacingOccurrences(of: "label", with: "a") + "Call"), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.talkTask(showMsg: (String(str_localValue.prefix(4)) + "s are" + String(str_onlineData.suffix(4)) + str_meReceiveData.replacingOccurrences(of: "model", with: "el")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_pageNameValue.suffix(7)) + str_presentationContent.replacingOccurrences(of: "head", with: "l")), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_roomImageData.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.talkTask(showMsg: (String(str_equalAppName) + String(str_thirdNameData) + String(str_topName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_centerSinceTitle.map{userLogActual(number: $0)}, encoding: .utf8)!: (String(str_mapSubAllContent.prefix(8))), String(bytes: str_kitData.map{appRow(reason: $0)}, encoding: .utf8)!: [String(bytes: str_lastData.map{pageRange(make: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        textCollection()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func momentErrorFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.subWith(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func afterwards(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.weight(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.coldShoulderCamera(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.televisionInStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func itemWindow() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        ManagingDirectorReactiveCompatible.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = TagView.choose()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.receive()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.exitWithMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.greetAt()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        textCollection()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func alongConversation() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension ControllerButtonErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func subviewsPath() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.atView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func form() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
