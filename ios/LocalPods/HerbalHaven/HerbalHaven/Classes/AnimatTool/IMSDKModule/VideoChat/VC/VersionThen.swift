
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_targetName:[UInt8] = [0xd5,0xd2,0xd5,0xc8,0x94,0xdf,0xd3,0xd8,0xd9,0xce,0x86,0x95,0x9c,0xd4,0xdd,0xcf,0x9c,0xd2,0xd3,0xc8,0x9c,0xde,0xd9,0xd9,0xd2,0x9c,0xd5,0xd1,0xcc,0xd0,0xd9,0xd1,0xd9,0xd2,0xc8,0xd9,0xd8]

private func selectedAdd(time num: UInt8) -> UInt8 {
    return num ^ 188
}

/*: "uid" :*/
fileprivate let str_showSelectData:[UInt8] = [0x72,0x66,0x61]

fileprivate func equalStatute(data num: UInt8) -> UInt8 {
    let value = Int(num) - 253
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let str_officialText:String = "data centerheadPic"

/*: "nickname" :*/
fileprivate let str_imageReplyTitle:String = "nickncurrente"

/*: "birthday" :*/
fileprivate let str_popTitle:[Character] = ["b","i","r","t","h","d","a"]
fileprivate let str_tagName:[Character] = ["y"]

/*: "videoUrl" :*/
fileprivate let str_followingValue:String = "image color then falsevideoUr"
fileprivate let str_kitName:String = "data"

/*: "location" :*/
fileprivate let str_editSkinValue:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_shareData:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let str_editData:String = "adfus"
fileprivate let str_greetTitle:String = "eCallcharm self case to new"

/*: "data" :*/
fileprivate let str_backExceptValue:[UInt8] = [0x89,0x86,0x99,0x86]

fileprivate func popSystem(path num: UInt8) -> UInt8 {
    let value = Int(num) - 37
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let str_shareName:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r"]
fileprivate let str_fatalText:String = "nameption"

/*: "stopCall" :*/
fileprivate let str_makeValue:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let str_atPointContent:[Character] = ["l"]

/*: "logId" :*/
fileprivate let str_norName:[UInt8] = [0xd1,0xd4,0xcc,0xae,0xc9]

fileprivate func recRoom(hidden num: UInt8) -> UInt8 {
    let value = Int(num) + 155
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_standValue:String = "cascreen"

/*: "No answer" :*/
fileprivate let str_quoteContent:String = "temp from color frameNo answer"

/*: "duration" :*/
fileprivate let str_playerOfForData:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let str_shareTitle:[Character] = ["S","h","e"," ","h","a"]
fileprivate let str_appContent:String = "touch mediums hu"
fileprivate let str_sinkValue:String = "return equalng up"

/*: "cancelSex" :*/
fileprivate let str_screenTitle:String = "ctextc"
fileprivate let str_pushName:String = "model if send kindelSex"

/*: "He has hung up" :*/
fileprivate let str_scopeData:[Character] = ["H"]
fileprivate let str_upSendData:[Character] = ["e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_conversationName:[Character] = ["e","n","d","T","y"]
fileprivate let str_maxSucceedData:[Character] = ["p","e"]

/*: "stopSex" :*/
fileprivate let str_blockMakeTitle:String = "guard image self gesture datastopSe"
fileprivate let str_moreData:String = "area"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_sectionData:[UInt8] = [0x96,0xb1,0xac,0xaa,0xb9,0xb9,0xb6,0xbc,0xb6,0xba,0xb1,0xab,0xff,0xbc,0xad,0xba,0xbb,0xb6,0xab,0xf3,0xff,0xbc,0xbe,0xb3,0xb3,0xff,0xb7,0xbe,0xac,0xff,0xba,0xb1,0xbb,0xba,0xbb]

/*: "The other hung up the phone" :*/
fileprivate let str_succeedData:String = "The otcontent any mode intimate gift"
fileprivate let str_tempTitle:String = "size arrayhung up "
fileprivate let str_aspectData:[Character] = ["t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_filterName:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_formatName:String = "value result count letjumpUrl"

/*: "Cancel" :*/
fileprivate let str_insideData:String = "else model false secondCance"
fileprivate let str_dismissValue:[Character] = ["l"]

/*: "Purchase" :*/
fileprivate let str_bagName:String = "Purchasevalue import collect equal"

/*: "&type=4" :*/
fileprivate let str_mViewData:String = "&tview value image label"
fileprivate let str_quickData:String = "gift varype=4"

/*: "addBean" :*/
fileprivate let str_observerValue:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_touchPushTitle:String = "refuseSelist var make self"
fileprivate let str_sexData:String = "X"

/*: "onAcceptCall" :*/
fileprivate let str_textStackImageValue:[Character] = ["o"]
fileprivate let str_fatalValue:String = "nAccepself raw"

/*: "acceptCall" :*/
fileprivate let str_transformData:[Character] = ["a","c","c","e","p","t"]
fileprivate let str_normalText:String = "Callgroup up path shared"

/*: "Calls are cancelled" :*/
fileprivate let str_imageModelMakeTitle:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c"]
fileprivate let str_markText:String = "ancellewarning"

/*: "The call has ended" :*/
fileprivate let str_acceptName:String = "The view view view"
fileprivate let str_countRoomValue:[Character] = [" ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VersionThen.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_appColorValue = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class VersionThen: ToControllerPositionBaseRecognizerDelegate {
	var selectedEnable: Bool = false
	var pullArray: [AnyHashable] = []
	var stallDictionary: [AnyHashable: String] = [:]
	var toMagnitude: Int = 31
	var compartmentName: String = "array"
	var itemSearchArray: [AnyHashable] = []
	var windowCount: Int = 75
	var valueName: String = "up"
	var equalArray: [AnyHashable] = []

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ActualChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CommitReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ShowMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagingDirectorReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagingDirectorReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_targetName.map{selectedAdd(time: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! LabelHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.logSize()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            untilVoiceOver()
        }
    
		if var caseTitleValue = self.chatModel?.pairHeadPic { 
			if var lineEffectValue = nav.protectedPushVc { 
		            if (lineEffectValue.extendedLayoutIncludesOpaqueBars) && (lineEffectValue.preferredStatusBarStyle == .lightContent) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let endSelect = WeddingController()
		            endSelect.roomKindContent = caseTitleValue
		            endSelect.roomOn = { [self] directionEnable in
		            self.selectedEnable = directionEnable
		            
		            var endSelect = animated
		            endSelect = true
		            if endSelect != self.selectedEnable {
		                self.selectedEnable = endSelect
		            }
		            
		            self.selectedEnable = false
		            return self.selectedEnable
		            }
		            endSelect.viewArray = { [self] appearLoadArray in
		            self.pullArray = appearLoadArray
		            
		            guard var value = self.pullArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            endSelect.endDictionary = { [self] giftDictionary in
		            self.stallDictionary = giftDictionary
		            
		            guard var value = self.stallDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                lineEffectValue.navigationController?.present(endSelect.self, animated: true) { [self] in
		                self.selectedEnable = false
		                self.selectedEnable = !self.selectedEnable
		                }
		            }
		
			}
		}
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.effectMessage()
    
		if var emptyValue = chatModel?.pairAge { 
	            if (interactionView.isFirstResponder) && (interactionView.convert(CGRect(x: 0, y: 0, width: 0, height: CGFloat(0)), to: interactionView.superview).origin.x == 75.32) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let formatLet = WeddingView(frame: interactionView.frame.insetBy(dx: CGFloat(374.41), dy: CGFloat(304.36)))
	            formatLet.screenName = interactionView.giftView.chatRoomID
	            
	            formatLet.doingBlockTotal = { [self] selectroomCount in
	            self.toMagnitude = selectroomCount
	            
	            var formatLet = emptyValue
	                formatLet -= 1
	                if formatLet <= 0 {
	                    formatLet = formatLet + 1
	                }
	            if formatLet < self.toMagnitude {
	                self.toMagnitude = formatLet
	            }
	            
	            return self.toMagnitude
	            }
	            formatLet.countComponentContent = { [self] skipSectionName in
	            self.compartmentName = skipSectionName
	            
	            var formatLet = interactionView.giftView.chatRoomID
	            let text = formatLet.suffix(from: formatLet.index(formatLet.startIndex, offsetBy: (formatLet.isContiguousUTF8 ? 1 : 1))).isEmpty
	            formatLet.removeAll(keepingCapacity: text)
	            if formatLet.contains(self.compartmentName) {
	                self.compartmentName = formatLet
	            }
	            
	            return self.compartmentName
	            }
	            formatLet.statusArray = { [self] appearLoadArray in
	            self.itemSearchArray = appearLoadArray
	            
	            guard var value = self.itemSearchArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                interactionView.addSubview(formatLet)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        atSubviews()
        //: setupSubViewsConstraint()
        modelLast()
        //: initNotificationObserver()
        fortifyObserver()
        //: req_callGetUserInfo()
        colorFinish()
        //: timeoutRules()
        exhibitRules()
        //: resumeTalk()
        laboratory()
        //: setUpBGMPlayer()
        withPlayer()
    
		if var needValue = chatModel?.logId { 
			if var upViewValue = self.chatModel?.pairBirthday { 
		            if (interactionView.isFirstResponder) && (interactionView.convert(CGRect(x: 0, y: 0, width: 0, height: CGFloat(0)), to: interactionView.superview).origin.x == 75.32) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let formatLet = WeddingView(frame: interactionView.frame.insetBy(dx: CGFloat(374.41), dy: CGFloat(304.36)))
		            formatLet.screenName = upViewValue
		            
		            formatLet.doingBlockTotal = { [self] selectroomCount in
		            self.windowCount = selectroomCount
		            
		            var formatLet = needValue
		                formatLet -= 1
		                if formatLet <= 0 {
		                    formatLet = formatLet + 1
		                }
		            if formatLet < self.windowCount {
		                self.windowCount = formatLet
		            }
		            
		            return self.windowCount
		            }
		            formatLet.countComponentContent = { [self] skipSectionName in
		            self.valueName = skipSectionName
		            
		            var formatLet = interactionView.giftView.chatRoomID
		            let text = formatLet.suffix(from: formatLet.index(formatLet.startIndex, offsetBy: (formatLet.isContiguousUTF8 ? 1 : 1))).isEmpty
		            formatLet.removeAll(keepingCapacity: text)
		            if formatLet.contains(self.valueName) {
		                self.valueName = formatLet
		            }
		            
		            return self.valueName
		            }
		            formatLet.statusArray = { [self] appearLoadArray in
		            self.equalArray = appearLoadArray
		            
		            guard var value = self.equalArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		                interactionView.addSubview(formatLet)
		            }
		
			}
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RecordAppManager.share.appConfigMode.disableShootScreen {
            if RecordAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TopShieldView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        ManagingDirectorReactiveCompatible.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.allOf()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: MemoryThen = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = MemoryThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: RangeChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = RangeChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func colorFinish() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        DirectorChatManager.infoWithCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_officialText.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_imageReplyTitle.replacingOccurrences(of: "current", with: "am"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(str_popTitle) + String(str_tagName))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_followingValue.suffix(7)) + str_kitName.replacingOccurrences(of: "data", with: "l"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_editSkinValue))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.small()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func untilVoiceOver() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        HiddenToolReactiveCompatible.dismissBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.talkTask(showMsg: kLet_replaceText)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_editData.replacingOccurrences(of: "ad", with: "re") + String(str_greetTitle.prefix(5))), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.rowAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.mixBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CommitReactiveCompatible.destroyType(type: .Response)
            //: self.startBGM()
            self.logSize()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func allOf() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.mixBgm()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ManagingDirectorReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ManagingDirectorReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func rowAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.mixBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func placeAddVideo() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.toId(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func laboratory() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            placeAddVideo()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func fortifyObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(imageGestureStop(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(tellTarget(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_mineData,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_appColorValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func imageGestureStop(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            rowAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func tellTarget(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ManagingDirectorReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagingDirectorReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagingDirectorReactiveCompatible.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.textId(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension VersionThen: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.talkTask(showMsg: (String(str_shareName) + str_fatalText.replacingOccurrences(of: "name", with: "u")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (String(str_makeValue) + String(str_atPointContent)), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_standValue.replacingOccurrences(of: "screen", with: "nc") + "elCall"), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_editData.replacingOccurrences(of: "ad", with: "re") + String(str_greetTitle.prefix(5))), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.disregardYearEndConversation()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            rowAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.module(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func withPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? BackBinaryCount.Call : BackBinaryCount.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CommitReactiveCompatible.destroyType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func logSize() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.executeWith()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func mixBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.analogDigitalConverterConversation()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 超时定时器
    //: private func timeoutRules() {
    private func exhibitRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_statusMaxText : self.chatModel?.sharedBorder()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            mash()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.mash()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func mash() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !ManagingDirectorReactiveCompatible.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.talkTask(showMsg: (String(str_quoteContent.suffix(9))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_standValue.replacingOccurrences(of: "screen", with: "nc") + "elCall"), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_editData.replacingOccurrences(of: "ad", with: "re") + String(str_greetTitle.prefix(5))), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ManagingDirectorReactiveCompatible.shared.levelFront(info: socketDict)
        //: popCurrentViewController()
        rowAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func network(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_playerOfForData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.constituent(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.cellBy()
            //: createTalkRoom()
            placeAddVideo()
            //: self.stopBGM()
            self.mixBgm()
        }
    }
}

// MARK: - ResultCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension VersionThen: ResultCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func socketUp(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_shareTitle) + String(str_appContent.suffix(4)) + String(str_sinkValue.suffix(5))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_screenTitle.replacingOccurrences(of: "text", with: "an") + String(str_pushName.suffix(5)))].stringValue == RowErrorScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_scopeData) + String(str_upSendData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        rowAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func technicality(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.mixBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.cellBy()
        //: self.createTalkRoom()
        self.placeAddVideo()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func loadCell(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_conversationName) + String(str_maxSucceedData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_shareTitle) + String(str_appContent.suffix(4)) + String(str_sinkValue.suffix(5))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_blockMakeTitle.suffix(6)) + str_moreData.replacingOccurrences(of: "area", with: "x"))].stringValue == RowErrorScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_scopeData) + String(str_upSendData)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_sectionData.map{$0^223}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_succeedData.prefix(6)) + "her " + String(str_tempTitle.suffix(8)) + String(str_aspectData)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.endOn()
        //: self.popCurrentViewController()
        self.rowAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_purchaseName, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func lose(msg: String, data: [String: Any]) {
        //: guard RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_filterName))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_formatName.suffix(7)))] as? String else { return }
            //: let httpsUrl = RecordAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RecordAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BackConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EnvelopeSinceAdministratorThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EnvelopeSinceAdministratorThen.share.caseConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            BackgroundAlertShow.openConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(str_insideData.suffix(5)) + String(str_dismissValue)).localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_bagName.prefix(8))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: } rightBlock: {
            } rightBlock: {
                //: EnvelopeSinceAdministratorThen.share.func__pushToRechargeAlert(appendParams: "&type=4")
                EnvelopeSinceAdministratorThen.share.willBy(appendParams: (String(str_mViewData.prefix(2)) + String(str_quickData.suffix(5))))
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func fromData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_observerValue))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.aboveGroupPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.toControl(point: addBean)
        }
    }
}

// MARK: - ActualObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension VersionThen: ActualObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func react(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_shareTitle) + String(str_appContent.suffix(4)) + String(str_sinkValue.suffix(5))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_touchPushTitle.prefix(8)) + str_sexData.lowercased())].stringValue == RowErrorScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_scopeData) + String(str_upSendData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.talkTask(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.disregardYearEndConversation()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        rowAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func currency(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.mixBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.cellBy()
        //: self.createTalkRoom()
        self.placeAddVideo()
    }
}

// MARK: - TopObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension VersionThen: TopObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func clickError(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_editData.replacingOccurrences(of: "ad", with: "re") + String(str_greetTitle.prefix(5))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.talkTask(showMsg: (String(str_quoteContent.suffix(9))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.disregardYearEndConversation()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ManagingDirectorReactiveCompatible.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.rowAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_textStackImageValue) + String(str_fatalValue.prefix(6)) + "tCall") && errorNo == 101 { // 接听通话时，余额不足
            //: EnvelopeSinceAdministratorThen.share.func__pushToRechargeAlert(appendParams: "&type=4")
            EnvelopeSinceAdministratorThen.share.willBy(appendParams: (String(str_mViewData.prefix(2)) + String(str_quickData.suffix(5))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_textStackImageValue) + String(str_fatalValue.prefix(6)) + "tCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.talkTask(showMsg: msg)
            //: self.popCurrentViewController()
            self.rowAnimated()
        }
    }
}

// MARK: - MediumObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension VersionThen: MediumObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func totaleractionShouldViewTextAcceptCall() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (String(str_transformData) + String(str_normalText.prefix(4))), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func custom() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.talkTask(showMsg: (String(str_imageModelMakeTitle) + str_markText.replacingOccurrences(of: "warning", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_standValue.replacingOccurrences(of: "screen", with: "nc") + "elCall"), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.talkTask(showMsg: (String(str_imageModelMakeTitle) + str_markText.replacingOccurrences(of: "warning", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (str_editData.replacingOccurrences(of: "ad", with: "re") + String(str_greetTitle.prefix(5))), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_showSelectData.map{equalStatute(data: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.talkTask(showMsg: (String(str_acceptName.prefix(4)) + "call" + String(str_countRoomValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_shareData.reversed(), encoding: .utf8)!: (String(str_makeValue) + String(str_atPointContent)), String(bytes: str_backExceptValue.map{popSystem(path: $0)}, encoding: .utf8)!: [String(bytes: str_norName.map{recRoom(hidden: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.endOn()
        //: self.popCurrentViewController()
        self.rowAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func researchLaboratory(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.changeCall(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func userBackHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.displaceToMortal(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func jalousie() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        ManagingDirectorReactiveCompatible.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ShowMiniView.premium()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.greetAt()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.afterPhoto()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.polls(talkingTime)
        //: popCurrentViewController()
        rowAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func reciprocationToTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.polls(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func modelFront(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.explain(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension VersionThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func atSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func modelLast() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
