
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let str_modelReleaseData:[UInt8] = [0xd1,0xc0,0xd3,0xd5,0xd8,0xde,0xd2,0xd5,0xc0,0xd5,0xd4,0xd2,0xde,0xc2,0xc9,0xc0,0xcf,0xc6,0xc4,0xde,0xcf,0xce,0xd5,0xc8,0xc7,0xc8,0xc2,0xc0,0xd5,0xc8,0xce,0xcf]

private func barValue(array num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "p- :*/
fileprivate let str_pathFollowData:[Character] = ["p","-"]

/*: - :*/
fileprivate let str_usColorTitle:String = "track"

/*: "redirectUrl" :*/
fileprivate let str_kitContent:[Character] = ["r","e","d","i","r","e","c","t","U","r","l"]

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let str_errorText:String = "VOIhidden"
fileprivate let str_kitName:String = "M: 进return extra var"
fileprivate let str_appValue:[Character] = ["\u{623f}","\u{6a21}","型","解","析","失","败","。"]

/*: "Party room exception" :*/
fileprivate let str_errPointText:String = "index falseParty "
fileprivate let str_managerData:[Character] = ["r","o"]
fileprivate let str_actualData:String = "om exformat insert guard kit if"

/*: _ :*/
fileprivate let str_effectData:[Character] = ["_"]

/*: "male" :*/
fileprivate let str_blockValue:[UInt8] = [0x93,0x9f,0x92,0x9b]

private func addTo(view num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "female" :*/
fileprivate let str_infoActualName:[Character] = ["f"]
fileprivate let str_errTitle:String = "EMALE"

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let str_pathMaxName:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","群","聊","\u{8fdb}","\u{623f}","\u{5931}","败","。","g","r","o","u","p","I","d"]
fileprivate let str_requestCenterName:[Character] = [":"," "]

/*: ." :*/
fileprivate let str_popText:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let str_kitValue:[Character] = ["V","O","I","C","E","R","O","O","M"]
fileprivate let str_whiteData:[Character] = [":"," ","切","房","模","\u{578b}","解","析","失","败","。"]

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let str_makeName:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","群","聊","切","房","失","败","。","g","r","o","u","p","I","d",":"," "]

/*: "errCode" :*/
fileprivate let str_amKeepTitle:[UInt8] = [0x68,0x7f,0x7f,0x4e,0x62,0x69,0x68]

private func moveTurn(end num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "custom_kickout_message" :*/
fileprivate let str_managerValue:String = "CUST"
fileprivate let str_checkTitle:String = "ickotitle"
fileprivate let str_intimateTitle:String = "sagwith"

/*: "closeRoom" :*/
fileprivate let str_errValue:String = "CLOSE"

/*: "adminCloseRoom" :*/
fileprivate let str_indicatorCommentValue:String = "equal self countadminCl"
fileprivate let str_hungViewTitle:[Character] = ["o","s","e","R","o","o","m"]

/*: "The room is closed, please go to another room" :*/
fileprivate let str_leadingValue:[UInt8] = [0xb4,0x88,0x85,0xc0,0x92,0x8f,0x8f,0x8d,0xc0,0x89,0x93,0xc0,0x83,0x8c,0x8f,0x93,0x85,0x84,0xcc,0xc0,0x90,0x8c,0x85,0x81,0x93,0x85,0xc0,0x87,0x8f,0xc0,0x94,0x8f,0xc0,0x81,0x8e,0x8f,0x94,0x88,0x85,0x92,0xc0,0x92,0x8f,0x8f,0x8d]

/*: "OK" :*/
fileprivate let str_markText:String = "Ocrush"

/*: "You were kicked out of the room" :*/
fileprivate let str_popValue:[UInt8] = [0x37,0x1,0x1b,0x4e,0x19,0xb,0x1c,0xb,0x4e,0x5,0x7,0xd,0x5,0xb,0xa,0x4e,0x1,0x1b,0x1a,0x4e,0x1,0x8,0x4e,0x1a,0x6,0xb,0x4e,0x1c,0x1,0x1,0x3]

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let str_deviceData:String = "VOshowC"
fileprivate let str_distanceData:String = "value bubble view value make进房失败，r"
fileprivate let str_sharePathData:String = "exit"

/*: , error:  :*/
fileprivate let str_managerContent:[Character] = [","]
fileprivate let str_textSucceedValue:[Character] = [" ","e","r","r","o","r",":"," "]

/*: , reason:  :*/
fileprivate let str_makeInputData:[Character] = [","," ","r","e","a","s","o","n",":"," "]

/*: , extendedData:  :*/
fileprivate let str_resultMainText:[Character] = [","," ","e","x","t","e","n","d","e","d"]
fileprivate let str_tagValue:[Character] = ["D","a","t","a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let str_userSpreadAsData:String = "VlabelIC"
fileprivate let str_viewNetworkValue:String = "M: \u{5f53}"
fileprivate let str_blockTitle:String = "失败，rmodel remove"
fileprivate let str_imageColorValue:String = "oomID: view if segment required if"

/*: , errorCode:  :*/
fileprivate let str_priorityValue:[Character] = [","," ","e","r","r","o","r","C","o","d","e",":"," "]

/*: , streamID:  :*/
fileprivate let str_makeStatusValue:String = ", smodel inside"
fileprivate let str_meIconName:[Character] = ["t","r","e","a","m","I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let str_giftTitle:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let str_timeItemIndexTitle:[Character] = ["V","O","I","C","E","R","O","O","M",":"," "]
fileprivate let str_appPathTitle:String = "拉流"
fileprivate let str_dismissData:String = "失败\u{ff0c}ro"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let str_fatalData:String = "dataOICEROO"
fileprivate let str_voiceTitle:[Character] = ["M",":"," ","信","令","\u{89e3}","析","失","\u{8d25}","，","c","o","m"]
fileprivate let str_titleClearValue:String = "mand: base key title cover"

/*: , roomID:  :*/
fileprivate let str_succeedName:String = ", roomself section left"
fileprivate let str_appTitle:String = "equal actual count guardID: "

/*: "data" :*/
fileprivate let str_tabTitle:String = "backata"

/*: "position" :*/
fileprivate let str_priceName:String = "changeosition"

/*: "xxxx--  :*/
fileprivate let str_touchVideoName:String = "height on loadxxxx-- "

/*: "opType" :*/
fileprivate let str_signTextContent:String = "refuse if interval varopType"

/*: "party:changeMike" :*/
fileprivate let str_textLayerContent:[Character] = ["p","a","r","t","y",":","c","h","a","n"]
fileprivate let str_resultTitle:[Character] = ["g","e","M","i","k","e"]

/*: "type" :*/
fileprivate let str_modeViewValue:[Character] = ["t","y","p","e"]

/*: "toPosition" :*/
fileprivate let str_enterName:String = "toPoson component"
fileprivate let str_refName:[Character] = ["i","t","i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let str_sunTagValue:[Character] = ["p","a","r","t","y",":","a","d","m"]
fileprivate let str_fileVoiceName:[Character] = ["i","n","C","h","a","n","g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let str_televisionValue:String = "You shared view equal"
fileprivate let str_leadingName:String = "icon"
fileprivate let str_panValue:[Character] = ["f"," ","t","h","e"," ","s","e","a","t"]

/*: "party:changeCoin" :*/
fileprivate let str_centerTitleName:String = "pardatay"
fileprivate let str_laboratoryName:String = ":chaage self false biology lab"

/*: "party:closeStream" :*/
fileprivate let str_visibleText:String = "paminiy"
fileprivate let str_upCellValue:String = "error icon lasteStrea"
fileprivate let str_shareValue:[Character] = ["m"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SoundThen.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let kLet_requestTitle = NSNotification.Name(rawValue: String(bytes: str_modelReleaseData.map{barValue(array: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kLet_liveRecordData = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum ItemPropertyProtocol: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class SoundThen: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = ItemMeasurable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_requestTitle, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: SoundThen?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: StormCenterReactiveCompatible?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: ModelKeyTransformable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = indexByStart()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func account() -> SoundThen {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = SoundThen()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func doingCell() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if SoundThen._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            SoundThen._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: TalkingEqualThen = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = TalkingEqualThen.waterView()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.tortureChamber()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func heading(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        VoiceReqTool.firstResult(roomId: partyModel.roomId,
                                            //: uid: Int(RecordAppManager.share.loginUid) ?? 0,
                                            uid: Int(RecordAppManager.share.loginUid) ?? 0,
                                            //: type: type,
                                            type: type,
                                            //: position: position,
                                            position: position,
                                            //: toPos: toPosition)
                                            toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.streamDown()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = ModelKeyTransformable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.monthColor(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.streamDown()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func caracole(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        VoiceReqTool.orientation(roomId: partyModel.roomId,
                                            //: uid: uid,
                                            uid: uid,
                                            //: type: type,
                                            type: type,
                                            //: position: position)
                                            position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func streamDown() -> String {
        //: return "p-\(RecordAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(RecordAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func turnAlongSearchVoiceBilliardParlourPresenceChamberDrunk(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        HiddenToolReactiveCompatible.dismissBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.talkTask(showMsg: kLet_replaceText)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            VoiceReqTool.pushAsideShared(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: EnvelopeSinceAdministratorThen.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            EnvelopeSinceAdministratorThen.share.caseConfig(urlStr: result[(String(str_kitContent))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.talkTask(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard ManagingDirectorReactiveCompatible.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.talkTask(showMsg: kLet_meetingValue)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = ItemMeasurable.deserialize(from: result as? Dictionary) else {
                    //: ShowLogTool.cellText(msg: "VOICEROOM: 进房模型解析失败。")
                    ShowLogTool.cellText(msg: (str_errorText.replacingOccurrences(of: "hidden", with: "C") + "EROO" + String(str_kitName.prefix(4)) + String(str_appValue)))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.talkTask(showMsg: (String(str_errPointText.suffix(6)) + String(str_managerData) + String(str_actualData.prefix(5)) + "ception").localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = StormCenterReactiveCompatible()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.sinceTouchLive()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.playView()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.suspend()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kLet_netTitle.currentIn(eventID: "\(kLet_deviceManagerValue)_\(RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue ? String(bytes: str_blockValue.map{addTo(view: $0)}, encoding: .utf8)! : (String(str_infoActualName) + str_errTitle.lowercased()))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                PublisherThen.product(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: ShowLogTool.cellText(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        ShowLogTool.cellText(msg: (String(str_pathMaxName) + String(str_requestCenterName)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.talkTask(showMsg: (String(str_errPointText.suffix(6)) + String(str_managerData) + String(str_actualData.prefix(5)) + "ception").localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.welt()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func coverLivingRoom(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        VoiceReqTool.statuteText(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.atDismiss()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == RecordAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == RecordAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = ItemMeasurable.deserialize(from: result as? Dictionary) else {
                //: ShowLogTool.cellText(msg: "VOICEROOM: 切房模型解析失败。")
                ShowLogTool.cellText(msg: (String(str_kitValue) + String(str_whiteData)))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.talkTask(showMsg: (String(str_errPointText.suffix(6)) + String(str_managerData) + String(str_actualData.prefix(5)) + "ception").localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = StormCenterReactiveCompatible()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.sinceTouchLive()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.playView()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.pushAwake(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kLet_netTitle.currentIn(eventID: "\(kLet_deviceManagerValue)_\(RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue ? String(bytes: str_blockValue.map{addTo(view: $0)}, encoding: .utf8)! : (String(str_infoActualName) + str_errTitle.lowercased()))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            PublisherThen.product(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: ShowLogTool.cellText(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    ShowLogTool.cellText(msg: (String(str_makeName)) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.talkTask(showMsg: (String(str_errPointText.suffix(6)) + String(str_managerData) + String(str_actualData.prefix(5)) + "ception").localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.welt()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func skirtGift() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.timeImage()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func tortureChamber() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = greetAt() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: FromBannerDelegate.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func welt() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.atDismiss() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.heading(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            SoundThen.doingCell() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func suspend() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = AreaReactiveCompatible.getVoiceRoomAppId()
        profile.appID = AreaReactiveCompatible.rowSuccess()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: RecordAppManager.share.loginUid)
        let user = ZegoUser(userID: RecordAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func pushAwake(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension SoundThen: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            VoiceReqTool.addCallFor(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.spoil(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.number()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == ItemPropertyProtocol.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.heading(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                VoiceReqTool.addCallFor(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.spoil(key: key, model: value)
                        //: if String(value.uid) == RecordAppManager.share.loginUid {
                        if String(value.uid) == RecordAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.streamDown()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.number()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_netTitle.gestureBottom(eventID: kLet_movieTitle, parameterStr: [String(bytes: str_amKeepTitle.map{moveTurn(end: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(str_managerValue.lowercased() + "om_k" + str_checkTitle.replacingOccurrences(of: "title", with: "u") + "t_mes" + str_intimateTitle.replacingOccurrences(of: "with", with: "e"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (str_errValue.lowercased() + "Room") || value == (String(str_indicatorCommentValue.suffix(7)) + String(str_hungViewTitle)) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    welt()
                    //: let config = ShowAlertConfig()
                    let config = HiddenAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    BackgroundAlertShow.userRight(message: String(bytes: str_leadingValue.map{$0^224}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: kLet_sensePointPathData, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    talkTask(showMsg: String(bytes: str_popValue.map{$0^110}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: ShowLogTool.cellText(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                ShowLogTool.cellText(msg: (str_deviceData.replacingOccurrences(of: "show", with: "I") + "EROOM：" + String(str_distanceData.suffix(6)) + "oomID:" + str_sharePathData.replacingOccurrences(of: "exit", with: " ")) + "\(roomID)" + (String(str_managerContent) + String(str_textSucceedValue)) + "\(errorCode)" + (String(str_makeInputData)) + "\(reason)" + (String(str_resultMainText) + String(str_tagValue)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                talkTask(showMsg: (String(str_errPointText.suffix(6)) + String(str_managerData) + String(str_actualData.prefix(5)) + "ception").localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_netTitle.gestureBottom(eventID: kLet_movieTitle, parameterStr: [String(bytes: str_amKeepTitle.map{moveTurn(end: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            welt()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: ShowLogTool.cellText(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ShowLogTool.cellText(msg: (str_userSpreadAsData.replacingOccurrences(of: "label", with: "O") + "EROO" + str_viewNetworkValue + "前\u{7528}户推\u{6d41}" + String(str_blockTitle.prefix(4)) + String(str_imageColorValue.prefix(7))) + "\(partyModel.roomId)" + (String(str_priorityValue)) + "\(errorCode)" + (String(str_makeStatusValue.prefix(3)) + String(str_meIconName)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            talkTask(showMsg: String(bytes: str_giftTitle.reversed(), encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = itemImagePositions()
            //: for (position, model) in dict where String(model.uid) == RecordAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == RecordAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                heading(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                positionWith(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                welt()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: ShowLogTool.cellText(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ShowLogTool.cellText(msg: (String(str_timeItemIndexTitle) + str_appPathTitle.capitalized + str_dismissData + "omID: ") + "\(partyModel.roomId)" + (String(str_priorityValue)) + "\(errorCode)" + (String(str_makeStatusValue.prefix(3)) + String(str_meIconName)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                welt()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.bring(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = itemImagePositions()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.bring(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: ShowLogTool.cellText(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            ShowLogTool.cellText(msg: (str_fatalData.replacingOccurrences(of: "data", with: "V") + String(str_voiceTitle) + String(str_titleClearValue.prefix(6))) + "\(command)" + (String(str_succeedName.prefix(6)) + String(str_appTitle.suffix(4))) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(str_tabTitle.replacingOccurrences(of: "back", with: "d"))][(str_priceName.replacingOccurrences(of: "change", with: "p"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kLet_liveRecordData else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = ModelKeyTransformable.deserialize(from: json[(str_tabTitle.replacingOccurrences(of: "back", with: "d"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(str_touchVideoName.suffix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(str_signTextContent.suffix(6)))].stringValue == (String(str_textLayerContent) + String(str_resultTitle)) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(str_tabTitle.replacingOccurrences(of: "back", with: "d"))][(String(str_modeViewValue))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                monthColor(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = equalUser(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    positionWith(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                positionWith(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(str_tabTitle.replacingOccurrences(of: "back", with: "d"))][(String(str_enterName.prefix(5)) + String(str_refName))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                monthColor(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                monthColor(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.userSemblance()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(str_signTextContent.suffix(6)))].stringValue == (String(str_sunTagValue) + String(str_fileVoiceName)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(str_tabTitle.replacingOccurrences(of: "back", with: "d"))][(String(str_modeViewValue))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                monthColor(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = equalUser(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    positionWith(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.userSemblance()
                    //: guard String(oldModel.uid) == RecordAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == RecordAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    talkTask(showMsg: (String(str_televisionValue.prefix(4)) + "were k" + str_leadingName.replacingOccurrences(of: "on", with: "ke") + "d of" + String(str_panValue)).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                monthColor(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == RecordAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == RecordAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                monthColor(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == RecordAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == RecordAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(str_signTextContent.suffix(6)))].stringValue == (str_centerTitleName.replacingOccurrences(of: "data", with: "t") + String(str_laboratoryName.prefix(4)) + "ngeCoin") { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            for (position, model) in SoundThen.account().itemImagePositions() {
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    monthColor(position: position, model: model)
                    //: return
                    continue
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(str_signTextContent.suffix(6)))].stringValue == (str_visibleText.replacingOccurrences(of: "mini", with: "rt") + ":clos" + String(str_upCellValue.suffix(6)) + String(str_shareValue)) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func monthColor(position: Int, model: ModelKeyTransformable) {
        //: modifyMikePosition(key: position, model: model)
        spoil(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.replace(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        totalimate(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func positionWith(position: Int, model: ModelKeyTransformable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        spoil(key: position, model: ModelKeyTransformable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.replace(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        totalimate(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func totalimate(uid: Int) {
        //: guard String(uid) == RecordAppManager.share.loginUid else { return }
        guard String(uid) == RecordAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = itemImagePositions()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.pastWhen()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension SoundThen {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func indexByStart() -> [Int: ModelKeyTransformable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kLet_liveRecordData {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            spoil(key: index, model: ModelKeyTransformable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func spoil(key: Int, model: ModelKeyTransformable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func equalUser(key: Int) -> ModelKeyTransformable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func itemImagePositions() -> [Int: ModelKeyTransformable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
