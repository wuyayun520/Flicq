
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_sendColorValue:[UInt8] = [0x4c,0x4b,0x4c,0x51,0xd,0x46,0x4a,0x41,0x40,0x57,0x1f,0xc,0x5,0x4d,0x44,0x56,0x5,0x4b,0x4a,0x51,0x5,0x47,0x40,0x40,0x4b,0x5,0x4c,0x48,0x55,0x49,0x40,0x48,0x40,0x4b,0x51,0x40,0x41]

private func collectionTo(text num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "Followers" :*/
fileprivate let str_aspectData:[Character] = ["F","o","l","l","o","w","e","r","s"]

/*: "Following" :*/
fileprivate let str_currentMakeData:String = "nor action userFoll"

/*: "#E6E6E6" :*/
fileprivate let str_sourceSharedData:[Character] = ["#","E","6","E","6","E","6"]

/*: "btn_message_more" :*/
fileprivate let str_roomCallData:String = "btn_mpath nor"
fileprivate let str_modelEditContent:String = "essared"
fileprivate let str_fillAddData:String = "to to colore_more"

/*: "UID:  :*/
fileprivate let str_rowValue:[Character] = ["U","I","D",":"," "]

/*: "level_ :*/
fileprivate let str_touchLengthInTitle:String = "for size selected selflevel_"

/*: "Gift" :*/
fileprivate let str_makeValue:[UInt8] = [0xa8,0x86,0x89,0x9b]

/*: "Mike" :*/
fileprivate let str_managerName:[UInt8] = [0x1e,0x3a,0x3c,0x36]

fileprivate func backgroundLocal(log num: UInt8) -> UInt8 {
    let value = Int(num) - 209
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let str_userLabValue:[UInt8] = [0x36,0x1f,0x1c,0x1c,0x1f,0x7]

private func userCounteraction(to num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "gift_voice_card" :*/
fileprivate let str_liveValueFileData:String = "tar data rowgift_v"
fileprivate let str_viewConstraintContent:[Character] = ["_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let str_tabValue:String = "btn_pacolor tag"
fileprivate let str_whiteTitle:[Character] = ["c"]
fileprivate let str_iconEqualIfValue:String = "false cookie tablee_nor"

/*: "btn_party_voice_pre" :*/
fileprivate let str_ageSendViewName:String = "moment color online letbtn_par"
fileprivate let str_emptySectionTitle:String = "view asty_voi"

/*: "attentionUid" :*/
fileprivate let str_topUserIfName:[Character] = ["a","t","t","e","n","t","i","o","n","U","i","d"]

/*: "source" :*/
fileprivate let str_toOfValue:String = "socallrce"

/*: "Kick out" :*/
fileprivate let str_viewCenterValue:String = "output title trueKick o"
fileprivate let str_userSizeLetValue:[Character] = ["u","t"]

/*: "Unmute" :*/
fileprivate let str_washValue:String = "Unmutlayer model let layer conversation"
fileprivate let str_normalData:[Character] = ["e"]

/*: "Mute" :*/
fileprivate let str_makeData:String = "Muteuser var"

/*: "Kick off" :*/
fileprivate let str_closedCardName:String = "Kick return empty model need"
fileprivate let str_sizeCenterName:[Character] = ["o","f","f"]

/*: "Block" :*/
fileprivate let str_originalTitle:[Character] = ["B","l","o","c","k"]

/*: "Report" :*/
fileprivate let str_transactionName:[Character] = ["R","e","p","o","r","t"]

/*: "#F6F6F6" :*/
fileprivate let str_whiteText:String = "video hidden user size#F"
fileprivate let str_tableEqualTitle:[Character] = ["6","F","6","F","6"]

/*: "Confirm to kick out the room?" :*/
fileprivate let str_hiddenContent:String = "then add label request ofConfir"
fileprivate let str_sectionReloadData:String = "self to current type count kick "
fileprivate let str_moreData:[Character] = ["h","e"," ","r","o","o","m","?"]

/*: "Cancel" :*/
fileprivate let str_allowContent:String = "model let table true layerCance"
fileprivate let str_leadingName:String = "key"

/*: "Sure" :*/
fileprivate let str_actionValue:String = "Surelaboratory data"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let str_labelName:[UInt8] = [0x3f,0x65,0x74,0x75,0x6d,0x20,0x65,0x68,0x74,0x20,0x6c,0x65,0x63,0x6e,0x61,0x63,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Confirm" :*/
fileprivate let str_appearImageData:[Character] = ["C","o","n","f","i","r","m"]

/*: "The mute has been lifted~" :*/
fileprivate let str_gameManagerData:[Character] = ["T","h","e"," ","m","u","t","e"," ","h","a","s"," ","b","e","e"]
fileprivate let str_upPathValue:String = "n lcard model equal add"
fileprivate let str_shareValue:String = "view defineifted~"

/*: "Confirm to kick off the seat?" :*/
fileprivate let str_timeText:String = "Confititle intimate self label"
fileprivate let str_kitValue:[Character] = ["i","c","k"," ","o"]
fileprivate let str_leadingText:String = "add labff the"

/*: "OK" :*/
fileprivate let str_clickSharedData:[Character] = ["O","K"]

/*: "Shielding Success" :*/
fileprivate let str_rowName:[Character] = ["S","h","i","e","l","d","i","n","g"," ","S","u","c","c","e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MediumCardView.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol CommunicationManagerObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func goBy(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func destroy(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func nameTo(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func loadUid(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class MediumCardView: UIView {
    //: var popView: TalkingPopView?
    var popView: RecommendReactiveCompatible?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: AppStrideable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(SoundThen.account().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = NextTransformable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: CommunicationManagerObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == RecordAppManager.share.loginUserMode.userID {
        if uid == RecordAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        instanceSubviews()
        //: setupSubViewsConstraint()
        tool()
        //: reqUserCardInfo()
        tidings()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_sendColorValue.map{collectionTo(text: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 12)
        label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.pullUpColor()
        label.textColor = UIColor.pullUpColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(methodIcon), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        label.font = .biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.segment()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 13)
        label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.pullUpColor()
        label.textColor = UIColor.pullUpColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .pullUpColor()
        lab.textColor = .pullUpColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(str_aspectData)).localized
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 12)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .segment()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .pullUpColor()
        lab.textColor = .pullUpColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(str_currentMakeData.suffix(4)) + "owing").localized
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 12)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .segment()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(str_sourceSharedData)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.markTag(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.markTag(name: (String(str_roomCallData.prefix(5)) + str_modelEditContent.replacingOccurrences(of: "red", with: "g") + String(str_fillAddData.suffix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(largesseClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: MeModelMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = MeModelMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension MediumCardView {
    //: func reqUserCardInfo() {
    func tidings() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        CellPushListener.modelAcross(uid: self.uid, streamerUid: liveUserId, roomId: SoundThen.account().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<NextTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.topName()
            }
        }
    }

    //: func setUserCardData() {
    func topName() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(str_rowValue)) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.failFinish(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.markTag(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.markTag(name: (String(str_touchLengthInTitle.suffix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            doSex(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func doSex(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: str_makeValue.map{$0^239}, encoding: .utf8)!)
            //: if liveUserId == RecordAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == RecordAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: str_managerName.map{backgroundLocal(log: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: str_userLabValue.map{userCounteraction(to: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.viewColor(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: str_makeValue.map{$0^239}, encoding: .utf8)! {
                //: btn.setImage(UIImage.markTag(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.markTag(name: (String(str_liveValueFileData.suffix(6)) + "oice" + String(str_viewConstraintContent))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: str_managerName.map{backgroundLocal(log: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.markTag(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.markTag(name: (String(str_tabValue.prefix(6)) + "rty_voi" + String(str_whiteTitle) + String(str_iconEqualIfValue.suffix(5)))), for: .normal)
                //: btn.setImage(UIImage.markTag(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.markTag(name: (String(str_ageSendViewName.suffix(7)) + String(str_emptySectionTitle.suffix(6)) + "ce_pre")), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.cutis(fontSize: 15)
                btn.titleLabel?.font = UIFont.cutis(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(controlSender(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(str_sourceSharedData)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func controlSender(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: str_makeValue.map{$0^239}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.destroy(uid: self.userModel.uid)
            //: self.dismiss()
            self.toBottomDismiss()
        //: case "Mike":
        case String(bytes: str_managerName.map{backgroundLocal(log: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.nameTo(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.toBottomDismiss()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.goBy(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.toBottomDismiss()
        //: case "Follow":
        case String(bytes: str_userLabValue.map{userCounteraction(to: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            popAcross(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func popAcross(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        sheaf(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.loadUid(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func sheaf(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(String(str_topUserIfName))] = uid
        //: dict["source"] = "2"
        dict[(str_toOfValue.replacingOccurrences(of: "call", with: "u"))] = "2"
        //: LiveRowRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        LiveRowRequestTool.privacyCompletion(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func methodIcon() {
        //: EnvelopeSinceAdministratorThen.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        EnvelopeSinceAdministratorThen.share.alongUid(uid: self.userModel.uid)
        //: self.dismiss()
        self.toBottomDismiss()
    }
}

//: extension TalkingVoiceRoomCardView {
extension MediumCardView {
    //: @objc func moreBtnClick() {
    @objc func largesseClick() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == RecordAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == RecordAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(str_viewCenterValue.suffix(6)) + String(str_userSizeLetValue)).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(str_washValue.prefix(5)) + String(str_normalData)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(str_makeData.prefix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(str_closedCardName.prefix(5)) + String(str_sizeCenterName)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(str_originalTitle)).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(str_transactionName)).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.RecommendLab.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = LocationDataSource.toArray(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(str_whiteText.suffix(2)) + String(str_tableEqualTitle)))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func sessionEvent() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = RecommendReactiveCompatible(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue)
        //: popView?.initWithView(view: self)
        popView?.matingView(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? AreaReactiveCompatible.getWindow())
        popView?.appInner(view: self.greetAt()?.view ?? AreaReactiveCompatible.rowWindow())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func toBottomDismiss() {
        //: popView?.dismissView()
        popView?.uttermost()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension MediumCardView: SoundViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func stateDown(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(str_viewCenterValue.suffix(6)) + String(str_userSizeLetValue)).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            BackgroundAlertShow.openConfig(title: nil, message: (String(str_hiddenContent.suffix(6)) + "m to" + String(str_sectionReloadData.suffix(6)) + "out t" + String(str_moreData)).localized, leftBtnTitle: (String(str_allowContent.suffix(5)) + str_leadingName.replacingOccurrences(of: "key", with: "l")).localized, rightBtnTitle: (String(str_actionValue.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                TotalerpretationProgressHUD.alienation()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                VoiceReqTool.kickout(targetUid: self.userModel.uid, roomId: SoundThen.account().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    TotalerpretationProgressHUD.countimateEqualDismiss()
                    //: self.dismiss()
                    self.toBottomDismiss()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(str_washValue.prefix(5)) + String(str_normalData)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            BackgroundAlertShow.openConfig(title: nil, message: String(bytes: str_labelName.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(str_allowContent.suffix(5)) + str_leadingName.replacingOccurrences(of: "key", with: "l")).localized, rightBtnTitle: (String(str_appearImageData)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                TotalerpretationProgressHUD.alienation()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                VoiceReqTool.putInAction(targetUid: self.userModel.uid, roomID: SoundThen.account().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    TotalerpretationProgressHUD.countimateEqualDismiss()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.moveAir(showMsg: (String(str_gameManagerData) + String(str_upPathValue.prefix(3)) + String(str_shareValue.suffix(6))).localized)
                    }
                    //: self.dismiss()
                    self.toBottomDismiss()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(str_makeData.prefix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.keyFor()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.toBottomDismiss()

        //: case "Kick off".localized: /// 踢下麦
        case (String(str_closedCardName.prefix(5)) + String(str_sizeCenterName)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            BackgroundAlertShow.openConfig(title: nil, message: (String(str_timeText.prefix(5)) + "rm to k" + String(str_kitValue) + String(str_leadingText.suffix(6)) + " seat?").localized, leftBtnTitle: (String(str_allowContent.suffix(5)) + str_leadingName.replacingOccurrences(of: "key", with: "l")).localized, rightBtnTitle: (String(str_actionValue.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                SoundThen.account().caracole(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.toBottomDismiss()
            }

        //: case "Report".localized:
        case (String(str_transactionName)).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = ConditionListReactiveCompatible(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.toView(view: nil)
        //: case "Block".localized:
        case (String(str_originalTitle)).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            BackgroundAlertShow.openConfig(title: nil, message: kLet_infoErrorData, leftBtnTitle: (String(str_allowContent.suffix(5)) + str_leadingName.replacingOccurrences(of: "key", with: "l")).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                PostulationReactiveCompatible.statusLab(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.moveAir(showMsg: (String(str_rowName)).localized)
                        //: self.dismiss()
                        self.toBottomDismiss()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension MediumCardView {
    // 添加视图
    //: private func setupSubviews() {
    private func instanceSubviews() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func tool() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kLet_itemData / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kLet_itemData / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kLet_itemData / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.greetAt()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.greetAt()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.toBottomDismiss()
        }
    }
}
