
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_giftTitle:String = "SOwindowKE"
fileprivate let str_unknownData:String = "class cell dataT_ADD"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_refImageWhiteData:[UInt8] = [0xd1,0xcd,0xc1,0xc9,0xc7,0xd6,0xdd,0xd4,0xcb,0xc6,0xc7,0xcd,0xc1,0xca,0xc3,0xd6,0xdd,0xd0,0xc7,0xd3,0xd7,0xc7,0xd1,0xd6,0xc1,0xc3,0xce,0xce,0xdd,0xcc,0xcd,0xd6,0xcb,0xc4,0xcb,0xc1,0xc3,0xd6,0xcb,0xcd,0xcc]

private func scienceLab(awake num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_jumpName:[UInt8] = [0xab,0x9e,0x9c,0xa2,0xaf,0x9e,0xb8,0x9f,0xa5,0xa8,0x9a,0xad,0xac,0x9c,0xab,0x9e,0x9e,0xa7,0xb8,0xa7,0xa8,0xad,0xa2,0x9f,0xa2,0x9c,0x9a,0xad,0xa2,0xa8,0xa7]

fileprivate func offlineMark(text num: UInt8) -> UInt8 {
    let value = Int(num) + 167
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isCalling" :*/
fileprivate let str_screenName:String = "video temp resultisCallin"
fileprivate let str_appTitleText:String = "gift"

/*: "isTalking" :*/
fileprivate let str_availableValue:[Character] = ["i","s","T","a","l","k","i"]
fileprivate let str_statusRangeData:[Character] = ["n","g"]

/*: "SOCKET 连接成功." :*/
fileprivate let str_markData:String = "SOClabT"

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_picData:String = "SOCrequestET"
fileprivate let str_adminText:[Character] = [" ","连","接","失","败",","," ","e","r","r","o","r",":"]

/*: ." :*/
fileprivate let str_imagePauseDataText:String = "without"

/*: "SOCKET 失败 :*/
fileprivate let str_mediumRowData:String = "SOCcontentT"
fileprivate let str_youName:String = " 失败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_laboratoryKindValue:String = "\u{6b21}, \u{5207}\u{6362}过"
fileprivate let str_selectedTopValue:String = "t\u{57df}名重"
fileprivate let str_honeyValue:[Character] = ["新","尝"]
fileprivate let str_layerName:[Character] = ["试","\u{ff0c}","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_coinText:String = "SOCKET self view touch at model"
fileprivate let str_colorModelTitle:[Character] = ["已","关","闭"]
fileprivate let str_textPlaceContent:String = ", code:segment model let manager"

/*: , reason: :*/
fileprivate let str_rowData:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: "ws:// :*/
fileprivate let str_scaleTitle:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let str_interactionTitle:String = "/?sid=sex read value"

/*: &uid= :*/
fileprivate let str_actionName:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let str_sizeValueName:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e"]
fileprivate let str_homeData:String = "on self let title messagersion="

/*: &packageId= :*/
fileprivate let str_kitName:String = "&packview let layer reason"
fileprivate let str_failureResultName:String = "ageId=color field"

/*: "1v1server. :*/
fileprivate let str_makeValue:String = "to table live1v1serv"
fileprivate let str_tabValue:[Character] = ["e","r","."]

/*: .com:7766" :*/
fileprivate let str_shareTitle:String = ".com:776view view new"
fileprivate let str_panImageValue:String = "listen"

/*: "43.129.185.194:7767" :*/
fileprivate let str_targetText:String = "at3.129.18"
fileprivate let str_fileToValue:[Character] = ["5",".","1"]
fileprivate let str_conversationContent:String = "9name:7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_hideName:[UInt8] = [0xb0,0xac,0xa0,0xa8,0xa2,0xb1,0x7d,0x44,0x1a,0xee,0x44,0x18,0xf9,0xcf,0xc2,0xbe,0xc0,0xc5,0xbe,0xbf,0xc6,0xc9,0xc6,0xd1,0xd6,0xa0,0xc5,0xbe,0xcb,0xc4,0xc2,0xc1,0x44,0xf8,0xee,0x42,0xed,0x9,0x4c,0x19,0xe9,0x43,0xf3,0xa,0x44,0x1a,0xee,0x42,0xe2,0x10,0x46,0xf4,0xa,0x8b]

fileprivate func actualPackageTable(load num: UInt8) -> UInt8 {
    let value = Int(num) - 93
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let str_mobileData:String = "cmdata"

/*: "ping" :*/
fileprivate let str_makeTopData:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let str_profileData:String = "callHblack free"
fileprivate let str_messageMakeValue:String = "BEAT"

/*: "recievedDate" :*/
fileprivate let str_cornerGiftText:[Character] = ["r","e","c","i","e","v","e","d","D"]
fileprivate let str_actionText:String = "ATE"

/*: "liveHeartBeat" :*/
fileprivate let str_equalViewIfData:String = "livresume"
fileprivate let str_enableValue:String = "Beatclass super live model add"

/*: "🔗 关闭 socket" :*/
fileprivate let str_actionExtraName:String = "🔗 关闭 sohead background button equal"
fileprivate let str_smallVideoData:String = "ckecontent"

/*: "The user logs out" :*/
fileprivate let str_labelValue:[Character] = ["T","h","e"," ","u"]
fileprivate let str_replaceTitle:String = "model text valueser l"

/*: "SOCKET  :*/
fileprivate let str_makeData:String = "SOCKET source detail sub"

/*: 重连. 重试次数： :*/
fileprivate let str_contentEqualData:[Character] = ["重","\u{8fde}","."]
fileprivate let str_responseTitle:String = " 重试次数："

/*: , 地址： :*/
fileprivate let str_becomeName:String = ", \u{5730}\u{5740}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_resumeValue:String = "SOlayerK"
fileprivate let str_actualContent:String = "正常 \u{4e0d}需"
fileprivate let str_actionTitle:String = "要重新连\u{63a5}."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_blockTitle:String = "soc"
fileprivate let str_uniformLocalData:String = "program moreKET "
fileprivate let str_scaleValue:String = " 不需equal重"

/*: "&resume=1" :*/
fileprivate let str_giftViewValue:String = "&resumeplayer free more in"
fileprivate let str_upLabelData:String = "=1"

/*: "&lang= :*/
fileprivate let str_controlValue:String = "layer size&lang"
fileprivate let str_succeedData:String = "="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_interactionData:[Character] = ["S","O","C","K","E","T"," ","开","始"]
fileprivate let str_topValue:String = "连接, can址："

/*: "onNetDisabled" :*/
fileprivate let str_youAtValue:String = "view in sectiononNe"
fileprivate let str_adjustmentImageListValue:[Character] = ["a","b","l","e","d"]

/*: "errno" :*/
fileprivate let str_toMomentData:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let str_managerName:String = "damodela"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_addTitlePostData:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{4fdd}","活","c","m"]
fileprivate let str_loadData:[Character] = ["d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_kitInfoValue:[UInt8] = [0x2e,0x74,0x61,0x65,0x62,0x74,0x72,0x61,0x65,0x48,0x6c,0x6c,0x61,0x63,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x63,0x74,0x72,0x74,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_likeShareText:[UInt8] = [0xe3,0xff,0xf3,0xfb,0xf5,0xe4,0x90,0xdc,0xd9,0xc6,0xd5,0x54,0xf,0x2d,0x56,0x4,0xb,0xd3,0xdd,0xd4,0x8a,0x90,0xdc,0xd9,0xc6,0xd5,0xf8,0xd5,0xd1,0xc2,0xc4,0xf2,0xd5,0xd1,0xc4,0x9e]

private func rangeRow(bubble num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "onP" :*/
fileprivate let str_mData:String = "onPfatal actual"

/*: "msg" :*/
fileprivate let str_readingModeLetName:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_tagText:String = "🔗 处理消\u{606f}："
fileprivate let str_shareValue:String = "time targetcmdN"

/*: , errNo: :*/
fileprivate let str_tempTitle:[Character] = [","," ","e","r","r","N"]
fileprivate let str_willViewTitle:String = "o:share self button model data"

/*: , msg: :*/
fileprivate let str_whiteText:String = ", msg:failure info"

/*: , data: :*/
fileprivate let str_storeText:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_fatalText:String = "SOCplan"
fileprivate let str_failValue:String = "常消息\u{ff1a}c"
fileprivate let str_waitName:[Character] = ["m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let str_netTitle:String = "image self to self infoonRec"

/*: "onVideoMatch" :*/
fileprivate let str_itemText:[Character] = ["o","n","V","i","d","e","o","M","a"]
fileprivate let str_coverPairTitle:String = "tcto"

/*: "onRequestCall" :*/
fileprivate let str_overData:String = "onReqbehind message"
fileprivate let str_firstGiftData:String = "ALL"

/*: "onCancelCall" :*/
fileprivate let str_detailTitle:String = "onCanfor height case"

/*: "onRefuseCall" :*/
fileprivate let str_willRequestName:[Character] = ["o","n"]
fileprivate let str_popMapData:[Character] = ["R","e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let str_searchGrayImageText:String = "onAccmake equal let block"
fileprivate let str_ownerText:String = "L"

/*: "onStopCall" :*/
fileprivate let str_valueTopData:[Character] = ["o","n","S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let str_toKeyPlaceData:String = "add mode asfreeCa"
fileprivate let str_layerValue:String = "credits"

/*: "mfCoin" :*/
fileprivate let str_hiddenData:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let str_reasonTableText:String = "oinfo"
fileprivate let str_makeName:String = "as message stringMissCoin"

/*: "acceptCall" :*/
fileprivate let str_touchData:[Character] = ["a","c","c","e","p","t"]
fileprivate let str_loadMessageName:[Character] = ["C","a","l","l"]

/*: "stopCall" :*/
fileprivate let str_layerContent:[Character] = ["s","t","o","p","C","a"]
fileprivate let str_makeSoundValue:[Character] = ["l","l"]

/*: "requestCall" :*/
fileprivate let str_stackName:[Character] = ["r","e"]
fileprivate let str_eachData:String = "QUEST"

/*: "[Invite you to a video call...]" :*/
fileprivate let str_mediumTitle:[UInt8] = [0x16,0x4,0x29,0x31,0x24,0x2f,0x20,0xdb,0x34,0x2a,0x30,0xdb,0x2f,0x2a,0xdb,0x1c,0xdb,0x31,0x24,0x1f,0x20,0x2a,0xdb,0x1e,0x1c,0x27,0x27,0xe9,0xe9,0xe9,0x18]

fileprivate func managerFile(share num: UInt8) -> UInt8 {
    let value = Int(num) + 69
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let str_locationText:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_textTitleContent:[UInt8] = [0x98,0x86,0xab,0xb3,0xa6,0xb1,0xa2,0x5d,0xb6,0xac,0xb2,0x5d,0xb1,0xac,0x5d,0x9e,0x5d,0xb3,0xac,0xa6,0xa0,0xa2,0x5d,0xa0,0x9e,0xa9,0xa9,0x6b,0x6b,0x6b,0x9a]

fileprivate func showSex(direction num: UInt8) -> UInt8 {
    let value = Int(num) - 61
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let str_blockName:String = "sid"

/*: "nickname" :*/
fileprivate let str_rowMarkUserTitle:[Character] = ["n","i","c"]
fileprivate let str_currentFillData:String = "knasharede"

/*: "headPic" :*/
fileprivate let str_labMakeData:String = "self style skipheadPic"

/*: "onCallResume" :*/
fileprivate let str_languageContent:String = "onCaltext progress"
fileprivate let str_touchViewValue:[Character] = ["l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let str_equalEffectText:String = "true acancelC"
fileprivate let str_labelModeValue:String = "aviewview"

/*: "refuseCall" :*/
fileprivate let str_titleCreateValue:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let str_needMoreMaleName:[Character] = ["o","n","C","a","l","l"]
fileprivate let str_gestureGiftTitle:String = "Motivahidden make end manager user"

/*: "onLiveWarning" :*/
fileprivate let str_managerTitle:String = "onLivlet right info"
fileprivate let str_appContent:[Character] = ["e","W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let str_wrapSharedFrameData:[Character] = ["r"]
fileprivate let str_yaValue:String = "EASON"

/*: "time" :*/
fileprivate let str_viewEnableCellValue:String = "ticonstraint"

/*: "onStopLive" :*/
fileprivate let str_reasonValue:String = "info view gift push laboratoryonStopLiv"
fileprivate let str_tableEqualName:String = "request"

/*: "onSendProgramGift" :*/
fileprivate let str_labelData:String = "block add pushonSe"
fileprivate let str_segmentValue:String = "devicegr"
fileprivate let str_interactionModelVoiceTitle:[Character] = ["a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let str_clearValue:[Character] = ["o","n","G","a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let str_taskTitle:String = "broadSguard player fill value"

/*: "onCallChatMsg" :*/
fileprivate let str_positionName:String = "O"
fileprivate let str_halfMakeText:[Character] = ["n","C","a","l","l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let str_topStatusData:String = "self type array with returnonNo"
fileprivate let str_homeName:String = "onPopif can"

/*: "onUserBlock" :*/
fileprivate let str_emptyBirthViewName:String = "double call class modeonUser"

/*: "freeCallWin" :*/
fileprivate let str_addBlockData:String = "freeCtitle error handle left"
fileprivate let str_frontValue:[Character] = ["i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManagingDirectorReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import UIKit
import UIKit

//: import SocketRocket
import SocketRocket

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_eventData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_barFileName = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_failureData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_clickUseText = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_progressNeedValue = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_deviceBottomName = (str_giftTitle.replacingOccurrences(of: "window", with: "C") + String(str_unknownData.suffix(5)) + "R_KEY")

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_contentIndexText = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_sizeTitle = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_backScreenDeviceValue = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_barLimitTitle = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_ofTitle = NSNotification.Name(rawValue: String(bytes: str_refImageWhiteData.map{scienceLab(awake: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_logData = NSNotification.Name(rawValue: String(bytes: str_jumpName.map{offlineMark(text: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol TopObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func clickError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol KeyMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func selectData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func theory(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ResultCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func technicality(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func loadCell(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func socketUp(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func lose(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol ActualObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func react(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func currency(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func fromData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ManagingDirectorReactiveCompatible: NSObject {
	var noteOpen: Bool = false
	var viewDictionary: [AnyHashable: String] = [:]

    //: enum SocketRetryType: String {
    enum ColorAppType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_itemData,
        //: y: StatusBarHeight+190
        y: kLet_pathErrData + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_itemData,
        //: y: StatusBarHeight+190
        y: kLet_pathErrData + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: LabelToReactiveCompatible.shared.direction == .rightToLeft ? 0 : kLet_itemData,
        //: y: StatusBarHeight+500
        y: kLet_pathErrData + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: TopObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: KeyMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ResultCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: ActualObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ManagingDirectorReactiveCompatible()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { 
		if var quoteValue = connectUrlStr { 
		if let containerView = containerView {
	
	            if (containerView.layer.anchorPoint.y != 0.5) && (containerView.forFirstBaselineLayout.center.y == 14.43) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let pendingAction = PlusView(frame: containerView.bounds.intersection(CGRect(x: CGFloat(0), y: CGFloat(261.75), width: CGFloat(19.98), height: CGFloat(160.58))))
	            pendingAction.itemName = quoteValue
	            
	            
	            pendingAction.labelClose = { [self] colorClose in
	            self.noteOpen = colorClose
	            
	            var pendingAction = isCalling
	            pendingAction = true
	            if pendingAction != self.noteOpen {
	                self.noteOpen = pendingAction
	            }
	            
	                self.noteOpen = true
	                self.noteOpen = true
	            return self.noteOpen
	            }
	            pendingAction.modelDictionary = { [self] methodGiftDictionary in
	            self.viewDictionary = methodGiftDictionary
	            
	            guard var value = self.viewDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                containerView.addSubview(pendingAction)
	            }
	
		}
	
		}
		return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(analogDigitalConverterNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(inset),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(str_screenName.suffix(8)) + str_appTitleText.replacingOccurrences(of: "gift", with: "g")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ContextApplication.shared as! ContextApplication).isGift()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(str_availableValue) + String(str_statusRangeData)))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ContextApplication.shared as! ContextApplication).isGift()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ManagingDirectorReactiveCompatible: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: ShowLogTool.cellText(msg: "SOCKET 连接成功.")
        ShowLogTool.cellText(msg: (str_markData.replacingOccurrences(of: "lab", with: "KE") + " 连接\u{6210}功."))
        //: sendPing()
        collideWith()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_mineData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_deviceBottomName)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        anNeed()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: ShowLogTool.cellText(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        ShowLogTool.cellText(msg: (str_picData.replacingOccurrences(of: "request", with: "K") + String(str_adminText)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = RecordAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = RecordAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_clickUseText {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = channelOff(isRetry: true)
            //: ShowLogTool.cellText(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            ShowLogTool.cellText(msg: (str_mediumRowData.replacingOccurrences(of: "content", with: "KE") + str_youName.capitalized) + "\(kLet_clickUseText)" + (str_laboratoryKindValue + "Socke" + str_selectedTopValue + String(str_honeyValue) + String(str_layerName)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.outsideEffect(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.viewAcrossMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: ShowLogTool.cellText(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        ShowLogTool.cellText(msg: (String(str_coinText.prefix(7)) + String(str_colorModelTitle) + String(str_textPlaceContent.prefix(7))) + "\(code)" + (String(str_rowData)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.hit(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_barLimitTitle {
            //: return
            return
        }
        //: if let status = RecordAppManager.share.reachability?.connection, status != .unavailable {
        if let status = RecordAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.outsideEffect(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func channelOff(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = RecordAppManager.share.appConfigMode.ws
        self.linkAddrArr = RecordAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = titleOf()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_deviceBottomName)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = RecordAppManager.share.loginUid
        let uid = RecordAppManager.share.loginUid
        //: let sid = RecordAppManager.share.loginSessionId
        let sid = RecordAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(str_interactionTitle.prefix(6))) + "\(sid)" + (String(str_actionName)) + "\(uid)" + (String(str_sizeValueName) + String(str_homeData.suffix(6))) + "\(kLet_userNoText)" + (String(str_kitName.prefix(5)) + String(str_failureResultName.prefix(6))) + "\(kLet_closedData)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func titleOf() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_marginRoomData ? [(String(str_makeValue.suffix(7)) + String(str_tabValue)) + "\(kLet_keyViewName)" + (String(str_shareTitle.prefix(8)) + str_panImageValue.replacingOccurrences(of: "listen", with: "6"))] : [(str_targetText.replacingOccurrences(of: "at", with: "4") + String(str_fileToValue) + str_conversationContent.replacingOccurrences(of: "name", with: "4"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func analogDigitalConverterNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: ShowLogTool.cellText(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            ShowLogTool.cellText(msg: String(bytes: str_hideName.map{actualPackageTable(load: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.hit(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.outsideEffect(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func inset() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.outsideEffect(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.counteractionModel()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// socket保活心跳包
    //: private func sendPing() {
    private func collideWith() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_eventData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.levelFront(info: [(str_mobileData.replacingOccurrences(of: "data", with: "d")): (String(str_makeTopData))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func penetrate() {
        //: endTalkingPing()
        briquette()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_barFileName, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.greetAt()?.isKind(of: ControllerButtonErrorDelegate.self) == false && self.greetAt()?.isKind(of: VersionThen.self) == false) && (self.currTalkingVC?.isKind(of: ControllerButtonErrorDelegate.self) == false && self.currTalkingVC?.isKind(of: VersionThen.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.levelFront(info: [(str_mobileData.replacingOccurrences(of: "data", with: "d")): (String(str_profileData.prefix(5)) + "eart" + str_messageMakeValue.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func briquette() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func counteractionModel() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(str_cornerGiftText) + str_actionText.lowercased())] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_methodPartyData {
                //: RecordAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                RecordAppManager.share.goEnable(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// 开启直播心跳包
    //: func startLivePing() {
    func simultaneouslyPing() {
        //: endLivePing()
        nest()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_failureData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.levelFront(info: [(str_mobileData.replacingOccurrences(of: "data", with: "d")): (str_equalViewIfData.replacingOccurrences(of: "resume", with: "e") + "Heart" + String(str_enableValue.prefix(4)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func nest() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func anNeed() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = ProgressThen()
        //: let keyWindow = AreaReactiveCompatible.getWindow()
        let keyWindow = AreaReactiveCompatible.rowWindow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func eds() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard CellPushListener.inserter().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard SoundThen.account().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func hit(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(str_actionExtraName.prefix(7)) + str_smallVideoData.replacingOccurrences(of: "content", with: "t")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_barLimitTitle, reason: (String(str_labelValue) + String(str_replaceTitle.suffix(5)) + "ogs out"))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        briquette()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            eds()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func sizeAction() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ManagingDirectorReactiveCompatible.shared.channelOff(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.outsideEffect(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func outsideEffect(timeInterval: TimeInterval, type: ColorAppType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: ShowLogTool.cellText(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            ShowLogTool.cellText(msg: (String(str_makeData.prefix(7))) + "\(type.rawValue)" + (String(str_contentEqualData) + str_responseTitle.capitalized) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.tick(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func tick(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: ShowLogTool.cellText(msg: "SOCKET 连接正常 不需要重新连接.")
                ShowLogTool.cellText(msg: (str_resumeValue.replacingOccurrences(of: "layer", with: "C") + "ET 连接" + str_actualContent + str_actionTitle))
                //: } else {
            } else {
                //: ShowLogTool.cellText(msg: "SOCKET 正在连接 不需要重新连接.")
                ShowLogTool.cellText(msg: (str_blockTitle.uppercased() + String(str_uniformLocalData.suffix(4)) + "正\u{5728}连接" + str_scaleValue.replacingOccurrences(of: "equal", with: "要") + "新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(str_giftViewValue.prefix(7)) + str_upLabelData.capitalized))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(str_controlValue.suffix(5)) + str_succeedData.capitalized) + "\(DemandReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        hit(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_progressNeedValue
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: ShowLogTool.cellText(msg: "SOCKET 开始连接, 地址：\(newStr).")
        ShowLogTool.cellText(msg: (String(str_interactionData) + str_topValue.replacingOccurrences(of: "can", with: "地")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func levelFront(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if RecordAppManager.share.reachability?.connection == nil || RecordAppManager.share.reachability?.connection == .unavailable {
        if RecordAppManager.share.reachability?.connection == nil || RecordAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(str_mobileData.replacingOccurrences(of: "data", with: "d")): (String(str_youAtValue.suffix(4)) + "tDis" + String(str_adjustmentImageListValue)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(str_toMomentData)): "\(kLet_backScreenDeviceValue)",
                                       //: "data": [:]]
                                       (str_managerName.replacingOccurrences(of: "model", with: "t")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            viewAcrossMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(str_mobileData.replacingOccurrences(of: "data", with: "d"))] as? String == (String(str_makeTopData)) {
                //: ShowLogTool.cellText(msg: "SOCKET 连接保活cmd: ping.")
                ShowLogTool.cellText(msg: (String(str_addTitlePostData) + String(str_loadData)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(str_mobileData.replacingOccurrences(of: "data", with: "d"))] as? String == (String(str_profileData.prefix(5)) + "eart" + str_messageMakeValue.lowercased()) {
                //: ShowLogTool.cellText(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                ShowLogTool.cellText(msg: String(bytes: str_kitInfoValue.reversed(), encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(str_mobileData.replacingOccurrences(of: "data", with: "d"))] as? String == (str_equalViewIfData.replacingOccurrences(of: "resume", with: "e") + "Heart" + String(str_enableValue.prefix(4))) {
                //: ShowLogTool.cellText(msg: "SOCKET live保活cmd: liveHeartBeat.")
                ShowLogTool.cellText(msg: String(bytes: str_likeShareText.map{rangeRow(bubble: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(str_mobileData.replacingOccurrences(of: "data", with: "d"))] as? String == (String(str_makeTopData)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(str_mobileData.replacingOccurrences(of: "data", with: "d")): (String(str_mData.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(str_toMomentData)): "\(kLet_contentIndexText)",
                                           //: "data": [:]]
                                           (str_managerName.replacingOccurrences(of: "model", with: "t")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                viewAcrossMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.outsideEffect(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func viewAcrossMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(str_mobileData.replacingOccurrences(of: "data", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(str_toMomentData))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(str_readingModeLetName))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(str_managerName.replacingOccurrences(of: "model", with: "t"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (str_tagText + String(str_shareValue.suffix(4)) + "ame:") + "\(cmdName)" + (String(str_tempTitle) + String(str_willViewTitle.prefix(2))) + "\(errorNo)" + (String(str_whiteText.prefix(6))) + "\(msg)" + (String(str_storeText)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        addIndex(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: ShowLogTool.cellText(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            ShowLogTool.cellText(msg: (str_fatalText.replacingOccurrences(of: "plan", with: "K") + "ET 接\u{6536}异" + str_failValue + String(str_waitName)) + "\(cmdName)" + (String(str_tempTitle) + String(str_willViewTitle.prefix(2))) + "\(errorNo)" + (String(str_whiteText.prefix(6))) + "\(msg)" + (String(str_storeText)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: RecordAppManager.share.loginUserMode.freeCallTimes = 0
                RecordAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !RecordAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !RecordAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ItemReactiveCompatible.shared.voiceMediumGift()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.clickError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_netTitle.suffix(5)) + "eivedGift") { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_playMessageData, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_itemText) + str_coverPairTitle.replacingOccurrences(of: "to", with: "h")) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.selectData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_overData.prefix(5)) + "uestC" + str_firstGiftData.lowercased()) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.theory(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_detailTitle.prefix(5)) + "celCall") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_willRequestName) + String(str_popMapData)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_searchGrayImageText.prefix(5)) + "eptCal" + str_ownerText.lowercased()) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            penetrate()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.currency(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_valueTopData)) { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            briquette()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_toKeyPlaceData.suffix(6)) + "llTim" + str_layerValue.replacingOccurrences(of: "credit", with: "e"))] as? Int {
                //: RecordAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                RecordAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: RecordAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RecordAppManager.share.loginUserMode.mf_coin = json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(String(str_hiddenData))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_purchaseName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ContextApplication.shared as! ContextApplication).jostle()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (str_reasonTableText.replacingOccurrences(of: "info", with: "n") + String(str_makeName.suffix(8))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.lose(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(str_touchData) + String(str_loadMessageName)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            penetrate()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.technicality(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_layerContent) + String(str_makeSoundValue)) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            briquette()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.loadCell(data: data)
            // 更新余额
            //: RecordAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RecordAppManager.share.loginUserMode.mf_coin = json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(String(str_hiddenData))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            happeningData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_purchaseName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ContextApplication.shared as! ContextApplication).jostle()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(str_stackName) + str_eachData.lowercased() + "Call") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_ofTitle, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: RecordAppManager.share.recieve1v1TalkRequest(info: data)
                RecordAppManager.share.goEnable(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(str_cornerGiftText) + str_actionText.lowercased())] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_mediumTitle.map{managerFile(share: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(String(str_locationText))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_textTitleContent.map{showSex(direction: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ColorDelegateHelper.numbererval(uid: String(json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(str_blockName.replacingOccurrences(of: "s", with: "u"))].intValue),
                                              //: title: data["nickname"] as? String,
                                              title: data[(String(str_rowMarkUserTitle) + str_currentFillData.replacingOccurrences(of: "shared", with: "m"))] as? String,
                                              //: body: body,
                                              body: body,
                                              //: imageUrl: data["headPic"] as? String)
                                              imageUrl: data[(String(str_labMakeData.suffix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ColorDelegateHelper.shared.getMoving()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(str_languageContent.prefix(5)) + String(str_touchViewValue)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            penetrate()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(str_languageContent.prefix(5)) + String(str_touchViewValue))] = "1"
            //: RecordAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            RecordAppManager.share.beyondInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ContextApplication.shared as! ContextApplication).isGift()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(str_equalEffectText.suffix(7)) + str_labelModeValue.replacingOccurrences(of: "view", with: "l")) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            briquette()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.socketUp(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ColorDelegateHelper.metadata(identifier: String(json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(str_blockName.replacingOccurrences(of: "s", with: "u"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ColorDelegateHelper.shared.stopBagCradle()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(str_titleCreateValue)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            briquette()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.react(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_needMoreMaleName) + String(str_gestureGiftTitle.prefix(6)) + "tion") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if RecordAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.fromData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_managerTitle.prefix(5)) + String(str_appContent)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            CellPushListener.listConstraint(content: msg, reason: json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(String(str_wrapSharedFrameData) + str_yaValue.lowercased())].stringValue, time: json[(str_managerName.replacingOccurrences(of: "model", with: "t"))][(str_viewEnableCellValue.replacingOccurrences(of: "constraint", with: "me"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_reasonValue.suffix(9)) + str_tableEqualName.replacingOccurrences(of: "request", with: "e")) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            CellPushListener.inserter().appResource()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_labelData.suffix(4)) + "ndPr" + str_segmentValue.replacingOccurrences(of: "device", with: "o") + String(str_interactionModelVoiceTitle)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.moveAir(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_clearValue)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(str_taskTitle.prefix(6)) + "tatus")] as? Int ?? -1
            //: if RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_logData, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_playMessageData, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_logData, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (str_positionName.lowercased() + String(str_halfMakeText)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TalkingNameAdministratorDanmuManager.correlationTable().globalMsg(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_topStatusData.suffix(4)) + "tificati" + String(str_homeName.prefix(5)) + "upWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ItemReactiveCompatible.shared.firstborn(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_emptyBirthViewName.suffix(6)) + "Block") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            CellPushListener.inserter().appResource()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            SoundThen.account().welt()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_domainChangeMessageValue, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ManagingDirectorReactiveCompatible {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func addIndex(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(str_stackName) + str_eachData.lowercased() + "Call") || cmdName == (String(str_overData.prefix(5)) + "uestC" + str_firstGiftData.lowercased()) {
            //: if TalkingLiveManager.shared().isLive == true {
            if CellPushListener.inserter().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_popVideoTitle = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                CellPushListener.addAt(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                CellPushListener.inserter().appResource()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_popVideoTitle == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(str_equalEffectText.suffix(7)) + str_labelModeValue.replacingOccurrences(of: "view", with: "l")) || cmdName == (String(str_detailTitle.prefix(5)) + "celCall") ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(str_titleCreateValue)) || cmdName == (String(str_willRequestName) + String(str_popMapData)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_layerContent) + String(str_makeSoundValue)) || cmdName == (String(str_valueTopData))
        {
            //: NEED_RESTORE_LIVE = false
            kLet_popVideoTitle = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ElectronicTitleView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                CellPushListener.inserter().equalCheck()
            }
            //: alert.show()
            alert.statusFor()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func happeningData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_toKeyPlaceData.suffix(6)) + "llTim" + str_layerValue.replacingOccurrences(of: "credit", with: "e"))] as? Int {
            //: RecordAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            RecordAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(str_addBlockData.prefix(5)) + "allW" + String(str_frontValue))] as? Bool
        //: if freeCallWin == true && RecordAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = RecordAlbumErrorDelegate()
            //: view.show()
            view.slipUp()
            //: view.setShowData(data: data)
            view.toSitDown(data: data)
        }
    }
}
