
//: Declare String Begin

/*: "formatType" :*/
fileprivate let str_cancelTitle:[Character] = ["f","o"]
fileprivate let str_hiddenData:[Character] = ["r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let str_actualValue:[Character] = ["p","l","a","i","n"]

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_jumpText:[UInt8] = [0x96,0x91,0x96,0x8b,0xd7,0x9c,0x90,0x9b,0x9a,0x8d,0xc5,0xd6,0xdf,0x97,0x9e,0x8c,0xdf,0x91,0x90,0x8b,0xdf,0x9d,0x9a,0x9a,0x91,0xdf,0x96,0x92,0x8f,0x93,0x9a,0x92,0x9a,0x91,0x8b,0x9a,0x9b]

private func lifeStyle(head num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "#FF2348" :*/
fileprivate let str_styleTitleData:[Character] = ["#"]
fileprivate let str_voiceValue:[Character] = ["F","F","2","3","4","8"]

/*: "icon_lounge" :*/
fileprivate let str_titleItemName:String = "request add progress title oficon_lou"
fileprivate let str_managerData:[Character] = ["n","g","e"]

/*: "icon_home_v" :*/
fileprivate let str_voicePastData:String = "afterco"
fileprivate let str_makeTitle:String = "view hidden size tagn_ho"

/*: "icon_chat_list_new" :*/
fileprivate let str_observerValue:String = "add height pathicon_"
fileprivate let str_directionValue:[Character] = ["_","l"]
fileprivate let str_beautyData:String = "ist_newpath color count to"

/*: "Official" :*/
fileprivate let str_giftTitle:String = "Officimonth string var"
fileprivate let str_subTitle:[Character] = ["a","l"]

/*: "#30D00B" :*/
fileprivate let str_actualDataCollectText:String = "net object equal#30D0"
fileprivate let str_yourObserveTitle:String = "professional white finish else let0B"

/*: "iv_message_honey" :*/
fileprivate let str_timeTitle:String = "user type awake finish bagiv_m"
fileprivate let str_observerData:[Character] = ["g","e","_","h","o","n","e","y"]

/*: "#FF60B9" :*/
fileprivate let str_socialHalfTitle:[Character] = ["#","F","F","6","0","B","9"]

/*: "#EEEEEE" :*/
fileprivate let str_addPriceValue:String = "#EEEEEEgroup temp false biology import"

/*: "opacity" :*/
fileprivate let str_dismissRangeData:String = "ocollection"
fileprivate let str_cellDocumentValue:[Character] = ["a","c","i","t","y"]

/*: "path" :*/
fileprivate let str_timeIndexTitle:[Character] = ["p","a","t","h"]

/*: "extra" :*/
fileprivate let str_freeName:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let str_attachValue:[Character] = ["m"]
fileprivate let str_barTempValue:String = "sgInfoicon fill player time show"

/*: "content" :*/
fileprivate let str_mValue:[Character] = ["c","o","n","t","e","n","t"]

/*: "messageType" :*/
fileprivate let str_normalName:[Character] = ["m","e","s","s","a"]
fileprivate let str_equalContent:[Character] = ["g"]
fileprivate let str_listContent:String = "eTypelaboratory row extension var"

/*: "[Reply messages to get points]" :*/
fileprivate let str_cookieTitle:[Character] = ["[","R","e","p","l","y"," ","m","e","s","s","a","g","e","s"," ","t","o"," ","g","e","t"," ","p"]
fileprivate let str_halfTitle:String = "type medium messageoints]"

/*: "title" :*/
fileprivate let str_kitData:String = "maketle"

/*: "img" :*/
fileprivate let str_skirtText:[Character] = ["i","m","g"]

/*: "[Photo]" :*/
fileprivate let str_compareValue:[Character] = ["[","P","h","o","t","o","]"]

/*: "[Audio]" :*/
fileprivate let str_noValue:[Character] = ["[","A","u","d","i","o","]"]

/*: "rYMsgType" :*/
fileprivate let str_skipValue:String = "rYMhalf change processor"
fileprivate let str_makeLayerName:String = "mark name to appear itemsgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_voiceName:[Character] = ["G","J",":","C","a","l","l","C","u","s","t"]
fileprivate let str_reportManagerEndTitle:String = "olab"

/*: "callCmd" :*/
fileprivate let str_listViewContent:String = "title"
fileprivate let str_atManagerName:String = "refuse corner trueallCmd"

/*: "missCall" :*/
fileprivate let str_layerText:[Character] = ["m","i","s","s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let str_pushRegularColorValue:[Character] = ["[","M","i","s","s","e","d"," ","C","a","l","l","]"]

/*: "logType" :*/
fileprivate let str_kitTitle:String = "logTytransform in center true view"
fileprivate let str_intimateText:String = "close"

/*: "[Voice Call]" :*/
fileprivate let str_errorMomentLetTitle:[Character] = ["[","V","o","i","c","e"," ","C","a","l","l","]"]

/*: "[Video Call]" :*/
fileprivate let str_timeText:[Character] = ["["]
fileprivate let str_sexRefreshLabValue:String = "to pathVideo"

/*: "video" :*/
fileprivate let str_resultMakeText:String = "videitem"

/*: "contentType" :*/
fileprivate let str_pushFromValue:String = "CONT"

/*: "VideoMsg" :*/
fileprivate let str_viewBlueCenterText:String = "VideoMstrue line day color let"
fileprivate let str_endLabTitle:String = "data"

/*: "[Short Video]" :*/
fileprivate let str_tagText:String = "[Shortself player return transaction"

/*: "match" :*/
fileprivate let str_sexValue:[Character] = ["m","a","t","c","h"]

/*: "[Cupid messaging]" :*/
fileprivate let str_editData:String = "[Cupidroom view hidden view"
fileprivate let str_viewSizeName:String = "S"
fileprivate let str_labPhotoValue:[Character] = ["a","g","i","n","g","]"]

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let str_iconFromName:String = "GJ:Sif view data"
fileprivate let str_makeContent:String = "lab videoTxtC"

/*: "isSend" :*/
fileprivate let str_valueRootText:[Character] = ["i","s","S","e","n","d"]

/*: "Sent Gift" :*/
fileprivate let str_resultCurveValue:String = "head"
fileprivate let str_cropVidName:[Character] = ["e","n","t"," ","G","i","f","t"]

/*: "Receive Gift" :*/
fileprivate let str_lineIconData:String = "import return targetReceiv"

/*: "uid" :*/
fileprivate let str_femaleCellData:String = "pathid"

/*: "gift" :*/
fileprivate let str_actionData:[Character] = ["g","i","f","t"]

/*: "name" :*/
fileprivate let str_messageLabContent:String = "NAME"

/*: "num" :*/
fileprivate let str_startRoomAData:String = "NUM"

/*:   :*/
fileprivate let str_normalTitle:String = " "

/*:  x :*/
fileprivate let str_modelTitleValue:String = " xconvert map model"

/*: "imageUri" :*/
fileprivate let str_requestLabName:String = "imaasset"

/*: "imageData" :*/
fileprivate let str_ofMakeTitle:String = "imageDamake true match make date"
fileprivate let str_upObserverName:String = "thidden"

/*: "[Location]" :*/
fileprivate let str_succeedText:String = "name indicator call label data[Loc"

/*: "<img>" :*/
fileprivate let str_popRemoveValue:String = "<img>feed open to page color"

/*: "</img>" :*/
fileprivate let str_putValue:String = "</img>view mode to container var"

/*: "wantGift" :*/
fileprivate let str_requestText:String = "wstreamn"
fileprivate let str_somethingContent:String = "tGiftvar else equal"

/*: "intimatePhoto" :*/
fileprivate let str_requestAddContent:[Character] = ["i","n","t"]
fileprivate let str_backModelName:String = "tap page image to elseimateP"

/*: "audioData" :*/
fileprivate let str_makePartyData:String = "audiuser"
fileprivate let str_aliveLocationData:[Character] = ["D","a","t","a"]

/*: "audioUri" :*/
fileprivate let str_failureText:String = "atodi"

/*: "draftContent" :*/
fileprivate let str_controlUpText:String = "drafdata"
fileprivate let str_clickValue:[Character] = ["C","o","n","t","e","n","t"]

/*: "[draft]%@" :*/
fileprivate let str_resumeValue:String = "[draft]%@bubble catch bag have"

/*: "New" :*/
fileprivate let str_videoBringReturnText:String = "for let item key cornerNew"

/*: "99+" :*/
fileprivate let str_errGiftContent:[Character] = ["9","9","+"]

/*: "Account is restricted！" :*/
fileprivate let str_succeedName:String = "item player textAccou"
fileprivate let str_managerTimeContent:String = "state price text to cell res"
fileprivate let str_allowText:String = "play view name view adted！"

/*: "%@ Online" :*/
fileprivate let str_pushData:[Character] = ["%","@"," ","O","n","l","i","n","e"]

/*: "Messages not been replied here" :*/
fileprivate let str_meetingName:String = "Messagtype data to"
fileprivate let str_cornerBottomName:String = "es nofirst start"
fileprivate let str_locationData:String = "present manager let import usern repl"
fileprivate let str_hiddenFrameData:[Character] = ["i","e","d"," ","h","e","r","e"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FileAllocationTableUserReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let kLet_buttonDeviceValue = (String(str_cancelTitle) + String(str_hiddenData))
//: let RCSystemFormatTypePlain            = "plain"
let kLet_intervalTitle = (String(str_actualValue))

//: class TalkingChatListTableCell: UITableViewCell {
class FileAllocationTableUserReactiveCompatible: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_jumpText.map{lifeStyle(head: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        startWith()
        //: createCellUIConstraints()
        incorporateLikeWearer()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 13)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(str_styleTitleData) + String(str_voiceValue)))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .segment()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        label.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        //: label.textColor = .pullUpColor()
        label.textColor = .pullUpColor()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        label.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        //: label.textColor = .pullUpColor()
        label.textColor = .pullUpColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.markTag(name: "icon_lounge")
        img.image = UIImage.markTag(name: (String(str_titleItemName.suffix(8)) + String(str_managerData)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.markTag(name: "icon_home_v")
        imgV.image = UIImage.markTag(name: (str_voicePastData.replacingOccurrences(of: "after", with: "i") + String(str_makeTitle.suffix(4)) + "me_v"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: EveningShirtFixingTalkingButton = {
        //: let imgV = TalkingButton()
        let imgV = EveningShirtFixingTalkingButton()
        //: imgV.setBackgroundImage(UIImage.markTag(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.markTag(name: (String(str_observerValue.suffix(5)) + "chat" + String(str_directionValue) + String(str_beautyData.prefix(7)))), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.viewState(color: UIColor.imageColor(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(str_giftTitle.prefix(6)) + String(str_subTitle)).localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.moveDismiss(fontSize: 11)
        img.titleLabel?.font = UIFont.moveDismiss(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(str_actualDataCollectText.suffix(5)) + String(str_yourObserveTitle.suffix(2))))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: EveningShirtFixingTalkingButton = {
        //: let btn = TalkingButton.init()
        let btn = EveningShirtFixingTalkingButton()
        //: btn.setImage(UIImage.markTag(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.markTag(name: (String(str_timeTitle.suffix(4)) + "essa" + String(str_observerData))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .biologyLaboratoryEqual(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_socialHalfTitle))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: btn.isHidden = true
        btn.isHidden = true
        //: if LabelToReactiveCompatible.shared.direction == .rightToLeft {
        if LabelToReactiveCompatible.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension FileAllocationTableUserReactiveCompatible {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(str_addPriceValue.prefix(7))))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (str_dismissRangeData.replacingOccurrences(of: "collection", with: "p") + String(str_cellDocumentValue)))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (str_dismissRangeData.replacingOccurrences(of: "collection", with: "p") + String(str_cellDocumentValue)))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (String(str_timeIndexTitle)))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (String(str_timeIndexTitle)))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension FileAllocationTableUserReactiveCompatible {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func managerName(model: DistinctConversationModel?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        soulAction(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        tagModel(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        fruitage(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.go(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(str_addPriceValue.prefix(7))))
            //: } else {
        } else {
            //: contentView.backgroundColor = (RecordAppManager.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (RecordAppManager.share.appStatus == MaculeRelatableTarget.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func sectionIcon(model: DistinctConversationModel) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.timeDraft(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(String(str_freeName))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(str_attachValue) + String(str_barTempValue.prefix(6)))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(String(str_mValue))].stringValue.deleteHtmlTips()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(String(str_normalName) + String(str_equalContent) + String(str_listContent.prefix(5)))].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(String(str_cancelTitle) + String(str_hiddenData))].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if MarkThen.get(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(str_cookieTitle) + String(str_halfTitle.suffix(6))).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.actionUp()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(str_kitData.replacingOccurrences(of: "make", with: "ti"))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(String(str_mValue))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(String(str_skirtText))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(str_compareValue)).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(String(str_mValue))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(String(str_mValue))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(str_noValue)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(str_skipValue.prefix(3)) + String(str_makeLayerName.suffix(6)))].stringValue == (String(str_voiceName) + str_reportManagerEndTitle.replacingOccurrences(of: "lab", with: "m")) { // 音视频通话
            if msgInfo["callCmd"] == "missCall" { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(str_pushRegularColorValue)).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(String(str_kitTitle.prefix(5)) + str_intimateText.replacingOccurrences(of: "close", with: "pe"))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(str_errorMomentLetTitle)).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(str_timeText) + String(str_sexRefreshLabValue.suffix(5)) + " Call]").localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

        } else if msgInfo["video"].exists() || msgInfo["contentType"] == "VideoMsg" { // 短视频
            //: content = "[Short Video]".localized
            content = (String(str_tagText.prefix(6)) + " Video]").localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(String(str_sexValue))].stringValue == "1" || msgInfo[(String(str_sexValue))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(str_editData.prefix(6)) + " mes" + str_viewSizeName.lowercased() + String(str_labPhotoValue)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(str_skipValue.prefix(3)) + String(str_makeLayerName.suffix(6)))].stringValue == (String(str_iconFromName.prefix(4)) + "ess:" + String(str_makeContent.suffix(4)) + "ustom") { // 提示消息
            //: content = json["content"].stringValue
            content = json[(String(str_mValue))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(str_valueRootText))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (str_resultCurveValue.replacingOccurrences(of: "head", with: "S") + String(str_cropVidName)).localized : (String(str_lineIconData.suffix(6)) + "e Gift").localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(str_femaleCellData.replacingOccurrences(of: "path", with: "u"))].stringValue
                //: string1 = (uid == RecordAppManager.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == RecordAppManager.share.loginUserMode.userID) ? (str_resultCurveValue.replacingOccurrences(of: "head", with: "S") + String(str_cropVidName)).localized : (String(str_lineIconData.suffix(6)) + "e Gift").localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(String(str_actionData))][(str_messageLabContent.lowercased())].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(String(str_actionData))][(str_startRoomAData.lowercased())].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(str_requestLabName.replacingOccurrences(of: "asset", with: "g") + "eUri")].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(str_ofMakeTitle.prefix(7)) + str_upObserverName.replacingOccurrences(of: "hidden", with: "a"))].stringValue

            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_noValue)).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_compareValue)).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: }else if messageType == 28 {
        } else if messageType == 28 { // 位置
            //: content = "[Location]".localized
            content = (String(str_succeedText.suffix(4)) + "ation]").localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == kLet_intervalTitle {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(String(str_mValue))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(str_requestLabName.replacingOccurrences(of: "asset", with: "g") + "eUri")].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(String(str_ofMakeTitle.prefix(7)) + str_upObserverName.replacingOccurrences(of: "hidden", with: "a"))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(str_popRemoveValue.prefix(5)))), content.hasSuffix((String(str_putValue.prefix(6)))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(str_popRemoveValue.prefix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(str_putValue.prefix(6))), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_compareValue)).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(str_requestText.replacingOccurrences(of: "stream", with: "a") + String(str_somethingContent.prefix(5)))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(String(str_mValue))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else {
        } else {
            // 图片

            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(str_requestLabName.replacingOccurrences(of: "asset", with: "g") + "eUri")].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(str_ofMakeTitle.prefix(7)) + str_upObserverName.replacingOccurrences(of: "hidden", with: "a"))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(String(str_requestAddContent) + String(str_backModelName.suffix(6)) + "hoto")].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_compareValue)).localized
            }
            // 语音消息
            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_noValue)).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(str_noValue)).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = RecordAppManager.share.loginUserMode.userID
                let mid = RecordAppManager.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(str_makePartyData.replacingOccurrences(of: "user", with: "o") + String(str_aliveLocationData))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(str_makePartyData.replacingOccurrences(of: "user", with: "o") + String(str_aliveLocationData))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(str_failureText.replacingOccurrences(of: "to", with: "u") + "oUri")].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(str_failureText.replacingOccurrences(of: "to", with: "u") + "oUri")].stringValue
                    }
                }
                //: let unreadColor = UIColor.pullUpColor()
                let unreadColor = UIColor.pullUpColor()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.pullUpColor()
                let unreadColor = UIColor.pullUpColor()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func timeDraft(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((str_controlUpText.replacingOccurrences(of: "data", with: "t") + String(str_clickValue))) {
            //: let dict = NSDictionary.init(jsonString: draft)
            let dict = NSDictionary(jsonString: draft)
            //: draft = "\(String(describing: dict["draftContent"]))"
            draft = "\(String(describing: dict[(str_controlUpText.replacingOccurrences(of: "data", with: "t") + String(str_clickValue))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(str_resumeValue.prefix(9))).localizedArguments(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.imageColor(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension FileAllocationTableUserReactiveCompatible {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: RowModelType?, model: TalkingConversationModel, isOther: Bool) {
    private func soulAction(userInfo: RowModelType?, model: DistinctConversationModel, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == AreaReactiveCompatible.getXiaoMiID() || userInfo?.uid == AreaReactiveCompatible.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == AreaReactiveCompatible.sink() || userInfo?.uid == AreaReactiveCompatible.inheritPlayer() {
            //: nickNameLabel.textColor = .appTitleColor()
            nickNameLabel.textColor = .segment()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.markTag(name: model.faceUrl)
                headImgView.image = UIImage.markTag(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.laughDoing(urlStr: model.faceUrl, placeImg: UIImage.overSize())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.appTitleColor()
        nickNameLabel.textColor = userInfo.loungePlus ? .fromNonsolidColor() : .segment()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.laughDoing(urlStr: userInfo.headPic, placeImg: UIImage.requestSex(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.glasses(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
//        imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
//        imtimateBtn.isHidden = model.totalIntimate <= 0
//        if model.totalIntimate > 0 {
//            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .biologyLaboratoryEqual(type: .Medium, fontSize: 12)], context: nil)
//            imtimateBtn.snp.updateConstraints { make in
//                make.width.equalTo((size?.width ?? 50) + 20)
//            }
//        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(str_videoBringReturnText.suffix(3))).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.cutis(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.cutis(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func tagModel(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func fruitage(model: DistinctConversationModel) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(str_succeedName.suffix(5)) + "nt is" + String(str_managerTimeContent.suffix(4)) + "tric" + String(str_allowText.suffix(4))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.pullUpColor()
                NSAttributedString.Key.foregroundColor: UIColor.pullUpColor(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = FileAllocationTableUserReactiveCompatible.sectionIcon(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(str_pushData)).localizedArguments(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.pullUpColor()
                NSAttributedString.Key.foregroundColor: UIColor.pullUpColor(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(str_meetingName.prefix(6)) + String(str_cornerBottomName.prefix(5)) + "t bee" + String(str_locationData.suffix(6)) + String(str_hiddenFrameData)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.pullUpColor()
                NSAttributedString.Key.foregroundColor: UIColor.pullUpColor(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = FileAllocationTableUserReactiveCompatible.timeDraft(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension FileAllocationTableUserReactiveCompatible {
    //: private func createCellUI() {
    private func startWith() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func incorporateLikeWearer() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LabelToReactiveCompatible.shared.direction == .rightToLeft {
            if LabelToReactiveCompatible.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
