
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let str_playerValue:[UInt8] = [0xc0,0xd1,0xd1,0xcd,0xc4,0xde,0xc8,0xc0,0xd1,0xde,0xd1,0xc0,0xd8,0xde,0xd2,0xd4,0xc2,0xc2,0xc4,0xc4,0xc5,0xde,0xcf,0xce,0xd5,0xc8,0xc7,0xc8,0xc2,0xc0,0xd5,0xc8,0xce,0xcf]

private func standIn(background num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let str_touchTitle:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x44,0x45,0x45,0x43,0x43,0x55,0x53,0x5f,0x45,0x42,0x49,0x52,0x43,0x53,0x42,0x55,0x53,0x5f,0x50,0x41,0x49,0x5f,0x45,0x4c,0x50,0x50,0x41]

/*: "mf/recharge/createApplePay" :*/
fileprivate let str_coinItemPlayValue:String = "writer returnmf/re"
fileprivate let str_buttonPathContent:[Character] = ["e","/","c","r","e","a","t","e","A","p","p","l","e","P","a","y"]

/*: "productId" :*/
fileprivate let str_normalValue:String = "prmanagerdu"

/*: "source" :*/
fileprivate let str_kindToText:[Character] = ["s","o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let str_yourselfLimitPathData:String = "IAP充\u{503c}\u{521b}"
fileprivate let str_centerData:String = ":"

/*: , 参数: :*/
fileprivate let str_privacyText:[Character] = [","," ","\u{53c2}","\u{6570}",":"]

/*: ." :*/
fileprivate let str_addAtIfValue:String = "interval"

/*: "orderNum" :*/
fileprivate let str_viewBeginName:[Character] = ["o","r"]
fileprivate let str_sexTitle:String = "i table or tablederNum"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let str_pushData:String = "mf/reclabel create user view voice"
fileprivate let str_modePageValue:String = "e/appimage adjust room error if"
fileprivate let str_componentValue:String = "app forlePay"

/*: "IAP充值校验失败: :*/
fileprivate let str_pathManagerName:String = "path viewIAP充"
fileprivate let str_actionContent:[Character] = [":"]

/*: "transactionId" :*/
fileprivate let str_topValue:[Character] = ["t","r","a","n","s","a","c","t"]
fileprivate let str_closedGiftText:String = "model language add let levelionId"

/*: "reportMoney" :*/
fileprivate let str_mixTitle:String = "in i stringreportMo"
fileprivate let str_bubbleTitle:String = "neerror"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let str_withVoiceData:[Character] = ["m","f","/","A","u","t","o","S","u","b","/","A","p","p","l","e","C","r","e","a","t"]
fileprivate let str_occupationTitle:[Character] = ["e","O","r","d","e","r"]

/*: "IAP订阅创建订单失败: :*/
fileprivate let str_titleTimeData:[Character] = ["I","A","P","订","\u{9605}","创"]
fileprivate let str_rowClearText:[Character] = ["建","\u{8ba2}","单","失","\u{8d25}",":"]

/*: "orderId" :*/
fileprivate let str_actualName:[UInt8] = [0x61,0x7c,0x6a,0x6b,0x7c,0x47,0x6a]

private func modelEqual(extra num: UInt8) -> UInt8 {
    return num ^ 14
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let str_dataButtonTitle:[Character] = ["m","f","/","A","u","t","o","S","u","b"]
fileprivate let str_natureText:String = "next occupation call error/Apple"
fileprivate let str_tapValue:[Character] = ["P","a","y","S","u","c","c","e","s","s"]

/*: "IAP订阅校验失败: :*/
fileprivate let str_paraData:String = "IAP\u{8ba2}阅"

/*: "App" :*/
fileprivate let str_managerData:[Character] = ["A","p","p"]

/*: "OrderTransactionInfo_ :*/
fileprivate let str_columnValue:[Character] = ["O","r","d","e","r","T","r","a","n","s","a"]
fileprivate let str_applicationTitle:String = "cbuttonio"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let str_showCleanValue:[UInt8] = [0xb,0x36,0x20,0x21,0x36,0x10,0x36,0x25,0x2a,0x37,0x25,0x27,0x30,0x2d,0x2b,0x2a,0xd,0x2a,0x22,0x2b,0x1b,0x17,0x31,0x26,0x37,0x27,0x36,0x2d,0x26,0x21,0x1b]

private func taskSize(index num: UInt8) -> UInt8 {
    return num ^ 68
}

/*: "verifyData" :*/
fileprivate let str_mValue:[UInt8] = [0xef,0xfc,0xeb,0xf0,0xff,0xe0,0xdd,0xf8,0xed,0xf8]

private func temporaryWorker(manager num: UInt8) -> UInt8 {
    return num ^ 153
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let str_laboratoryName:String = "left self showIAP查询"
fileprivate let str_filterData:String = "\u{5185}购商品失败"
fileprivate let str_mapValue:String = "fatal let background, pr"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let str_tagData:[Character] = ["I","A","P","查","询"]
fileprivate let str_noTitleText:[Character] = ["内","购","商","品","失","败",","," ","p","r","o"]
fileprivate let str_moreValue:[Character] = ["d","u","c","t","s",".","c","o","u","n","t","为","0","."]

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let str_sizeModelName:String = "IAP\u{67e5}"
fileprivate let str_bankData:String = "\u{54c1}失败, "

/*: "IAP-purchased: :*/
fileprivate let str_dismissGestureViewData:String = "equal var varIAP-p"
fileprivate let str_labelName:[Character] = ["s","e","d",":"]

/*: , 订单号: :*/
fileprivate let str_selectData:String = ", 订单号:"

/*: , 商品Id: :*/
fileprivate let str_greetValue:String = ", 商品Idtrue error guard area"
fileprivate let str_centerContentValue:[Character] = [":"]

/*: , 苹果transId: :*/
fileprivate let str_layerTitle:[Character] = [","," ","苹","果","t","r","a","n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let str_reSucceedValue:String = "finish heightIAP-f"
fileprivate let str_modelToTitle:String = "ailed:by add"

/*: "IAP-deferred: :*/
fileprivate let str_controlContent:String = "IAP-count if task class return"
fileprivate let str_statusPhotoName:[Character] = ["d","e","f","e","r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let str_recordLineValue:String = "iap"
fileprivate let str_kitContent:[Character] = ["\u{672a}","知","交","易","类","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let str_byDismissData:String = "\u{f8ff} \u{672a}"
fileprivate let str_sectionName:[Character] = ["知","的","交","易","类","型"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let str_leadingData:[Character] = ["I","A","P","获","\u{53d6}","本","地","\u{6536}","据","数","据","\u{5931}","\u{8d25}",","," ","t","r","a","n","s","a","c","t","i","o","n","I"]
fileprivate let str_viewItemText:String = "d:fill share close case net"

/*: , orderId: :*/
fileprivate let str_valueNameTitle:String = ", orinput message lab"
fileprivate let str_makeValue:String = ":"

/*: , payType: :*/
fileprivate let str_shareData:String = "position my instance voice, pay"
fileprivate let str_shadowDirectionName:String = "Type:original self"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ColorRequestDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import UIKit
import UIKit

//: import StoreKit
import StoreKit

//: import SwiftyJSON
import SwiftyJSON

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kLet_barUseData = NSNotification.Name(rawValue: String(bytes: str_playerValue.map{standIn(background: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let kLet_soundUserName = NSNotification.Name(rawValue: String(bytes: str_touchTitle.reversed(), encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kLet_statusContent = 9

/// 支付类型
//: enum ApplePayType {
enum PutUpType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum MeTransformable: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double) -> Void
typealias IAPcompletionHandle = (MeTransformable, Double) -> Void

//: class AppleIAPManager: NSObject {
class ColorRequestDelegate: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: PutUpType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = ColorRequestDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(miniColor),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func miniColor() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension ColorRequestDelegate {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func detailDraw(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(str_coinItemPlayValue.suffix(5)) + "charg" + String(str_buttonPathContent))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(str_normalValue.replacingOccurrences(of: "manager", with: "o") + "ctId")] = productId
        //: dict["source"] = source
        dict[(String(str_kindToText))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: ShowLogTool.cellText(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                ShowLogTool.cellText(msg: (str_yourselfLimitPathData + "建订单\u{5931}\u{8d25}" + str_centerData.capitalized) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(str_viewBeginName) + String(str_sexTitle.suffix(6)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func tithe(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(str_pushData.prefix(6)) + "harg" + String(str_modePageValue.prefix(5)) + String(str_componentValue.suffix(5)) + "Notify")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: ShowLogTool.cellText(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    ShowLogTool.cellText(msg: (String(str_pathManagerName.suffix(4)) + "值校验失败" + String(str_actionContent)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.cocktailLounge(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.stall()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_barUseData, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(str_mixTitle.suffix(8)) + str_bubbleTitle.replacingOccurrences(of: "error", with: "y"))].doubleValue)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension ColorRequestDelegate {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func vertical(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(str_withVoiceData) + String(str_occupationTitle))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(str_normalValue.replacingOccurrences(of: "manager", with: "o") + "ctId")] = productId
        //: dict["source"] = source
        dict[(String(str_kindToText))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: ShowLogTool.cellText(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                ShowLogTool.cellText(msg: (String(str_titleTimeData) + String(str_rowClearText)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: str_actualName.map{modelEqual(extra: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func atNumberervalTitle(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(str_dataButtonTitle) + String(str_natureText.suffix(6)) + String(str_tapValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: ShowLogTool.cellText(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    ShowLogTool.cellText(msg: (str_paraData + "校验\u{5931}败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.cocktailLounge(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.subscribeSize()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_soundUserName, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(str_mixTitle.suffix(8)) + str_bubbleTitle.replacingOccurrences(of: "error", with: "y"))].doubleValue)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension ColorRequestDelegate {
    /// 初始化数据
    //: private func iap_initData() {
    private func schoolbagAppear() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = everyTarget(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = everyTarget(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func everyTarget(payType: PutUpType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = stall()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = subscribeSize()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func stall() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_managerData)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_backgroundText.string(forKey: kLet_serverTextName) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(str_columnValue) + str_applicationTitle.replacingOccurrences(of: "button", with: "t") + "nInfo_") + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func subscribeSize() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_managerData)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_backgroundText.string(forKey: kLet_serverTextName) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: str_showCleanValue.map{taskSize(index: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func boutEnable(_ transactionId: String, _ payType: PutUpType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: str_mValue.map{temporaryWorker(manager: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: str_mValue.map{temporaryWorker(manager: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension ColorRequestDelegate {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func transactions() {
        //: iap_initData()
        schoolbagAppear()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            failed(dict[(String(str_topValue) + String(str_closedGiftText.suffix(5)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            failed(dict[(String(str_topValue) + String(str_closedGiftText.suffix(5)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func failed(_ transactionId: String?, _ payType: PutUpType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        cocktailLounge(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension ColorRequestDelegate {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func schmoose(productId: String, payType: PutUpType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        schoolbagAppear()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            detailDraw(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.remark(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            vertical(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.remark(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func remark(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: ShowLogTool.cellText(msg: "IAP查询内购商品失败, productId:\(productId).")
            ShowLogTool.cellText(msg: (String(str_laboratoryName.suffix(5)) + str_filterData + String(str_mapValue.suffix(4)) + "oductId:") + "\(productId).")
            //: self.completionHandle?(.notArrow, 0)
            self.completionHandle?(.notArrow, 0)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.anInsert()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func anInsert() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension ColorRequestDelegate: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: ShowLogTool.cellText(msg: "IAP查询内购商品失败, products.count为0.")
            ShowLogTool.cellText(msg: (String(str_tagData) + String(str_noTitleText) + String(str_moreValue)))
            //: self.completionHandle?( .noProductId, 0)
            self.completionHandle?(.noProductId, 0)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: ShowLogTool.cellText(msg: "IAP查询内购商品失败, Error:\(error).")
        ShowLogTool.cellText(msg: (str_sizeModelName + "询\u{5185}购\u{5546}" + str_bankData + "Error:") + "\(error).")
        //: self.completionHandle?( .noProductId, 0)
        self.completionHandle?(.noProductId, 0)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension ColorRequestDelegate: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0)
                    self.completionHandle?(.renewSucceed, 0)
                    //: } else {
                } else { // 普通购买和订阅
                    //: ShowLogTool.cellText(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    ShowLogTool.cellText(msg: (String(str_dismissGestureViewData.suffix(5)) + "urcha" + String(str_labelName)) + "\(currentPayType)" + (str_selectData.capitalized) + "\(String(describing: createOrderId))" + (String(str_greetValue.prefix(6)) + String(str_centerContentValue)) + "\(transaction.payment.productIdentifier)" + (String(str_layerTitle)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    cocktailLounge(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: ShowLogTool.cellText(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ShowLogTool.cellText(msg: (String(str_reSucceedValue.suffix(5)) + String(str_modelToTitle.prefix(6))) + "\(currentPayType)" + (str_selectData.capitalized) + "\(String(describing: createOrderId))" + (String(str_greetValue.prefix(6)) + String(str_centerContentValue)) + "\(transaction.payment.productIdentifier)" + (String(str_layerTitle)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0)
                self.completionHandle?(.failed, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0)
                self.completionHandle?(.restored, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: ShowLogTool.cellText(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ShowLogTool.cellText(msg: (String(str_controlContent.prefix(4)) + String(str_statusPhotoName)) + "\(currentPayType)" + (str_selectData.capitalized) + "\(String(describing: createOrderId))" + (String(str_greetValue.prefix(6)) + String(str_centerContentValue)) + "\(transaction.payment.productIdentifier)" + (String(str_layerTitle)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0)
                self.completionHandle?(.deferred, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0)
                self.completionHandle?(.unknow, 0)
                //: createOrderId = nil
                createOrderId = nil
                //: ShowLogTool.cellText(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ShowLogTool.cellText(msg: (str_recordLineValue.uppercased() + String(str_kitContent)) + "\(currentPayType)" + (str_selectData.capitalized) + "\(String(describing: createOrderId))" + (String(str_greetValue.prefix(6)) + String(str_centerContentValue)) + "\(transaction.payment.productIdentifier)" + (String(str_layerTitle)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((str_byDismissData + String(str_sectionName)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func cocktailLounge(_ transactionId: String, _ payType: PutUpType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = boutEnable(transactionId, payType) else {
            //: ShowLogTool.cellText(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            ShowLogTool.cellText(msg: (String(str_leadingData) + String(str_viewItemText.prefix(2))) + "\(transactionId)" + (String(str_valueNameTitle.prefix(4)) + "derId" + str_makeValue.capitalized) + "\(String(describing: createOrderId))" + (String(str_shareData.suffix(5)) + String(str_shadowDirectionName.prefix(5))) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据

                if self.payCacheList.filter({ $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId || $0[String(bytes: str_actualName.map{modelEqual(extra: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(str_topValue) + String(str_closedGiftText.suffix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_actualName.map{modelEqual(extra: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_mValue.map{temporaryWorker(manager: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.stall()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据

                if self.subscribeCacheList.filter({ $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId || $0[String(bytes: str_actualName.map{modelEqual(extra: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(str_topValue) + String(str_closedGiftText.suffix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_actualName.map{modelEqual(extra: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_mValue.map{temporaryWorker(manager: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.subscribeSize()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kLet_statusContent {
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            tithe(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(String(str_topValue) + String(str_closedGiftText.suffix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            atNumberervalTitle(transactionId, params: paramsArr.first!)
        }
    }
}
