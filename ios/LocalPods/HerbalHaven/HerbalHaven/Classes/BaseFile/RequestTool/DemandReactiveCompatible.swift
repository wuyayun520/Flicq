
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_phoneName:[Character] = ["h","t","t","p",":"]
fileprivate let str_userViewValue:[Character] = ["/","/"]

/*: "https://" :*/
fileprivate let str_locationText:[Character] = ["h"]
fileprivate let str_atToolValue:String = "ttps://var top view for player"

/*: "http:// :*/
fileprivate let str_errorTargetValue:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let str_managerData:String = "seviewe"
fileprivate let str_priceName:[Character] = ["t"]
fileprivate let str_colorMagnitudeTitle:String = "Index = title model view show"

/*: "serverUrlStr =  :*/
fileprivate let str_leadingTitle:String = "cellerv"
fileprivate let str_tagName:String = "voice text raw typer = "

/*: "app. :*/
fileprivate let str_shareLaunchTitle:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let str_makeText:String = ".comsuccess touch"

/*: "IM/getMyBlackList" :*/
fileprivate let str_observerTitle:String = "IM/geto model leading new more"
fileprivate let str_actualValue:[Character] = ["c"]
fileprivate let str_contentDataName:[Character] = ["k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let str_filePointTitle:String = "photo var namemf/b"
fileprivate let str_normalData:String = "self make model itemess/"

/*: "mf/business/sendGift" :*/
fileprivate let str_lastValue:String = "mf/busreturn again kind make"
fileprivate let str_timeItemValue:String = "sendGiftview view hour"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_valueMoreName:String = "model view titlemf/ch"
fileprivate let str_redTitle:String = "atRoomfalse indicator right"
fileprivate let str_norTitle:String = "dMsgstring touch input price aspect"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_responseName:String = "mf/cbiology view self cell tap"
fileprivate let str_dismissValue:String = "guard color/sen"

/*: "recharge/applePayNotify" :*/
fileprivate let str_explainName:[Character] = ["r"]
fileprivate let str_noText:[Character] = ["e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let str_beanData:String = "uarrayr"
fileprivate let str_actionScaleData:[Character] = ["/","a","p","p","l","e","L","o","g","i","n"]

/*: "en" :*/
fileprivate let str_topText:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let str_errEqualContent:String = "micr"

/*: "es" :*/
fileprivate let str_duringContent:String = "eto"

/*: "pt" :*/
fileprivate let str_readyName:String = "PT"

/*: "GJ21FB" :*/
fileprivate let str_tipFileData:String = "nameJ21FB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_kitValue:[Character] = ["完","成","了","\u{68c0}","测",",","请","求","状","态","正"]
fileprivate let str_sexName:[Character] = ["常"]

/*: "接口请求失败" :*/
fileprivate let str_makeData:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_imageWithoutName:String = "完成time测,"
fileprivate let str_signName:String = "scale change tover url"

/*: "HTTPProxy" :*/
fileprivate let str_thirdValue:String = "HTTPProxtag re"
fileprivate let str_giftTitle:[Character] = ["y"]

/*: "__SCOPED__" :*/
fileprivate let str_countColorTitle:String = "__SCdata to cut"

/*: "tap" :*/
fileprivate let str_flowMethodText:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let str_callImageData:String = "tuactivity"

/*: "ipsec" :*/
fileprivate let str_blockData:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let str_meImageText:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_netValue:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let str_mediumInputData:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_visibleName:[UInt8] = [0x5d,0x4e,0x55,0x58,0x64,0x64,0x5f,0x5e,0x57,0x5e,0x4f,0x5e,0x68,0x5a,0x56,0x5e,0x78,0x54,0x54,0x50,0x52,0x5e,0x48,0x6c,0x52,0x4f,0x53,0x6e,0x49,0x57,0x1b,0x6,0x1b]

private func imageQuickApp(biology num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "UID" :*/
fileprivate let str_deviceValue:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let str_groupData:String = "phpse"
fileprivate let str_styleFatalTitle:[Character] = ["S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let str_pictureTitle:String = "gift self version numberfunc__"
fileprivate let str_keySubForTitle:String = "lCookpremium text"
fileprivate let str_tempTitle:String = "let result self starties = "

/*: "func__saveAllCookies  error" :*/
fileprivate let str_imageLiveName:String = "view blockfunc_"
fileprivate let str_borderBottomName:String = "make request center color upeAllC"
fileprivate let str_colorLayerData:String = "  errsuper let view view"
fileprivate let str_mainData:String = "OR"

/*: "🍪 删除 :*/
fileprivate let str_clickContent:String = "\u{0001f36a} \u{5220}\u{9664}"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DemandReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: enum LangType: String {
enum PropagandaNameProtocol: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class DemandReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = DemandReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        dataConverterSpeaker()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_phoneName) + String(str_userViewValue))) || newValue.hasPrefix((String(str_locationText) + String(str_atToolValue.prefix(7)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_marginRoomData && !kLet_succeedSuccessName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_errorTargetValue)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_errorTargetValue)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    nameModifyCookiesFuncTalking(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func dataConverterSpeaker() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_backgroundText.integer(forKey: kLet_halfCloseHeightTitle)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (str_managerData.replacingOccurrences(of: "view", with: "rv") + "rSelc" + String(str_priceName) + String(str_colorMagnitudeTitle.prefix(8))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = headquarter()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_phoneName) + String(str_userViewValue))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_marginRoomData && !kLet_succeedSuccessName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_phoneName) + String(str_userViewValue))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (str_leadingTitle.replacingOccurrences(of: "cell", with: "s") + "erUrlSt" + String(str_tagName.suffix(4))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func headquarter() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func contextOfUse(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_shareLaunchTitle)) + "\(kLet_keyViewName)" + (String(str_makeText.prefix(4))), (String(str_shareLaunchTitle)) + "\(kLet_keyViewName)" + (String(str_makeText.prefix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = AreaReactiveCompatible.getSystemLangCode()
        let lang = AreaReactiveCompatible.shareSize()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .randomString(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (str_tipFileData.replacingOccurrences(of: "name", with: "G")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension DemandReactiveCompatible {
    //: func checkServerUrl() {
    func showRelease() {
        //: if RecordAppManager.share.networkStatus != .Unavailable {
        if RecordAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            addHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            micTime()
        }
    }

    //: func func_starCodeTime() {
    func micTime() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.addHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func compareTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func addHost() {
        //: if RecordAppManager.share.networkStatus == .Unavailable {
        if RecordAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        compareTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        SubjectMatterThen.ignore { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(str_kitValue) + String(str_sexName)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (str_makeData.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.micTime()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (str_imageWithoutName.replacingOccurrences(of: "time", with: "了检") + "需要更换ser" + String(str_signName.suffix(7))))
                    //: self.func__changeHostUrl()
                    self.m()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func m() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        compareTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_backgroundText.set(serverSelctIndex, forKey: kLet_halfCloseHeightTitle)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_phoneName) + String(str_userViewValue))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_marginRoomData, !kLet_succeedSuccessName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_phoneName) + String(str_userViewValue))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension DemandReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func charmData() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(str_thirdValue.prefix(8)) + String(str_giftTitle))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func televisionVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_countColorTitle.prefix(4)) + "OPED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(str_flowMethodText))) || keyStr.contains((str_callImageData.replacingOccurrences(of: "activity", with: "n"))) || keyStr.contains((String(str_blockData))) || keyStr.contains((String(str_meImageText))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension DemandReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func nameModifyCookiesFuncTalking(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(str_netValue)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = listWithoutRestaurantAttendant()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_phoneName) + String(str_userViewValue)), with: (String(str_locationText) + String(str_atToolValue.prefix(7))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_locationText) + String(str_atToolValue.prefix(7)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(str_mediumInputData)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            giftHorse(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        searchedCookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func giftHorse(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_visibleName.map{imageQuickApp(biology: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func listWithoutRestaurantAttendant() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_phoneName) + String(str_userViewValue))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_marginRoomData, !kLet_succeedSuccessName {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_locationText) + String(str_atToolValue.prefix(7)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_shareLaunchTitle)) + "\(kLet_keyViewName)" + (String(str_makeText.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func makeNote(note _: Notification) {
        //: if RecordAppManager.share.loginUid.isEmptyString {
        if RecordAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            equalAddVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            addVersion()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func equalAddVersion() {
        //: var htmlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_deviceValue)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_deviceValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_groupData.uppercased() + String(str_styleFatalTitle)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_groupData.uppercased() + String(str_styleFatalTitle))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_deviceValue)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(str_deviceValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_groupData.uppercased() + String(str_styleFatalTitle)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(str_groupData.uppercased() + String(str_styleFatalTitle))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(str_deviceValue))), h5Cookie.keys.contains((String(str_deviceValue))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(str_deviceValue))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(str_deviceValue))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((str_groupData.uppercased() + String(str_styleFatalTitle))), h5Cookie.keys.contains((str_groupData.uppercased() + String(str_styleFatalTitle))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(str_groupData.uppercased() + String(str_styleFatalTitle))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(str_groupData.uppercased() + String(str_styleFatalTitle))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func addVersion() {
        //: let htmlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = RecordAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == RecordAppManager.share.loginUserMode.userID {
            if cookie.name == (String(str_deviceValue)), cookie.value == RecordAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (str_groupData.uppercased() + String(str_styleFatalTitle)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_deviceValue)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_deviceValue))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_groupData.uppercased() + String(str_styleFatalTitle)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_groupData.uppercased() + String(str_styleFatalTitle))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(str_deviceValue))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(str_deviceValue))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((str_groupData.uppercased() + String(str_styleFatalTitle))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_groupData.uppercased() + String(str_styleFatalTitle))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func searchedCookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(str_pictureTitle.suffix(6)) + "saveAl" + String(str_keySubForTitle.prefix(5)) + String(str_tempTitle.suffix(6))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_backgroundText.set(cookieData, forKey: kLet_priceText)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_imageLiveName.suffix(5)) + "_sav" + String(str_borderBottomName.suffix(5)) + "ookies" + String(str_colorLayerData.prefix(5)) + str_mainData.lowercased()))
        }
    }

    //: func func__setAppCookies() {
    func cookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_backgroundText.object(forKey: kLet_priceText)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func clickWithinCookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_backgroundText.removeObject(forKey: kLet_priceText)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
