
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let str_makeListContent:String = "usinteractionr"
fileprivate let str_clickText:String = "ateInfoitem super"

/*: "gallery/getGalleryByUid" :*/
fileprivate let str_requestText:String = "GALLE"
fileprivate let str_quoteNameValue:String = "ry/geany model equal if device"
fileprivate let str_textPathValue:String = "to type to lettGalle"

/*: "gallery/upload" :*/
fileprivate let str_rankContent:String = "ggroupl"
fileprivate let str_reasonTitle:String = "pltemp"
fileprivate let str_dropData:String = "image"

/*: "gallery/deletePic" :*/
fileprivate let str_laughTitle:[Character] = ["g","a","l","l","e","r","y","/","d","e","l","e","t","e","P"]
fileprivate let str_tabData:[Character] = ["i","c"]

/*: "user/tagDelete" :*/
fileprivate let str_directionName:[Character] = ["u","s","e","r","/","t","a","g","D","e","l","e","t","e"]

/*: "mf/user/tagEdit" :*/
fileprivate let str_moveValue:String = "equal equal tomf/use"
fileprivate let str_markAnalyzeData:String = "r/taequal open pin gift"
fileprivate let str_toLiveValue:[Character] = ["g","E","d","i","t"]

/*: "app/feedback" :*/
fileprivate let str_centerData:String = "frame record white model requestapp/fe"
fileprivate let str_indicatorData:String = "edbmk"

/*: "IM/getMyBlackList" :*/
fileprivate let str_appContent:String = "frame button with to makeIM/ge"
fileprivate let str_blockValue:String = "send leadingckList"

/*: "mf/user/getSetting" :*/
fileprivate let str_viewInputBeautyValue:[Character] = ["m","f","/","u","s","e","r","/","g"]
fileprivate let str_eventEqualValue:String = "rank model let picetSetting"

/*: "mf/user/setConfig" :*/
fileprivate let str_tabText:[Character] = ["m","f","/","u","s","e","r","/","s","e","t","C","o","n","f"]
fileprivate let str_allLinkIfValue:String = "IG"

/*: "mf/user/settingStatus" :*/
fileprivate let str_makeContentText:[Character] = ["m","f","/","u","s","e","r","/","s","e"]
fileprivate let str_femaleName:[Character] = ["t","t","i","n","g","S","t","a","t","u","s"]

/*: "type" :*/
fileprivate let str_accountLeadingTaskValue:[UInt8] = [0xb0,0xbd,0xb4,0xa1]

/*: "user/lockAccount" :*/
fileprivate let str_joinImageData:String = "cell return withuser/"
fileprivate let str_refData:[Character] = ["u","n","t"]

/*: "mf/user/editInfo" :*/
fileprivate let str_maxModelName:String = "mf/usrequest effect return"
fileprivate let str_cellPathValue:String = "itInfstring text stop var view"
fileprivate let str_releaseTopTitle:String = "row"

/*: "mf/userSign/list" :*/
fileprivate let str_popEntryTitle:String = "if instance indexmf/"
fileprivate let str_appMakeData:[Character] = ["u","s","e","r","S","i","g","n","/","l","i","s","t"]

/*: "mf/userSign/signIn" :*/
fileprivate let str_femaleSizeName:String = "mf/usertype index"
fileprivate let str_playAppName:String = "down viewSi"
fileprivate let str_storageItemValue:String = "model at height requestgn/si"

/*: "mf/dating/saveVideo" :*/
fileprivate let str_biologyTitle:String = "me stylemf/d"
fileprivate let str_errBackgroundText:String = "to user sub talksaveVideo"

/*: "mf/dating/getConfig" :*/
fileprivate let str_biologyName:[Character] = ["m","f","/","d","a","t","i","n","g","/","g"]
fileprivate let str_greenName:[Character] = ["e","t","C"]
fileprivate let str_segmentTitle:String = "ONFIG"

/*: "mf/dating/deleteVideo" :*/
fileprivate let str_playerName:[Character] = ["m","f","/","d","a","t","i","n","g","/","d","e","l","e"]
fileprivate let str_millModelValue:String = "cell"
fileprivate let str_arrowData:String = "Videoicon label reply import self"

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let str_makeData:String = "mf/grbackground size"
fileprivate let str_modeViewData:String = "uagea"
fileprivate let str_leadingName:String = "error text model response sampledioRe"

/*: "mf/greet/list" :*/
fileprivate let str_streamName:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let str_equalHandleData:[Character] = ["/","l","i","s","t"]

/*: "mf/greet/add" :*/
fileprivate let str_listPanTitle:String = "text sizemf/gre"

/*: "mf/greet/del" :*/
fileprivate let str_actionTitle:String = "mf/greeshare s table user var"
fileprivate let str_blockData:String = "height response text imaget/del"

/*: "mf/greet/getSettings" :*/
fileprivate let str_toPanOfText:String = "as image false datamf/greet/"
fileprivate let str_layerData:String = "getSdata bottom on to equal"

/*: "mf/greet/extraSetting" :*/
fileprivate let str_voiceManagerData:[Character] = ["m","f","/","g","r","e","e","t","/","e","x","t","r","a","S","e"]
fileprivate let str_labelAcceptTitle:[Character] = ["t","t","i","n","g"]

/*: "game/list" :*/
fileprivate let str_dismissStatusTitle:[Character] = ["g","a"]
fileprivate let str_topContent:String = "show let color requestme/list"

/*: "category" :*/
fileprivate let str_appearContent:[UInt8] = [0x75,0x77,0x62,0x73,0x71,0x79,0x64,0x6f]

private func valueUser(location num: UInt8) -> UInt8 {
    return num ^ 22
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FocusThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class FocusThen: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func mentalRepresentation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (str_makeListContent.replacingOccurrences(of: "interaction", with: "e") + "/upd" + String(str_clickText.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func diligent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (str_requestText.lowercased() + String(str_quoteNameValue.prefix(5)) + String(str_textPathValue.suffix(6)) + "ryByUid")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func record(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (str_rankContent.replacingOccurrences(of: "group", with: "al") + "ery/u" + str_reasonTitle.replacingOccurrences(of: "temp", with: "oa") + str_dropData.replacingOccurrences(of: "image", with: "d"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func viewModel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(str_laughTitle) + String(str_tabData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func ofPlayer(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(str_directionName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func progress(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(str_moveValue.suffix(6)) + String(str_markAnalyzeData.prefix(4)) + String(str_toLiveValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func wrap(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(str_centerData.suffix(6)) + str_indicatorData.replacingOccurrences(of: "m", with: "ac"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func subAt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(str_appContent.suffix(5)) + "tMyBla" + String(str_blockValue.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func groupAction(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(str_viewInputBeautyValue) + String(str_eventEqualValue.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func week(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(str_tabText) + str_allLinkIfValue.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func lightMessage(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(str_makeContentText) + String(str_femaleName))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: str_accountLeadingTaskValue.map{$0^196}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func ditheredColor(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(str_joinImageData.suffix(5)) + "lockAcco" + String(str_refData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sessionOf(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(str_maxModelName.prefix(5)) + "er/ed" + String(str_cellPathValue.prefix(5)) + str_releaseTopTitle.replacingOccurrences(of: "row", with: "o"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func clue(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(str_popEntryTitle.suffix(3)) + String(str_appMakeData))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func tokenDown(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(str_femaleSizeName.prefix(7)) + String(str_playAppName.suffix(2)) + String(str_storageItemValue.suffix(5)) + "gnIn")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func chromaticColour(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(str_biologyTitle.suffix(4)) + "ating/" + String(str_errBackgroundText.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func pastLocal(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(str_biologyName) + String(str_greenName) + str_segmentTitle.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func subView(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(str_playerName) + str_millModelValue.replacingOccurrences(of: "cell", with: "te") + String(str_arrowData.prefix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func coordinatePhoto(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(str_makeData.prefix(5)) + "eet/" + str_modeViewData.replacingOccurrences(of: "age", with: "pd") + "teAu" + String(str_leadingName.suffix(5)) + "mark")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func extract(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(str_streamName) + String(str_equalHandleData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func indexAction(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(str_listPanTitle.suffix(6)) + "et/add")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modelRow(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(str_makeData.prefix(5)) + "eet/" + str_modeViewData.replacingOccurrences(of: "age", with: "pd") + "teAu" + String(str_leadingName.suffix(5)) + "mark")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func level(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(str_actionTitle.prefix(7)) + String(str_blockData.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func upCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(str_toPanOfText.suffix(9)) + String(str_layerData.prefix(4)) + "ettings")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func memoryImage(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(str_voiceManagerData) + String(str_labelAcceptTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func mediumPlay(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(str_dismissStatusTitle) + String(str_topContent.suffix(7)))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: str_appearContent.map{valueUser(location: $0)}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
