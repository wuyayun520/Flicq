
//: Declare String Begin

/*: "nil" :*/
fileprivate let str_equalPageName:String = "NIL"

/*: "Edit profiles" :*/
fileprivate let str_loadValue:String = "end"
fileprivate let str_reportValue:String = "view vardit "

/*: "Done" :*/
fileprivate let str_netTitle:String = "Doneequal title back in user"

/*: "#999999" :*/
fileprivate let str_appEqualName:[Character] = ["#","9","9","9","9","9","9"]

/*: "uid" :*/
fileprivate let str_tempValue:String = "uimodel"

/*: "UnseeableViewCell" :*/
fileprivate let str_succeedValue:[Character] = ["T","a","l","k","i","n","g","E","d"]
fileprivate let str_markTitle:String = "itNamif self index if model"
fileprivate let str_appToData:String = "eCellsuccess enable make in"

/*: "DistinctThen" :*/
fileprivate let str_sectionValue:[Character] = ["T","a","l","k","i","n","g","E","d"]
fileprivate let str_resumeValue:String = "biology"
fileprivate let str_succeedData:String = "online model iftSignCell"

/*: "Save the change?" :*/
fileprivate let str_arrayUserViewData:String = "Savecenter manager error"
fileprivate let str_priceTitle:String = "chidden"
fileprivate let str_interactionName:[Character] = ["a","n","g","e","?"]

/*: "Cancel" :*/
fileprivate let str_barStopValue:[Character] = ["C","a","n","c","e","l"]

/*: "Save" :*/
fileprivate let str_biologyName:String = "Savestring photo"

/*: "nickname" :*/
fileprivate let str_shareName:String = "present"
fileprivate let str_mData:[Character] = ["i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let str_wrapText:[Character] = ["b"]
fileprivate let str_topTitle:String = "IRTHDAY"

/*: "desc" :*/
fileprivate let str_callTabTitle:String = "coloresc"

/*: "Modify the success" :*/
fileprivate let str_modeMessageData:[Character] = ["M","o","d","i","f","y"," ","t","h","e"]
fileprivate let str_rowData:String = "red file suc"

/*: "pic" :*/
fileprivate let str_stateHeadDataName:[Character] = ["p","i","c"]

/*: "[ :*/
fileprivate let str_rowTagData:String = "["

/*: ]" :*/
fileprivate let str_tabModelValue:String = "on"

/*: "pids" :*/
fileprivate let str_tipUserValue:String = "clickids"

/*: "aboutMe" :*/
fileprivate let str_feeKeyNeverContent:String = "AB"
fileprivate let str_rangeBackText:String = "outMeframe message equal self social"

/*: "interest" :*/
fileprivate let str_intervalName:String = "INTEREST"

/*: "tagIds" :*/
fileprivate let str_everyManagerName:[Character] = ["t"]
fileprivate let str_touchUserValue:String = "for color to leadingagIds"

/*: "category" :*/
fileprivate let str_regularName:[Character] = ["c","a","t","e","g","o","r","y"]

/*: "An error occurred, please try again" :*/
fileprivate let str_mainReasonData:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "imgUrl" :*/
fileprivate let str_textFatalContent:[Character] = ["i","m","g","U","r","l"]

/*: "videoUrl" :*/
fileprivate let str_layerContent:[Character] = ["v","i","d"]
fileprivate let str_placeValue:String = "eoUrllet equal layer any"

/*: "Video cover submitted successfully" :*/
fileprivate let str_colorThroughValue:[UInt8] = [0x66,0x59,0x54,0x55,0x5f,0x10,0x53,0x5f,0x46,0x55,0x42,0x10,0x43,0x45,0x52,0x5d,0x59,0x44,0x44,0x55,0x54,0x10,0x43,0x45,0x53,0x53,0x55,0x43,0x43,0x56,0x45,0x5c,0x5c,0x49]

private func modeTable(all num: UInt8) -> UInt8 {
    return num ^ 48
}

/*: "WeltanschauungReactiveCompatible" :*/
fileprivate let str_finishPopContent:String = "Talkingcount photo"
fileprivate let str_listLayerText:String = "mode modeEditVi"

/*: "video_url" :*/
fileprivate let str_usData:String = "VID"
fileprivate let str_useText:String = "eo_urldata current time case name"

/*: "img_url" :*/
fileprivate let str_performTitle:String = "img_uview video bottom"
fileprivate let str_targetNameData:String = "rshared"

/*: "status" :*/
fileprivate let str_mediumTitle:String = "STATUS"

/*: "CenterThen" :*/
fileprivate let str_lastData:String = "Talkinindex data return"
fileprivate let str_hiddenTitle:String = "PhotoCellcolor reason view sub any"

/*: "OrbitThen" :*/
fileprivate let str_imageMessageWindowValue:String = "level add true make gestureTalki"
fileprivate let str_titleErrValue:String = "tBirrender in"

/*: "BackgroundMeCell" :*/
fileprivate let str_moreText:String = "to time true text contentTalk"
fileprivate let str_levelItemData:String = "self viewditA"
fileprivate let str_clickTitle:[Character] = ["b","o","u"]
fileprivate let str_imageInfoData:[Character] = ["t","M","e","C","e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let str_frameLaboratoryName:[Character] = ["U","I","T","a","b","l","e","V","i","e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProfilesVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class ProfilesVc: ToControllerPositionBaseRecognizerDelegate {
	var giftSum: Int = 3
	var viewQuantity: Double = 96.8
	var evanesceArray: [AnyHashable] = []

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [PathMeasurable] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [PathMeasurable] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [NextTopTransformable] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (str_equalPageName.lowercased())

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.appIcon()
        //: self.title = "Edit profiles".localized
        self.title = (str_loadValue.replacingOccurrences(of: "end", with: "E") + String(str_reportValue.suffix(4)) + "profiles").localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(str_netTitle.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_appEqualName))), for: .normal)
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(addFor), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = RecordAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = RecordAppManager.share.loginUserMode.aboutMe!
        //: seleteInters = RecordAppManager.share.loginUserMode.interest!
        seleteInters = RecordAppManager.share.loginUserMode.interest!
        //: designView()
        cell()
        //: GetGallery()
        position()
        //: getVideoInfo()
        ofCheckPlay()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(keyboardMatchAdd(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (MainTable.canBecomeFirstResponder) && (MainTable.convert(CGRect(), to: MainTable.superview).size.width == 61.24) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let voiceProductLet = CountView(frame: MainTable.bounds.intersection(CGRect(x: CGFloat(353.30), y: CGFloat(425.29), width: CGFloat(106.01), height: CGFloat(103.42))))
            voiceProductLet.gestureSwitch = isChangeVideo
            voiceProductLet.balanceQuantity = videoStatus
            
            voiceProductLet.messageQuantity = { [self] textQuantity in
            self.giftSum = textQuantity
            
            var voiceProductLet = videoStatus
            voiceProductLet |= voiceProductLet | 9
            if voiceProductLet < self.giftSum {
                self.giftSum = voiceProductLet
            }
            
            return self.giftSum
            }
            voiceProductLet.withCount = { [self] iconSectionNumber in
            self.viewQuantity = iconSectionNumber
            
                self.viewQuantity += 1
                if self.viewQuantity >= 0 {
                    self.viewQuantity = self.viewQuantity - 1
                }
            return self.viewQuantity
            }
            voiceProductLet.completeArray = { [self] liquidEcstasyArray in
            self.evanesceArray = liquidEcstasyArray
            
            guard var value = self.evanesceArray as? [String] else {
                return nil
            }
            return value
            }
                MainTable.addSubview(voiceProductLet)
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        TotalerpretationProgressHUD.countimateEqualDismiss()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension ProfilesVc {
    //: func GetGallery() {
    func position() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = RecordAppManager.share.loginUserMode.userID
        dict[(str_tempValue.replacingOccurrences(of: "model", with: "d"))] = RecordAppManager.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        FocusThen.diligent(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<NextTopTransformable>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [NextTopTransformable])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func animateBeing() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_succeedValue) + String(str_markTitle.prefix(5)) + String(str_appToData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: UnseeableViewCell = i as! UnseeableViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_sectionValue) + str_resumeValue.replacingOccurrences(of: "biology", with: "i") + String(str_succeedData.suffix(9))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: DistinctThen = i as! DistinctThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&RecordAppManager.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&RecordAppManager.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && RecordAppManager.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && RecordAppManager.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && RecordAppManager.share.loginUserMode.birthday != self.birthday) || (signStr != (str_equalPageName.lowercased()) && RecordAppManager.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = HiddenAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            BackgroundAlertShow.userRight(message: (String(str_arrayUserViewData.prefix(4)) + " the " + str_priceTitle.replacingOccurrences(of: "hidden", with: "h") + String(str_interactionName)).localized, leftBtnTitle: (String(str_barStopValue)).localized, rightBtnTitle: (String(str_biologyName.prefix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                BackgroundAlertShow.everySize()
                //: self.uploadTool(isBack: true)
                self.comb(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func addFor() {
        //: uploadTool(isBack: false)
        comb(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func comb(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_succeedValue) + String(str_markTitle.prefix(5)) + String(str_appToData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: UnseeableViewCell = i as! UnseeableViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_sectionValue) + str_resumeValue.replacingOccurrences(of: "biology", with: "i") + String(str_succeedData.suffix(9))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: DistinctThen = i as! DistinctThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && RecordAppManager.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, RecordAppManager.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(str_shareName.replacingOccurrences(of: "present", with: "n") + String(str_mData))] = nameStr
        }
        //: if birthday.count > 0 && RecordAppManager.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, RecordAppManager.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(str_wrapText) + str_topTitle.lowercased())] = birthday
        }
        //: if signStr != "nil" && RecordAppManager.share.loginUserMode.signature != signStr {
        if signStr != (str_equalPageName.lowercased()), RecordAppManager.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(str_callTabTitle.replacingOccurrences(of: "color", with: "d"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        blockColorTips(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && RecordAppManager.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.female.rawValue {
            //: uploadVideo()
            past()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        FocusThen.mentalRepresentation(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.moveAir(showMsg: (String(str_modeMessageData) + String(str_rowData.suffix(4)) + "cess").localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: RecordAppManager.share.loginUserMode.nickname = self.nameStr
                    RecordAppManager.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: RecordAppManager.share.loginUserMode.birthday = self.birthday
                    RecordAppManager.share.loginUserMode.birthday = self.birthday
                }
                //: RecordAppManager.share.loginUserMode.signature = self.signStr
                RecordAppManager.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.talkTask(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func blockColorTips(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            viewDrop()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                showRoom()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            schmooze(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            schmooze(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func showRoom() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].creatureData()! as NSData
            let resultData: NSData = seletePhotoArray[i].creatureData()! as NSData
            //: dict["pic"] = resultData
            dict[(String(str_stateHeadDataName))] = resultData
            //: ProgressHUD.show()
            TotalerpretationProgressHUD.alienation()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            FocusThen.record(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.moveAir(showMsg: (String(str_modeMessageData) + String(str_rowData.suffix(4)) + "cess").localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.position()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func viewDrop() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(str_tipUserValue.replacingOccurrences(of: "click", with: "p"))] = str
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        FocusThen.viewModel(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.showRoom()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.moveAir(showMsg: (String(str_modeMessageData) + String(str_rowData.suffix(4)) + "cess").localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.position()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func schmooze(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [PathMeasurable] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (str_feeKeyNeverContent.lowercased() + String(str_rangeBackText.prefix(5)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (str_intervalName.lowercased())
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(str_everyManagerName) + String(str_touchUserValue.suffix(5)))] = str
        //: dict["category"] = category
        dict[(String(str_regularName))] = category
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        FocusThen.progress(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.moveAir(showMsg: (String(str_modeMessageData) + String(str_rowData.suffix(4)) + "cess").localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: RecordAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    RecordAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: RecordAppManager.share.loginUserMode.interest = self.seleteInters
                    RecordAppManager.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func past() {
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        FromThen.shared.mudra(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.talkTask(showMsg: String(bytes: str_mainReasonData.reversed(), encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(str_textFatalContent))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(str_layerContent) + String(str_placeValue.prefix(5)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            FocusThen.chromaticColour(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.talkTask(showMsg: String(bytes: str_mainReasonData.reversed(), encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.moveAir(showMsg: String(bytes: str_colorThroughValue.map{modeTable(all: $0)}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(str_finishPopContent.prefix(7)) + String(str_listLayerText.suffix(6)) + "deoCell") {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: WeltanschauungReactiveCompatible = i as! WeltanschauungReactiveCompatible
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.upwardShared(videoPlayPath: dict[(String(str_layerContent) + String(str_placeValue.prefix(5)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.write(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func ofCheckPlay() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        FocusThen.pastLocal { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(str_usData.lowercased() + String(str_useText.prefix(6)))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(str_performTitle.prefix(5)) + str_targetNameData.replacingOccurrences(of: "shared", with: "l"))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(str_mediumTitle.lowercased())] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension ProfilesVc {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func keyboardMatchAdd(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_succeedValue) + String(str_markTitle.prefix(5)) + String(str_appToData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: UnseeableViewCell = i as! UnseeableViewCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_sectionValue) + str_resumeValue.replacingOccurrences(of: "biology", with: "i") + String(str_succeedData.suffix(9))) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: DistinctThen = i as! DistinctThen
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension ProfilesVc: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if RecordAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RecordAppManager.share.loginUserMode.sex == RowErrorScalarLiteral.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_itemData - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.variant(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.variant(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(str_finishPopContent.prefix(7)) + String(str_listLayerText.suffix(6)) + "deoCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? WeltanschauungReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = WeltanschauungReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.write(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.totalerlinkChoice(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.upwardShared(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(str_lastData.prefix(6)) + "gEdit" + String(str_hiddenTitle.prefix(9)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CenterThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = CenterThen(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.each(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.substanceLoadWeddingPicture()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(str_succeedValue) + String(str_markTitle.prefix(5)) + String(str_appToData.prefix(5)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? UnseeableViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = UnseeableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(str_imageMessageWindowValue.suffix(5)) + "ngEdi" + String(str_titleErrValue.prefix(4)) + "thdayCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? OrbitThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = OrbitThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(str_sectionValue) + str_resumeValue.replacingOccurrences(of: "biology", with: "i") + String(str_succeedData.suffix(9)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? DistinctThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = DistinctThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(str_moreText.suffix(4)) + "ingE" + String(str_levelItemData.suffix(4)) + String(str_clickTitle) + String(str_imageInfoData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? BackgroundMeCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = BackgroundMeCell(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.setViewRubric()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.parallelize(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.parallelize(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = LiveLayoutDelegate()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.leadership(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.leadership(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.leadership(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.leadership(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(str_frameLaboratoryName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func variant(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [PathMeasurable]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if DemandReactiveCompatible.share.interfaceLang == PropagandaNameProtocol.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if DemandReactiveCompatible.share.interfaceLang == PropagandaNameProtocol.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if DemandReactiveCompatible.share.interfaceLang == PropagandaNameProtocol.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_itemData - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - HistoryReactiveCompatible

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension ProfilesVc: HistoryReactiveCompatible {
    //: func freshSeleteTag() {
    func supplementTag() {
        //: seleteAboutme =  RecordAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = RecordAppManager.share.loginUserMode.aboutMe!
        //: seleteInters =  RecordAppManager.share.loginUserMode.interest!
        seleteInters = RecordAppManager.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - PortionPhotoDelegate

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension ProfilesVc: PortionPhotoDelegate {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func app(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = NextTopTransformable()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func sizeAcross(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func circumference(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension ProfilesVc {
    //: private func designView() {
    private func cell() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(WeltanschauungReactiveCompatible.self, forCellReuseIdentifier: (String(str_finishPopContent.prefix(7)) + String(str_listLayerText.suffix(6)) + "deoCell"))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(CenterThen.self, forCellReuseIdentifier: (String(str_lastData.prefix(6)) + "gEdit" + String(str_hiddenTitle.prefix(9))))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(UnseeableViewCell.self, forCellReuseIdentifier: (String(str_succeedValue) + String(str_markTitle.prefix(5)) + String(str_appToData.prefix(5))))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(OrbitThen.self, forCellReuseIdentifier: (String(str_imageMessageWindowValue.suffix(5)) + "ngEdi" + String(str_titleErrValue.prefix(4)) + "thdayCell"))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(DistinctThen.self, forCellReuseIdentifier: (String(str_sectionValue) + str_resumeValue.replacingOccurrences(of: "biology", with: "i") + String(str_succeedData.suffix(9))))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(BackgroundMeCell.self, forCellReuseIdentifier: (String(str_moreText.suffix(4)) + "ingE" + String(str_levelItemData.suffix(4)) + String(str_clickTitle) + String(str_imageInfoData)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
