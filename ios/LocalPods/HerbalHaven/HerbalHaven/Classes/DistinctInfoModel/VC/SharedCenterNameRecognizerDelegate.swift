
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_succeedContent:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Text greeting" :*/
fileprivate let str_placeValue:String = "text data var height succeedText gr"
fileprivate let str_leadingData:[Character] = ["e","e","t","i","n","g"]

/*: "Add" :*/
fileprivate let str_tabTitle:String = "Addview path model from"

/*: "type" :*/
fileprivate let str_cropData:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_appData:String = "move self array count selfaddStatus"

/*: "list" :*/
fileprivate let str_layerStatusData:String = "LIST"

/*: "Are you sure to delete this message？" :*/
fileprivate let str_defineData:[UInt8] = [0x9f,0xbc,0xef,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x73,0x69,0x68,0x74,0x20,0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Cancel" :*/
fileprivate let str_equalKeyValue:String = "Cancestatus lab"
fileprivate let str_playerValue:[Character] = ["l"]

/*: "Delete" :*/
fileprivate let str_titleMakeName:[Character] = ["D","e","l","e","t","e"]

/*: "id" :*/
fileprivate let str_targetData:String = "iarea"

/*: "The upper limit has been reached" :*/
fileprivate let str_managerTextIfData:[UInt8] = [0x91,0xa5,0xa2,0x5d,0xb2,0xad,0xad,0xa2,0xaf,0x5d,0xa9,0xa6,0xaa,0xa6,0xb1,0x5d,0xa5,0x9e,0xb0,0x5d,0x9f,0xa2,0xa2,0xab,0x5d,0xaf,0xa2,0x9e,0xa0,0xa5,0xa2,0xa1]

fileprivate func titleNor(bottom num: UInt8) -> UInt8 {
    let value = Int(num) - 61
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "text" :*/
fileprivate let str_viewTempValue:String = "tcellt"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_giftBarTitle:[UInt8] = [0x8e,0xa8,0xbe,0xbe,0xb8,0xae,0xae,0xbb,0xa8,0xb1,0xb1,0xa4,0xfd,0xbc,0xb9,0xb9,0xb8,0xb9,0xf1,0xfd,0xad,0xb1,0xb8,0xbc,0xae,0xb8,0xfd,0xaa,0xbc,0xb4,0xa9,0xfd,0xbb,0xb2,0xaf,0xfd,0xbc,0xad,0xad,0xaf,0xb2,0xab,0xbc,0xb1]

private func userEnter(server num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "Any-Hex/Java" :*/
fileprivate let str_sectionData:[Character] = ["A","n","y","-","H","e","x","/"]
fileprivate let str_hiddenTitle:String = "array match mark nor viewJava"

/*: "FocusClickViewCell" :*/
fileprivate let str_plusValue:[Character] = ["T","a","l","k","i","n","g","G","r","e","e","t","T","e","x","t","C","e","l"]
fileprivate let str_topValue:[Character] = ["l"]

/*: "#FF506D" :*/
fileprivate let str_contentTitleCenterText:String = "self let in data as#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let str_itemBeautyName:[Character] = ["b","t","n","_","m","e","s","s"]
fileprivate let str_showDateDataValue:[Character] = ["a","g","e","_","d","e","l","e","t","e","_","n","o","r"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_maleFreeData:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63,0xa,0x64,0x64,0x61,0x20,0x6f,0x74,0x20,0x77,0x6f,0x6c,0x65,0x62,0x20,0x6e,0x6f,0x74,0x74,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let str_appTitle:[UInt8] = [0xe9,0xd6,0xdb,0xd0,0x9e,0xdf,0xda,0xda,0xd7,0xd0,0xd9,0x9e,0xd3,0xcb,0xd2,0xca,0xd7,0xce,0xd2,0xdb,0x9e,0xca,0xdb,0xc6,0xca,0x9e,0xd3,0xdb,0xcd,0xcd,0xdf,0xd9,0xdb,0x92,0x9e,0xca,0xd6,0xdb,0x9e,0xcd,0xc7,0xcd,0xca,0x9e,0xdb,0xd3,0x9e,0xc9,0xd7,0xd2,0xd2,0x9e,0xcd,0xdb,0xd0,0xda,0x9e,0xca,0xd6,0xdb,0xd3,0x9e,0xdc,0xc7,0x9e,0xca,0xcb,0xcc,0xd0,0xcd,0x85,0xcd,0xd2,0xd7,0xda,0xdb,0x9e,0xca,0xd1,0x9e,0xca,0xd6,0xdb,0x9e,0xd3,0xdb,0xcd,0xcd,0xdf,0xd9,0xdb,0x9e,0xca,0xd1,0x9e,0xda,0xdb,0xd2,0xdb,0xca,0xdb]

private func byTo(top num: UInt8) -> UInt8 {
    return num ^ 190
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SharedCenterNameRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class SharedCenterNameRecognizerDelegate: ToControllerPositionBaseRecognizerDelegate {
	var bottomTitle: String = "view"
	var limitOn: Bool = true
	var videoCount: Double = 57.2
	var someoneDictionary: [AnyHashable: String] = [:]

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: CommitRowView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_succeedContent.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var detailTotalValue = recordView?.inputTView.placeholder { 
			if var lastValue = recordView?.inputTView.placeholder { 
			if let recordView = recordView {
		
		            if (recordView.layer.position.x == 34.74) && (recordView.intrinsicContentSize.height == 264.75) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let liveModel = PicView(frame: recordView.bounds.intersection(CGRect(x: CGFloat(53), y: CGFloat(67), width: CGFloat(0), height: CGFloat(98))))
		            liveModel.loadTitle = lastValue
		            
		            
		            liveModel.imageName = { [self] indexContent in
		            self.bottomTitle = indexContent
		            
		            var liveModel = detailTotalValue
		            let page = liveModel.suffix(from: liveModel.startIndex).isEmpty
		            liveModel.removeAll(keepingCapacity: page)
		            if liveModel.contains(self.bottomTitle) {
		                self.bottomTitle = liveModel
		            }
		            
		            return self.bottomTitle
		            }
		                recordView.addSubview(liveModel)
		            }
		
			}
		
			}
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var gutterValue = recordView?.inputTView.placeholder { 
	            if (self.children.count == 19) && (self.modalTransitionStyle == .partialCurl) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let viewMatch = PicController()
	            viewMatch.modelTotal = addStatus
	            viewMatch.accountingTitle = gutterValue
	            viewMatch.talkSwitch = { [self] playerClose in
	            self.limitOn = playerClose
	            
	            var viewMatch = animated
	                viewMatch = false
	                viewMatch = false
	            if viewMatch != self.limitOn {
	                self.limitOn = viewMatch
	            }
	            
	            self.limitOn = !self.limitOn
	            return self.limitOn
	            }
	            viewMatch.modelGiftNumber = { [self] todaySum in
	            self.videoCount = todaySum
	            
	            return self.videoCount
	            }
	            viewMatch.resourceDictionary = { [self] labChangeDictionary in
	            self.someoneDictionary = labChangeDictionary
	            
	            guard var value = self.someoneDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                self.navigationController?.pushViewController(viewMatch.self, animated: false)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(str_placeValue.suffix(7)) + String(str_leadingData)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.appIcon()
        //: designView()
        contrive()
        //: getAudioListData()
        user()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [RowTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [RowTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_tabTitle.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.imageSize(colors: UIColor.appColorVideo(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mode), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension SharedCenterNameRecognizerDelegate {
    //: func getAudioListData() {
    func user() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(str_cropData))] = 2
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        FocusThen.extract(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.statusCell()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_appData.suffix(9)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(str_layerStatusData.lowercased())] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [RowTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<RowTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func showIndex(index: Int) {
        //: let config = ShowAlertConfig()
        let config = HiddenAlertConfig()
        //: config.textFont = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        config.textFont = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        BackgroundAlertShow.userRight(message: String(bytes: str_defineData.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(str_equalKeyValue.prefix(5)) + String(str_playerValue)).localized, rightBtnTitle: (String(str_titleMakeName)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            BackgroundAlertShow.everySize()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            BackgroundAlertShow.everySize()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: RowTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            TotalerpretationProgressHUD.alienation()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            FocusThen.level(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.user()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension SharedCenterNameRecognizerDelegate {
    //: @objc func addButtonClick() {
    @objc func mode() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.talkTask(showMsg: String(bytes: str_managerTextIfData.map{titleNor(bottom: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = CommitRowView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.duringLimit()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.toAGreaterExtent(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(str_cropData))] = 2
            //: dict["text"] = tempStr
            dict[(str_viewTempValue.replacingOccurrences(of: "cell", with: "ex"))] = tempStr
            //: ProgressHUD.show()
            TotalerpretationProgressHUD.alienation()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            FocusThen.indexAction(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                TotalerpretationProgressHUD.countimateEqualDismiss()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.moveAir(showMsg: String(bytes: str_giftBarTitle.map{userEnter(server: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.user()
                //: self.recordView?.dismiss()
                self.recordView?.pop()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func toAGreaterExtent(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(str_sectionData) + String(str_hiddenTitle.suffix(4))) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension SharedCenterNameRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kLet_itemData - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(str_plusValue) + String(str_topValue))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: FocusClickViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! FocusClickViewCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.freebeeLoad(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_titleMakeName)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.showIndex(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_contentTitleCenterText.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.showIndex(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.markTag(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.markTag(name: (String(str_itemBeautyName) + String(str_showDateDataValue))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = FocusViewThen(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension SharedCenterNameRecognizerDelegate {
    //: private func setupEmptyDataView() {
    private func statusCell() {
        //: var style = EmptyStyle()
        var style = SharedEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_maleFreeData.reversed(), encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .appIcon()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func contrive() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(FocusClickViewCell.self, forCellReuseIdentifier: (String(str_plusValue) + String(str_topValue)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.pullUpColor()
        label.textColor = UIColor.pullUpColor()
        //: label.font = UIFont.moveDismiss(fontSize: 15)
        label.font = UIFont.moveDismiss(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: str_appTitle.map{byTo(top: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
