
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_pairData:[UInt8] = [0x5a,0x5d,0x5a,0x47,0x1b,0x50,0x5c,0x57,0x56,0x41,0x9,0x1a,0x13,0x5b,0x52,0x40,0x13,0x5d,0x5c,0x47,0x13,0x51,0x56,0x56,0x5d,0x13,0x5a,0x5e,0x43,0x5f,0x56,0x5e,0x56,0x5d,0x47,0x56,0x57]

/*: "Cancel" :*/
fileprivate let str_laySpendFrameName:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let str_removeNorContent:[Character] = ["S","u","b","m","i"]
fileprivate let str_videoQuantityData:String = "sound"

/*: "Please tell us your problems                   " :*/
fileprivate let str_makeImageTitle:[UInt8] = [0x4a,0x76,0x7f,0x7b,0x69,0x7f,0x3a,0x6e,0x7f,0x76,0x76,0x3a,0x6f,0x69,0x3a,0x63,0x75,0x6f,0x68,0x3a,0x6a,0x68,0x75,0x78,0x76,0x7f,0x77,0x69,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a,0x3a]

/*: "You need to choose at least one." :*/
fileprivate let str_labelTitle:[UInt8] = [0x3,0x35,0x2f,0x7a,0x34,0x3f,0x3f,0x3e,0x7a,0x2e,0x35,0x7a,0x39,0x32,0x35,0x35,0x29,0x3f,0x7a,0x3b,0x2e,0x7a,0x36,0x3f,0x3b,0x29,0x2e,0x7a,0x35,0x34,0x3f,0x74]

private func indexDevice(record num: UInt8) -> UInt8 {
    return num ^ 90
}

/*: "Please tell us your problems." :*/
fileprivate let str_equalContent:[Character] = ["P","l","e","a","s","e"," ","t","e","l","l"," ","u","s"," ","y","o","u","r"," ","p","r","o","b","l","e","m","s"]
fileprivate let str_giftTitle:[Character] = ["."]

/*: "app/userReport" :*/
fileprivate let str_tabData:String = "handle voice drag forapp/"
fileprivate let str_labelValue:[Character] = ["R","e","p","o","r","t"]

/*: "id" :*/
fileprivate let str_tapData:[UInt8] = [0x33,0x3e]

private func countensityServer(mark num: UInt8) -> UInt8 {
    return num ^ 90
}

/*: "reason" :*/
fileprivate let str_frontName:String = "R"
fileprivate let str_clearTitle:String = "eanoron"

/*: "desc" :*/
fileprivate let str_popValue:[UInt8] = [0x63,0x73,0x65,0x64]

/*: "mf/moment/report" :*/
fileprivate let str_modeEqualForTitle:String = "status clear modelmf/mome"
fileprivate let str_managerAddContent:String = "nt/reportuser button lab appear else"

/*: "momentId" :*/
fileprivate let str_characterTitle:[UInt8] = [0x5c,0x5e,0x5c,0x54,0x5f,0x45,0x78,0x55]

private func modelTitleText(live num: UInt8) -> UInt8 {
    return num ^ 49
}

/*: "mf/momentReply/report" :*/
fileprivate let str_finishValue:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e"]
fileprivate let str_contextText:[Character] = ["p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let str_replyText:[Character] = ["a","p","p","/","v","i","d","e"]
fileprivate let str_betweenValue:[Character] = ["o","R","e","p","o","r","t"]

/*: "logId" :*/
fileprivate let str_requestTitle:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let str_resultLabelValue:[Character] = ["S","u","b","m","i","t","t"]
fileprivate let str_centerAspectName:String = "ed succelet right"
fileprivate let str_backName:[Character] = ["s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let str_userInstanceTitle:String = "model titleNudity"

/*: "Violence" :*/
fileprivate let str_aspectName:String = "v"
fileprivate let str_taskValueAddText:String = "ioletexte"

/*: "Harassment" :*/
fileprivate let str_enterValue:String = "Harassview task mode mask value"

/*: "Suicide or Self-injury" :*/
fileprivate let str_nowFormatSeatName:String = "var conversationSuicide "
fileprivate let str_noticeData:String = "or Self-iname path"
fileprivate let str_appTitle:String = "njrowy"

/*: "False Information" :*/
fileprivate let str_appearUserValue:[Character] = ["F","a","l","s","e"," "]
fileprivate let str_clearName:String = "content name push selected ofInform"

/*: "Spam" :*/
fileprivate let str_priceNextName:String = "next beginSpam"

/*: "Hate Speech" :*/
fileprivate let str_adjustTitleName:String = "Hatemake any user"

/*: "Terrorism" :*/
fileprivate let str_subData:[Character] = ["T","e","r","r","o","r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let str_viewSharedContainerName:[UInt8] = [0x1e,0x2c,0x37,0x30,0xeb,0x3a,0x31,0xeb,0x14,0x37,0x37,0x30,0x32,0x2c,0x37,0xeb,0x3a,0x3d,0xeb,0x1d,0x30,0x32,0x40,0x37,0x2c,0x3f,0x30,0x2f,0xeb,0x12,0x3a,0x3a,0x2f,0x3e]

fileprivate func laboratoryFile(look num: UInt8) -> UInt8 {
    let value = Int(num) - 203
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let str_targetName:String = "make response array pathSend"
fileprivate let str_indexSectionText:String = "viewguview"
fileprivate let str_tempGestureValue:String = "pathessages"

/*: "Using photos of nudity or drugs" :*/
fileprivate let str_confirmData:[UInt8] = [0xe5,0xc3,0xd9,0xde,0xd7,0x90,0xc0,0xd8,0xdf,0xc4,0xdf,0xc3,0x90,0xdf,0xd6,0x90,0xde,0xc5,0xd4,0xd9,0xc4,0xc9,0x90,0xdf,0xc2,0x90,0xd4,0xc2,0xc5,0xd7,0xc3]

/*: "Harassing or sending ads" :*/
fileprivate let str_languageTitle:String = "view type true out pathHara"
fileprivate let str_resignSizeName:String = "data value trueor s"
fileprivate let str_wayName:String = "g adsmode any corner view to"

/*: "Defraud or using fake info" :*/
fileprivate let str_labFrameValue:String = "last case beautyDefrau"
fileprivate let str_leadingTitle:String = "usstatusng"
fileprivate let str_hiddenFrontData:String = "report make inside halfe info"

/*: "Others" :*/
fileprivate let str_noFromValue:[Character] = ["O","t","h","e","r","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ConditionListReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import UIKit
import UIKit

//: import Then
import Then

//: enum TalkingReportAlertType: CaseIterable {
enum RowMakeCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ConditionListReactiveCompatible: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: RowMakeCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [EqualColorThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = EqualColorThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: RowMakeCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        cardSubviews()
        //: setupSubViewsConstraint()
        cutFromConstraint()
        //: setupReasons()
        single()
        //: bindInteraction()
        quantityerest()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_pairData.map{$0^51}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(str_laySpendFrameName)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.viewColor(), for: .normal)
        //: $0.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.viewColor().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(toSelect), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(str_removeNorContent) + str_videoQuantityData.replacingOccurrences(of: "sound", with: "t")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.imageSize(colors: UIColor.appColorVideo(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(saveAction), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kLet_itemData - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.quantityerfoil(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.pullUpColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = MissiveReactiveCompatible(placeholder: String(bytes: str_makeImageTitle.map{$0^26}, encoding: .utf8)!.localized, placeholderColor: UIColor.pullUpColor(), frame: CGRect(x: 10, y: 10, width: kLet_itemData - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.segment()
        //: $0.placeholdertextView.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .biologyLaboratoryEqual(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.quantityerfoil(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(YoungBegettingViewCell.self, forCellReuseIdentifier: YoungBegettingViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ConditionListReactiveCompatible {
    //: @objc func func__TapGestureRecognizer() {
    @objc func live() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func toView(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = AreaReactiveCompatible.getWindow()
            let window = AreaReactiveCompatible.rowWindow()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func atWant(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func toSelect() {
        //: self.dismissReportView(succeed: false)
        self.atWant(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func saveAction() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.talkTask(showMsg: String(bytes: str_labelTitle.map{indexDevice(record: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.talkTask(showMsg: (String(str_equalContent) + String(str_giftTitle)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(str_tabData.suffix(4)) + "user" + String(str_labelValue))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (str_frontName.lowercased() + str_clearTitle.replacingOccurrences(of: "nor", with: "s")): selectedModel.type, String(bytes: str_popValue.reversed(), encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(str_modeEqualForTitle.suffix(7)) + String(str_managerAddContent.prefix(9)))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_characterTitle.map{modelTitleText(live: $0)}, encoding: .utf8)!: tempId, (str_frontName.lowercased() + str_clearTitle.replacingOccurrences(of: "nor", with: "s")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(str_finishValue) + String(str_contextText))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (str_frontName.lowercased() + str_clearTitle.replacingOccurrences(of: "nor", with: "s")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(str_replyText) + String(str_betweenValue))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_requestTitle.reversed(), encoding: .utf8)!: tempId, (str_frontName.lowercased() + str_clearTitle.replacingOccurrences(of: "nor", with: "s")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        PostulationReactiveCompatible.componentWay(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.moveAir(showMsg: (String(str_resultLabelValue) + String(str_centerAspectName.prefix(8)) + String(str_backName)).localized)
                //: self.dismissReportView(succeed: true)
                self.atWant(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ConditionListReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: YoungBegettingViewCell.className()) as! YoungBegettingViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.objectModel(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ConditionListReactiveCompatible {
    /// 添加视图
    //: private func setupSubviews() {
    private func cardSubviews() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func cutFromConstraint() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func single() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(str_userInstanceTitle.suffix(6))).localized,
                           //: "Violence".localized,
                           (str_aspectName.uppercased() + str_taskValueAddText.replacingOccurrences(of: "text", with: "nc")).localized,
                           //: "Harassment".localized,
                           (String(str_enterValue.prefix(6)) + "ment").localized,
                           //: "Suicide or Self-injury".localized,
                           (String(str_nowFormatSeatName.suffix(8)) + String(str_noticeData.prefix(9)) + str_appTitle.replacingOccurrences(of: "row", with: "ur")).localized,
                           //: "False Information".localized,
                           (String(str_appearUserValue) + String(str_clearName.suffix(6)) + "ation").localized,
                           //: "Spam".localized,
                           (String(str_priceNextName.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(str_adjustTitleName.prefix(4)) + " Speech").localized,
                           //: "Terrorism".localized,
                           (String(str_subData)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: str_viewSharedContainerName.map{laboratoryFile(look: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(str_targetName.suffix(4)) + "ing di" + str_indexSectionText.replacingOccurrences(of: "view", with: "s") + "ting " + str_tempGestureValue.replacingOccurrences(of: "path", with: "m")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: str_confirmData.map{$0^176}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(str_languageTitle.suffix(4)) + "ssing " + String(str_resignSizeName.suffix(4)) + "endin" + String(str_wayName.prefix(5))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(str_labFrameValue.suffix(6)) + "d or " + str_leadingTitle.replacingOccurrences(of: "status", with: "i") + " fak" + String(str_hiddenFrontData.suffix(6))).localized,
                           //: "Others".localized]
                           (String(str_noFromValue)).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = RecordAppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = RecordAppManager.share.appConfigMode.videoReport[(str_frontName.lowercased() + str_clearTitle.replacingOccurrences(of: "nor", with: "s"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = EqualColorThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func quantityerest() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
