
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let str_currentTipName:String = "if usermf/mom"
fileprivate let str_makeSucceedVarData:String = "professional toent/list"

/*: "mf/moment/moments" :*/
fileprivate let str_appValue:String = "frame formf/mo"
fileprivate let str_blockAddAccountData:[Character] = ["/","m","o","m","e","n","t","s"]

/*: "mf/moment/like" :*/
fileprivate let str_groupName:String = "mf/moimage player"
fileprivate let str_labelText:[Character] = ["m","e","n","t","/","l","i","k","e"]

/*: "mf/crush/send" :*/
fileprivate let str_bottomLastContent:[Character] = ["m","f","/","c","r","u","s","h","/","s"]
fileprivate let str_interactionContent:String = "enmake"

/*: "mf/moment/view" :*/
fileprivate let str_appearTitle:String = "ting image greenmf/mo"
fileprivate let str_actionArrayName:String = "ment/self model label"

/*: "mf/momentReply/list" :*/
fileprivate let str_withEqualName:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","l","i","s","t"]

/*: "mf/momentReply/save" :*/
fileprivate let str_myData:String = "narrow else else kit tomf/mo"
fileprivate let str_searchContent:[Character] = ["m","e"]
fileprivate let str_tapData:String = "ntRehoney result name"

/*: "mf/moment/videoSign" :*/
fileprivate let str_pageData:String = "social position model tomf/m"
fileprivate let str_picName:String = "t/vidname var let let segment"
fileprivate let str_normalToText:[Character] = ["e","o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let str_labelTitle:String = "pagenami"
fileprivate let str_markValue:[Character] = ["c","P","i","c",".","z","i","p"]

/*: "pic" :*/
fileprivate let str_hiddenName:[UInt8] = [0x82,0x9b,0x91]

private func colorView(indicator num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "content" :*/
fileprivate let str_tapName:[Character] = ["c"]
fileprivate let str_sectionMakeData:String = "onrestrictionnt"

/*: "mf/moment/save" :*/
fileprivate let str_targetData:[Character] = ["m","f","/","m","o","m","e","n","t","/","s"]
fileprivate let str_makeName:[Character] = ["a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let str_seatName:[UInt8] = [0x7f,0xa4,0x91,0x9b,0x98,0xa2,0x97,0x4f,0x82,0xa4,0x92,0x92,0x94,0xa2,0xa2,0x95,0xa4,0x9b,0x5b,0x4f,0x76,0x9e,0x4f,0xa3,0x9e,0x4f,0x7c,0xa8,0x4f,0x73,0xa8,0x9d,0x90,0x9c,0x98,0x92,0xa2,0x4f,0xa3,0x9e,0x4f,0xa5,0x98,0x94,0xa6]

fileprivate func colorRaw(temp num: UInt8) -> UInt8 {
    let value = Int(num) - 47
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let str_clubValue:[Character] = ["m","f","/"]
fileprivate let str_leadingTitle:String = "infooinfoent"
fileprivate let str_actualText:String = "indexeo"

/*: .png" :*/
fileprivate let str_labelValue:String = ".pngadd manager share gift first"

/*: "dynamicPic" :*/
fileprivate let str_clearName:String = "dyntitleic"
fileprivate let str_matchValue:String = "make selfPic"

/*: "freeImage" :*/
fileprivate let str_handleRestoreName:String = "frup"
fileprivate let str_aspectTitle:[Character] = ["I","m","a","g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let str_equalRawPopName:String = "mf/momequal pic"
fileprivate let str_sectionContent:String = "add self target/re"
fileprivate let str_cardText:[Character] = ["p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let str_beautyData:[Character] = ["m","f","/","m","o","m"]
fileprivate let str_listStartData:[Character] = ["e","n","t","/","d","e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let str_mainValue:String = "mf/momself tag to let"
fileprivate let str_selectLabelName:String = "eply/detext register pic var"
fileprivate let str_layerData:String = "lerrorterror"

/*: "mf/moment/pin" :*/
fileprivate let str_enableData:[Character] = ["m","f","/","m","o","m"]
fileprivate let str_applicationVideoTitleText:[Character] = ["e","n","t","/","p","i","n"]

/*: "mf/moment/remindNum" :*/
fileprivate let str_scaleName:String = "mf/momoment background count"
fileprivate let str_logContent:String = "instance"
fileprivate let str_giftSectionData:[Character] = ["e","n","t","/","r","e","m","i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let str_rankName:String = "mf/moblock let below file"
fileprivate let str_fieldTimeName:String = "manager var position view appment/r"
fileprivate let str_makeData:String = "gift let white layerList"

/*: "page" :*/
fileprivate let str_pushSizeTitle:[UInt8] = [0xd4,0xc5,0xc3,0xc1]

/*: "cos/getCredential" :*/
fileprivate let str_emptyFrameTryValue:String = "as layercos/ge"
fileprivate let str_voiceTitle:String = "denactionia"
fileprivate let str_kitIndexTitle:String = "L"

/*: "type" :*/
fileprivate let str_toSharedValue:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let str_clickModelValue:String = "self mode view canceluser/"
fileprivate let str_shareMenuData:String = "array break window equal observerrtUser"
fileprivate let str_wantValue:String = "extension model labelInfo"

/*: "uid" :*/
fileprivate let str_pushData:String = "uvoice"

/*: "deviceId" :*/
fileprivate let str_starPathName:String = "DE"
fileprivate let str_blockValue:[Character] = ["v","i","c","e","I","d"]

/*: "deviceName" :*/
fileprivate let str_bottomActionContent:[Character] = ["d","e","v","i","c","e","N"]
fileprivate let str_cellMagnitudeervalTitle:[Character] = ["a","m","e"]

/*: "iphone" :*/
fileprivate let str_messageLabelTextContent:String = "isessionhone"

/*: "fileUrl" :*/
fileprivate let str_shadowName:String = "fileUlet label info"
fileprivate let str_fromManagerData:[Character] = ["r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ConditionRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import UIKit
import UIKit

//: import Adjust
import Adjust

//: class TalkingMomentRequestTool: NSObject {
class ConditionRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func awakeQuit(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(str_currentTipName.suffix(6)) + String(str_makeSucceedVarData.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func magnitude(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(str_appValue.suffix(5)) + "ment" + String(str_blockAddAccountData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func love(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(str_groupName.prefix(5)) + String(str_labelText))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func crushRequest(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(str_bottomLastContent) + str_interactionContent.replacingOccurrences(of: "make", with: "d"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func common(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(str_appearTitle.suffix(5)) + String(str_actionArrayName.prefix(5)) + "view")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func noteDisappear(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(str_withEqualName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func addMy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(str_myData.suffix(5)) + String(str_searchContent) + String(str_tapData.prefix(4)) + "ply/save")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func titleCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(str_pageData.suffix(4)) + "omen" + String(str_picName.prefix(5)) + String(str_normalToText))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func prompt(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        jowl()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            purchase(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((str_labelTitle.replacingOccurrences(of: "page", with: "dy") + String(str_markValue)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: str_hiddenName.map{colorView(indicator: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(str_tapName) + str_sectionMakeData.replacingOccurrences(of: "restriction", with: "te"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = PtolemaicSystemThen()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(str_targetData) + String(str_makeName))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.moveAir(showMsg: String(bytes: str_seatName.map{colorRaw(temp: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.jowl()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func towardVideo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(str_clubValue) + str_leadingTitle.replacingOccurrences(of: "info", with: "m") + "/saveV" + str_actualText.replacingOccurrences(of: "index", with: "id"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func purchase(index: Int, image: UIImage) {
        //: let data = image.creatureData()
        let data = image.creatureData()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(str_labelValue.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((str_clearName.replacingOccurrences(of: "title", with: "am") + String(str_matchValue.suffix(3))))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func jowl() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((str_handleRestoreName.replacingOccurrences(of: "up", with: "ee") + String(str_aspectTitle)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func award(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(str_equalRawPopName.prefix(6)) + "entReply" + String(str_sectionContent.suffix(3)) + String(str_cardText))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func faculty(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(str_beautyData) + String(str_listStartData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func resonator(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(str_mainValue.prefix(6)) + "entR" + String(str_selectLabelName.prefix(7)) + str_layerData.replacingOccurrences(of: "error", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func search(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(str_enableData) + String(str_applicationVideoTitleText))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func dataList(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(str_scaleName.prefix(5)) + str_logContent.replacingOccurrences(of: "instance", with: "m") + String(str_giftSectionData))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func getEventCompletion(page: Int, completion: @escaping (_ data: [FocusModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(str_rankName.prefix(5)) + String(str_fieldTimeName.suffix(6)) + "emind" + String(str_makeData.suffix(4)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: str_pushSizeTitle.map{$0^164}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [FocusModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<FocusModelType>.deserialize(from: arr as? Array) as? [FocusModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func makeFull(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(str_emptyFrameTryValue.suffix(6)) + "tCre" + str_voiceTitle.replacingOccurrences(of: "action", with: "t") + str_kitIndexTitle.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(str_toSharedValue)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func somebody(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestServer = RecordAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = RecordAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(str_clickModelValue.suffix(5)) + "repo" + String(str_shareMenuData.suffix(6)) + String(str_wantValue.suffix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = RecordAppManager.share.loginUserMode.userID
        params[(str_pushData.replacingOccurrences(of: "voice", with: "id"))] = RecordAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(str_starPathName.lowercased() + String(str_blockValue))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(str_bottomActionContent) + String(str_cellMagnitudeervalTitle))] = (str_messageLabelTextContent.replacingOccurrences(of: "session", with: "p"))
        //: params["type"] = 1
        params[(String(str_toSharedValue))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(str_shadowName.prefix(5)) + String(str_fromManagerData))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
