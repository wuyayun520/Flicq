
//: Declare String Begin

/*: "cmd" :*/
fileprivate let str_clickTitle:[UInt8] = [0x54,0x5a,0x53]

private func viewVar(origin num: UInt8) -> UInt8 {
    return num ^ 55
}

/*: "cancelCall" :*/
fileprivate let str_makeTitle:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let str_appTitle:[UInt8] = [0x46,0x43,0x56,0x43]

private func labMake(image num: UInt8) -> UInt8 {
    return num ^ 34
}

/*: "logId" :*/
fileprivate let str_leadingValue:String = "logIdrequest let progress"

/*: "#322870" :*/
fileprivate let str_failureContent:String = "#view2870"

/*: "Tap to start video chatting" :*/
fileprivate let str_ageValue:String = "Tap to lab data else"
fileprivate let str_errData:[Character] = ["s","t","a","r","t"," ","v","i","d","e","o"," ","c","h"]
fileprivate let str_tagName:[Character] = ["a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let str_detailTitle:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let str_lockTitle:[Character] = ["o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let str_pairValue:String = "ICON"
fileprivate let str_toColorValue:String = "_numberplayer if ask manager"

/*: "%@ chatting" :*/
fileprivate let str_transformValue:[Character] = ["%","@"]
fileprivate let str_firstText:String = "time action chat"

/*: "icon_lounge" :*/
fileprivate let str_toLabelActionContent:[Character] = ["i","c","o","n","_","l","o","u","n"]
fileprivate let str_makeText:String = "third"

/*: "icon_rank_coin" :*/
fileprivate let str_moreTitle:String = "icon layer data last frameicon_ran"
fileprivate let str_sexText:String = "k_coinroom attribute error"

/*: "vip_btn_top" :*/
fileprivate let str_dismissValue:String = "vip_bbeat to unable"
fileprivate let str_inviteVideoValue:[Character] = ["o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_cornerClickName:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_"]
fileprivate let str_giftFatalTitle:[Character] = ["n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let str_effectValue:String = "%@ "
fileprivate let str_emptyContent:String = "temp for height let nameCoin"

/*: "get json error" :*/
fileprivate let str_fillValue:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let str_jumpTitle:String = "matchIequal view"
fileprivate let str_showKeyData:[Character] = ["d"]

/*: "errno" :*/
fileprivate let str_makeAllText:[UInt8] = [0x6f,0x6e,0x72,0x72,0x65]

/*: "VIPFee" :*/
fileprivate let str_padName:String = "VIPFeecontent if string"

/*: "Matching now" :*/
fileprivate let str_finishData:[Character] = ["M","a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let str_blockName:String = "userTto name table"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_successValue:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V"]
fileprivate let str_addEqualTitle:String = "iderro"

/*: "fromType" :*/
fileprivate let str_viewContentValue:[UInt8] = [0x65,0x70,0x79,0x54,0x6d,0x6f,0x72,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let str_shareData:[UInt8] = [0xeb,0xdd,0xc7,0x95,0xc4,0xd7,0x92,0xd0,0xd7,0xd7,0xdc,0x92,0xd0,0xd3,0xc0,0xc0,0xd7,0xd6,0x92,0xd4,0xc0,0xdd,0xdf,0x92,0xc0,0xd7,0xd1,0xd7,0xdb,0xc4,0xdb,0xdc,0xd5,0x92,0xd1,0xd3,0xde,0xde,0xc1]

private func protrudeLaugh(make num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "source" :*/
fileprivate let str_labelValue:[UInt8] = [0xb7,0xb3,0xb9,0xb6,0xa7,0xa9]

fileprivate func timingBubble(view num: UInt8) -> UInt8 {
    let value = Int(num) + 188
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let str_requestValue:[UInt8] = [0xd1,0xdc,0xd5,0xc0]

private func timeName(mark num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: "uid" :*/
fileprivate let str_iconObserverName:[UInt8] = [0xef,0xf3,0xfe]

private func titleAspect(value num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "fromFreeCall" :*/
fileprivate let str_makeData:String = "fparty"
fileprivate let str_playerName:String = "omFreeself false random height"

/*: "requestCall" :*/
fileprivate let str_detailContent:String = "REQU"

/*: "onVideoMatch" :*/
fileprivate let str_divisionName:String = "pin share stack letonVi"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_makeValue:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "refuseCall" :*/
fileprivate let str_executeFrameData:String = "refuseCalcolor phone skin data dismiss"
fileprivate let str_haveText:String = "make"

/*: "onCancelCall" :*/
fileprivate let str_shareName:[Character] = ["o","n","C"]
fileprivate let str_devicePartyValue:[Character] = ["a","n","c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let str_inviteNameTitle:[Character] = ["r","e","t"]
fileprivate let str_actionData:[Character] = ["r","y","A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let str_rowCornerReturnValue:[Character] = ["o","n","R"]
fileprivate let str_viewContentName:String = "infoquinfo"

/*: "failType" :*/
fileprivate let str_colorYouName:String = "fequal"
fileprivate let str_labelManagerData:[Character] = ["i","l","T","y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let str_keyImageContent:String = "callWaitkit input let image"
fileprivate let str_tapTitle:String = "ingTimeline at image mental"

/*: "toUid" :*/
fileprivate let str_behaviorData:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let str_responseContent:String = "Freeas succeed"
fileprivate let str_searchTitle:String = "network"

/*: "%@/min" :*/
fileprivate let str_kitName:String = "model if%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ClickViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ClickViewController: ToControllerPositionBaseRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.teensy()
            }
        }
        //: self.resetCamera()
        self.cameraCoin()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        dataFormat()
        //: setupSubViewsConstraint()
        buttonWith()
        //: bindInteraction()
        counteractionResource()
        //: videoManager.startPreview(isMatch: true)
        videoManager.rangeEvaluate(isMatch: true)
        //: setPlayer(isRandon: false)
        showGenerate(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            anyBlock()
        }
    
            if (renderView.layer.contentsRect.origin.y != 0) && (renderView.layer.shadowRadius == 1.41) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let draftTitle = YearView()

                renderView.addSubview(draftTitle)
            }

	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_clickTitle.map{viewVar(origin: $0)}, encoding: .utf8)!: (String(str_makeTitle)), String(bytes: str_appTitle.map{labMake(image: $0)}, encoding: .utf8)!: [(String(str_leadingValue.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        maxBy()
        //: if TalkingSocketManager.shared.isTalking == false {
        if ManagingDirectorReactiveCompatible.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ManagingDirectorReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ManagingDirectorReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ManagingDirectorReactiveCompatible.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: DirectorChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = DirectorChatManager()
        //: let info = TalkingVideoChatModel()
        let info = ActualChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .segment()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(anyBlock), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_itemData + 1, height: kLet_dismissValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (str_failureContent.replacingOccurrences(of: "view", with: "32")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_ageValue.prefix(7)) + String(str_errData) + String(str_tagName)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.markTag(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.markTag(name: (String(str_detailTitle) + String(str_lockTitle))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(toLab), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: EveningShirtFixingTalkingButton = {
        //: let btn = TalkingButton()
        let btn = EveningShirtFixingTalkingButton()
        //: btn.setImage(UIImage.markTag(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.markTag(name: (str_pairValue.lowercased() + "_views" + String(str_toColorValue.prefix(7)))), for: .normal)
        //: btn.setImage(UIImage.markTag(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.markTag(name: (str_pairValue.lowercased() + "_views" + String(str_toColorValue.prefix(7)))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_transformValue) + String(str_firstText.suffix(5)) + "ting").localizedArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.markTag(name: "icon_lounge")
        img.image = UIImage.markTag(name: (String(str_toLabelActionContent) + str_makeText.replacingOccurrences(of: "third", with: "ge")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.markTag(name: "icon_rank_coin")
        img.image = UIImage.markTag(name: (String(str_moreTitle.suffix(8)) + String(str_sexText.prefix(6))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.markTag(name: "vip_btn_top")
        imgV.image = UIImage.markTag(name: (String(str_dismissValue.prefix(5)) + "tn_t" + String(str_inviteVideoValue)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: EveningShirtFixingTalkingButton = {
        //: let btn = TalkingButton()
        let btn = EveningShirtFixingTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(dataCell), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.markTag(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.markTag(name: (String(str_cornerClickName) + String(str_giftFatalTitle))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.markTag(name: "icon_rank_coin")
        imgV.image = UIImage.markTag(name: (String(str_moreTitle.suffix(8)) + String(str_sexText.prefix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .fromNonsolidColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (str_effectValue.capitalized + String(str_emptyContent.suffix(4)) + "s/min").localizedArguments(vipFee)
        //: lab.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        lab.font = UIFont.biologyLaboratoryEqual(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ElectronicTitleView = //: return ElectronicTitleView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ClickViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func showGenerate(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = EffectTool.default.confirm(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_fillValue)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func join(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(str_jumpTitle.prefix(6)) + String(str_showKeyData)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: str_clickTitle.map{viewVar(origin: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: str_makeAllText.reversed(), encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ShowVideoManager.confirmCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.pastLess()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_jumpTitle.prefix(6)) + String(str_showKeyData))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func freebee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ShowVideoManager.warning { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(str_padName.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.teensy()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ClickViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func dataCell() {
        //: EnvelopeSinceAdministratorThen.share.func__pushToSubscribeAlert()
        EnvelopeSinceAdministratorThen.share.dismissBy()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func toLab() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        roundName()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_netTitle.currentIn(eventID: kLet_searchData)

        //: if RecordAppManager.share.appConfigMode.displayMatchV4PopUp, RecordAppManager.share.loginUserMode.freeCallTimes > 0,
        if RecordAppManager.share.appConfigMode.displayMatchV4PopUp, RecordAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.statusFor()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        greetAt()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func anyBlock() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ManagingDirectorReactiveCompatible.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.talkTask(showMsg: kLet_meetingValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        air()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_finishData)).localized
        //: setPlayer(isRandon: true)
        showGenerate(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if RecordAppManager.share.loginUserMode.randomVideoType == 2 {
        if RecordAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_netTitle.currentIn(eventID: kLet_cameraText)
            //: req_getVideoInfo()
            findInsideInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_netTitle.currentIn(eventID: kLet_deviceLikeData)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ManagingDirectorReactiveCompatible.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ManagingDirectorReactiveCompatible.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ManagingDirectorReactiveCompatible.shared.errorDelegate = self
            //: restart_randomVideo()
            errnoImagePause()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func errnoImagePause(cmd: String = (String(str_blockName.prefix(5)) + "ouch"), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        maxBy()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        join(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func pastLess() {
        // 停止定时器
        //: destroyWaitingTimer()
        maxBy()
        //: showCloseBtn()
        roundName()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_ageValue.prefix(7)) + String(str_errData) + String(str_tagName)).localized
        //: setPlayer(isRandon: false)
        showGenerate(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if ManagingDirectorReactiveCompatible.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ManagingDirectorReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ManagingDirectorReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ManagingDirectorReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ManagingDirectorReactiveCompatible.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        teensy()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func maxBy() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func findInsideInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PtolemaicSystemThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_successValue) + str_addEqualTitle.replacingOccurrences(of: "err", with: "e") + "Info")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_viewContentValue.reversed(), encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_reasonText.priceSub(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.pastLess()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: RecordAppManager.share.loginUserMode.randomVideoType = 1
                    RecordAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.anyBlock()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.talkTask(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = HubModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = NameTextThen()
                //: player.setMute(bEnable: true)
                player.element(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.betweenWith(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                MeReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                HiddenToolReactiveCompatible.terminateColor { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.statusButton()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func statusButton() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_netTitle.currentIn(eventID: kLet_giftContent)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard MeReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.talkTask(showMsg: String(bytes: str_shareData.map{protrudeLaugh(make: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            MeReactiveCompatible.shared.baseballTeam(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.pastLess()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = DorsumReactiveCompatible()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.greetAt()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.pastLess()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func air() {
        //: if RecordAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if RecordAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(RecordAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(RecordAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func roundName() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func cameraCoin() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.rangeEvaluate()
        }
    }
}

// MARK: - KeyMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ClickViewController: KeyMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func selectData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = ItemHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(str_jumpTitle.prefix(6)) + String(str_showKeyData)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: str_labelValue.map{timingBubble(view: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: str_requestValue.map{timeName(mark: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: str_iconObserverName.map{titleAspect(value: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = SinceQuickLookable.randow
        //: if RecordAppManager.share.loginUserMode.freeCallTimes > 0 && RecordAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if RecordAppManager.share.loginUserMode.freeCallTimes > 0 && RecordAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (str_makeData.replacingOccurrences(of: "party", with: "r") + String(str_playerName.prefix(6)) + "Call"))
            //: TalkingSocketManager.shared.isFreeCall = true
            ManagingDirectorReactiveCompatible.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: str_clickTitle.map{viewVar(origin: $0)}, encoding: .utf8)!: (str_detailContent.lowercased() + "estCall"), String(bytes: str_appTitle.map{labMake(image: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ManagingDirectorReactiveCompatible.shared.levelFront(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ManagingDirectorReactiveCompatible.shared.levelFront(info: [String(bytes: str_clickTitle.map{viewVar(origin: $0)}, encoding: .utf8)!: (String(str_makeTitle)), String(bytes: str_appTitle.map{labMake(image: $0)}, encoding: .utf8)!: [(String(str_leadingValue.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.errnoImagePause(cmd: (String(str_divisionName.suffix(4)) + "deoMatch"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func theory(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(str_leadingValue.prefix(5)))].intValue
    }
}

// MARK: - ResultCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ClickViewController: ResultCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func socketUp(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func loadCell(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func lose(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func technicality(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ColorDelegateHelper.numbererval(body: String(bytes: str_makeValue.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = ItemHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            hideUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        pastLess()
        //: RecordAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        RecordAppManager.share.bySituation(info: data, situation: .Calling)
    }
}

// MARK: - ActualObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ClickViewController: ActualObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func react(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        errnoImagePause(cmd: (String(str_executeFrameData.prefix(9)) + str_haveText.replacingOccurrences(of: "make", with: "l")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func currency(data _: [String: Any]) {}
}

// MARK: - TopObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ClickViewController: TopObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func clickError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_shareName) + String(str_devicePartyValue)) { // 取消通话异常
            //: stop_randomVideo()
            pastLess()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(str_divisionName.suffix(4)) + "deoMatch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            pastLess()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_jumpTitle.prefix(6)) + String(str_showKeyData))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.talkTask(showMsg: msg)
            //: uploadFailTime(type: 2)
            picEditionTo(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_divisionName.suffix(4)) + "deoMatch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_inviteNameTitle) + String(str_actionData))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_jumpTitle.prefix(6)) + String(str_showKeyData))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.errnoImagePause(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(str_rowCornerReturnValue) + str_viewContentName.replacingOccurrences(of: "info", with: "e") + "stCall"), errorNo == RecordVisualCommunicationSignedNumeric.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            pastLess()
            //: guard RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue else { return }
            //: EnvelopeSinceAdministratorThen.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            EnvelopeSinceAdministratorThen.share.colorFailure(clickEvent: kLet_readListData, sufficient: false)
            //: uploadFailTime(type: 1)
            picEditionTo(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        errnoImagePause(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ClickViewController {
    //: func uploadFailTime(type: Int) {
    func picEditionTo(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(str_colorYouName.replacingOccurrences(of: "equal", with: "a") + String(str_labelManagerData))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(str_keyImageContent.prefix(8)) + String(str_tapTitle.prefix(7)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_netTitle.gestureBottom(eventID: kLet_screenBottomValue, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func hideUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(str_keyImageContent.prefix(8)) + String(str_tapTitle.prefix(7)))] = time
        //: parm["toUid"] = toUid
        parm[(String(str_behaviorData))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_netTitle.gestureBottom(eventID: kLet_pointPathValue, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ClickViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func teensy() {
        //: guard RecordAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard RecordAppManager.share.appStatus == MaculeRelatableTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !RecordAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !RecordAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = SinceQuickLookable.randow
        //: if RecordAppManager.share.loginUserMode.freeCallTimes > 0 && RecordAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if RecordAppManager.share.loginUserMode.freeCallTimes > 0, RecordAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if RecordAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if RecordAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_responseContent.prefix(4)) + " 1 m" + str_searchTitle.replacingOccurrences(of: "network", with: "in")).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = RecordAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = RecordAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(str_kitName.suffix(6))).localizedArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.cutis(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(str_kitName.suffix(6))).localizedArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.cutis(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = RecordAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = RecordAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = RecordAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = RecordAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !RecordAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !RecordAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func dataFormat() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func buttonWith() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_pathErrData + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func counteractionResource() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(teensy),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_nameClickValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(freebee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_statusBottomName,
                                               //: object: nil)
                                               object: nil)
    }
}
