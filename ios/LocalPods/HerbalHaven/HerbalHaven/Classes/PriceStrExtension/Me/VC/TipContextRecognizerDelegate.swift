
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_appearData:[UInt8] = [0x32,0x35,0x32,0x2f,0x73,0x38,0x34,0x3f,0x3e,0x29,0x61,0x72,0x7b,0x33,0x3a,0x28,0x7b,0x35,0x34,0x2f,0x7b,0x39,0x3e,0x3e,0x35,0x7b,0x32,0x36,0x2b,0x37,0x3e,0x36,0x3e,0x35,0x2f,0x3e,0x3f]

private func hiddenTime(head num: UInt8) -> UInt8 {
    return num ^ 91
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let str_frameWithValue:[UInt8] = [0xf5,0xca,0xdf,0xd4,0x9a,0xce,0xd2,0xdf,0xd4,0x9a,0x9f,0xfa,0x9a,0xcd,0xd3,0xd6,0xd6,0x9a,0xc8,0xdf,0xd9,0xd5,0xd7,0xd7,0xdf,0xd4,0xde,0x9a,0xcf,0xc9,0xdf,0xc8,0xc9,0x9a,0xce,0xd5,0x9a,0xc3,0xd5,0xcf,0x94,0x9a,0xfb,0x9a,0xd4,0xdf,0xcd,0x9a,0xd9,0xd2,0xdb,0xce,0x9a,0xcd,0xd3,0xd6,0xd6,0x9a,0xd8,0xdf,0x9a,0xd7,0xdb,0xde,0xdf,0x9a,0xdb,0xdc,0xce,0xdf,0xc8,0x9a,0xdb,0x9a,0xc9,0xcf,0xd9,0xd9,0xdf,0xc9,0xc9,0xdc,0xcf,0xd6,0x9a,0xc8,0xdf,0xd9,0xd5,0xd7,0xd7,0xdf,0xd4,0xde,0xdb,0xce,0xd3,0xd5,0xd4,0x94]

private func factorActual(message num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_miniData:[UInt8] = [0xaf,0x90,0x85,0x8e,0xc0,0x94,0x88,0x85,0x8e,0xc0,0xa1,0x90,0x90,0x8c,0x99,0x8e,0x81,0x8d,0x85,0xc0,0x97,0x89,0x8c,0x8c,0xc0,0x92,0x85,0x83,0x8f,0x8d,0x8d,0x85,0x8e,0x84,0xc0,0x95,0x93,0x85,0x92,0x93,0xc0,0x94,0x8f,0xc0,0x99,0x8f,0x95,0xce,0xc0,0xb5,0x93,0x85,0xc0,0x99,0x8f,0x95,0x92,0xc0,0x89,0x84,0x85,0x8e,0x94,0x89,0x94,0x99,0xc0,0x94,0x8f,0xc0,0x87,0x92,0x85,0x85,0x94,0xc0,0x85,0x81,0x83,0x88,0xc0,0x8f,0x94,0x88,0x85,0x92,0xce]

private func playerPhotoShare(moment num: UInt8) -> UInt8 {
    return num ^ 224
}

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let str_sharedExhibitValue:[UInt8] = [0x2e,0x67,0x6e,0x69,0x6b,0x6e,0x61,0x72,0x20,0x65,0x68,0x74,0x20,0x6e,0x69,0x20,0x64,0x65,0x79,0x61,0x6c,0x70,0x73,0x69,0x64,0x20,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x6c,0x6c,0x69,0x77,0x20,0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e,0x20,0x64,0x6e,0x61,0x20,0x72,0x61,0x74,0x61,0x76,0x61,0x20,0x72,0x75,0x6f,0x59]

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let str_fillResultText:[UInt8] = [0xbd,0x82,0x97,0x9c,0xd2,0x86,0x9a,0x97,0x9c,0xd2,0xd7,0xb2,0xd2,0x85,0x9b,0x9e,0x9e,0xd2,0x80,0x97,0x91,0x9d,0x9f,0x9f,0x97,0x9c,0x96,0xd2,0x87,0x81,0x97,0x80,0x81,0xd2,0x86,0x9d,0xd2,0x8b,0x9d,0x87,0xdc,0xd2,0xa7,0x81,0x97,0xd2,0x8b,0x9d,0x87,0x80,0xd2,0x9b,0x96,0x97,0x9c,0x86,0x9b,0x86,0x8b,0xd2,0x86,0x9d,0xd2,0x95,0x80,0x97,0x97,0x86,0xd2,0x97,0x93,0x91,0x9a,0xd2,0x9d,0x86,0x9a,0x97,0x80,0xdc]

private func circleClass(image num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "SinceView" :*/
fileprivate let str_limitData:String = "content content appTalki"
fileprivate let str_transformTitle:String = "table backgroundingCe"
fileprivate let str_bottomSizeValue:String = "colorcolor"

/*: "#FF2348" :*/
fileprivate let str_timeName:[Character] = ["#"]
fileprivate let str_blockValue:String = "tag2348"

/*: "SoundReactiveCompatible" :*/
fileprivate let str_photoFatalValue:String = "var varTalk"
fileprivate let str_requestTitle:String = "ettiimage"
fileprivate let str_makeName:[Character] = ["g","N","o","t","i","f","C","e","l","l"]

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let str_selectTitle:[UInt8] = [0x73,0x48,0x7,0x54,0x42,0x53,0x7,0x52,0x57,0x7,0x46,0x7,0x57,0x46,0x54,0x54,0x50,0x48,0x55,0x43,0xb,0x7,0x5e,0x48,0x52,0x7,0x49,0x42,0x42,0x43,0x7,0x53,0x48,0x7,0x51,0x42,0x55,0x4e,0x41,0x5e,0x7,0x53,0x4f,0x42,0x7,0x46,0x44,0x44,0x48,0x52,0x49,0x53,0xb,0x7,0x44,0x4f,0x48,0x48,0x54,0x42,0x7,0x5e,0x48,0x52,0x55,0x7,0x51,0x42,0x55,0x4e,0x41,0x4e,0x44,0x46,0x53,0x4e,0x48,0x49,0x7,0x4a,0x42,0x53,0x4f,0x48,0x43]

/*: "Verified by Phone" :*/
fileprivate let str_needInfoAcrossTitle:String = "Verifvideo equal model"
fileprivate let str_floorData:String = "by Pvar let"
fileprivate let str_makeData:String = "borderone"

/*: "Verified by Email" :*/
fileprivate let str_backValue:[Character] = ["V","e","r","i","f","i","e","d"," ","b","y"," ","E","m","a","i","l"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let str_appValue:[UInt8] = []

private func extraVideo(succeed num: UInt8) -> UInt8 {
    return num ^ 15
}

/*: "UNRECOVERABLE" :*/
fileprivate let str_greetName:[Character] = ["U","N","R","E","C","O","V","E","R","A","B","L","E"]

/*: "Delete Account" :*/
fileprivate let str_toolIndexTitle:String = "indicator centerDele"
fileprivate let str_labelData:String = "oviewt"

/*: "Cancel" :*/
fileprivate let str_rowTitle:[Character] = ["C","a","n","c","e"]
fileprivate let str_timeData:String = "event"

/*: "Delete" :*/
fileprivate let str_responseName:String = "Deletesection index return"

/*: " ( :*/
fileprivate let str_makeTitle:String = "live("

/*: s)" :*/
fileprivate let str_videoBackgroundName:[Character] = ["s",")"]

/*: "Logout succeeded!" :*/
fileprivate let str_editTitle:String = "video self render guidance contentLogou"
fileprivate let str_normalMakeText:String = "eeded!call shared title line"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TipContextRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum SettingsContentLiteral: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum KeyRepresentable: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class TipContextRecognizerDelegate: ToControllerPositionBaseRecognizerDelegate {
	var equalNumber: Double = -59.5
	var promptDictionary: [AnyHashable: String] = [:]
	var buttonPairCount: Double = -52.6
	var clickDictionary: [AnyHashable: String] = [:]

    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = MakeModelType()

    //: var  type = SettingsUniversal.Contact_information
    var type = SettingsContentLiteral.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_appearData.map{hiddenTime(head: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    
            if (self.navigationItem.title == "push") && (self.textInputContextIdentifier != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let voiceLet = AllowController()

            voiceLet.rowSexSum = { [self] aboveCount in
            self.buttonPairCount = aboveCount
            
                self.buttonPairCount += 1
                if self.buttonPairCount != 77 {
                    self.buttonPairCount = self.buttonPairCount - 1
                }
            return self.buttonPairCount
            }
            voiceLet.paperDictionary = { [self] tapDictionary in
            self.clickDictionary = tapDictionary
            
            guard var value = self.clickDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(voiceLet.self, animated: true)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (self.tabBarItem.badgeValue == " ") && (self.supportedInterfaceOrientations == .portraitUpsideDown) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let routeChange = AllowController()

            routeChange.rowSexSum = { [self] aboveCount in
            self.equalNumber = aboveCount
            
            self.equalNumber -= 1
            return self.equalNumber
            }
            routeChange.paperDictionary = { [self] tapDictionary in
            self.promptDictionary = tapDictionary
            
            guard var value = self.promptDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.present(routeChange.self, animated: false) { [self] in
            self.equalNumber = 297.72
                }
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.appIcon()
        //: designView()
        allowIt()
        //: getSettingData()
        overdoColor()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [KeyRepresentable] = {
        //: var array = [SettingsUniversalType]()
        var array = [KeyRepresentable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_itemData, height: kLet_dismissValue - kLet_listTagData), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized, "Your avatar and nickname will not be displayed in the ranking.".localized]
        var array = ["", String(bytes: str_frameWithValue.map{factorActual(message: $0)}, encoding: .utf8)!.localizedArguments(kLet_limitEnterData), String(bytes: str_miniData.map{playerPhotoShare(moment: $0)}, encoding: .utf8)!.localized, String(bytes: str_sharedExhibitValue.reversed(), encoding: .utf8)!.localized]
        //: if RecordAppManager.share.loginUserMode.sex == "2" {
        if RecordAppManager.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: str_fillResultText.map{circleClass(image: $0)}, encoding: .utf8)!.localizedArguments(kLet_limitEnterData)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension TipContextRecognizerDelegate {
    //: func getSettingData() {
    func overdoColor() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        FocusThen.groupAction(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<MakeModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func quote(type: SettingsContentLiteral) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.Phone_Number)
            DataSource.append(.Phone_Number)
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if RecordAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 || RecordAppManager.share.loginUserMode.email.count > 0 {
            if RecordAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 || RecordAppManager.share.loginUserMode.email.count > 0 {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func toSms() {
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        ActualListThen.appColor { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EnvelopeSinceAdministratorThen.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: RecordAppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
            EnvelopeSinceAdministratorThen.share.pathViewPwd(type: .PhoneLogin, content: RecordAppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func pushBack() {
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        ActualListThen.underbelly { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EnvelopeSinceAdministratorThen.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: RecordAppManager.share.loginUserMode.email, isNewPwd: false)
            EnvelopeSinceAdministratorThen.share.pathViewPwd(type: .EmailLogin, content: RecordAppManager.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension TipContextRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if RecordAppManager.share.loginUserMode.sex == "2" {
                if RecordAppManager.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let identifier = "TalkingSettingCell"
            let identifier = (String(str_limitData.suffix(5)) + "ngSett" + String(str_transformTitle.suffix(5)) + str_bottomSizeValue.replacingOccurrences(of: "color", with: "l"))
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingCell
            let cell: SinceView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! SinceView

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.cellLastGroundLineMessageTitile(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == KeyRepresentable.Phone_Number {
                //: if RecordAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if RecordAppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: RecordAppManager.share.loginUserMode.mobile ?? "")
                    cell.resource(detail: RecordAppManager.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .segment()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == KeyRepresentable.Email_Address {
                //: if RecordAppManager.share.loginUserMode.email.count > 0 {
                if RecordAppManager.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: RecordAppManager.share.loginUserMode.email )
                    cell.resource(detail: RecordAppManager.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == KeyRepresentable.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == KeyRepresentable.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(str_timeName) + str_blockValue.replacingOccurrences(of: "tag", with: "FF")))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let identifier = "TalkingSettingNotifCell"
            let identifier = (String(str_photoFatalValue.suffix(4)) + "ingS" + str_requestTitle.replacingOccurrences(of: "image", with: "n") + String(str_makeName))
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingNotifCell
            let cell: SoundReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! SoundReactiveCompatible

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.paradigm(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.sharedHandle(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.page(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.page(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.page(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.page(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.page(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == KeyRepresentable.Phone_Number.rawValue {
                //: let mobile = RecordAppManager.share.loginUserMode.mobile ?? ""
                let mobile = RecordAppManager.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = FillRecognizerDelegate()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == KeyRepresentable.Email_Address.rawValue {
                //: if RecordAppManager.share.loginUserMode.email.count <= 0 {
                if RecordAppManager.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = TitleDistinctReactiveCompatible()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == KeyRepresentable.DeleteA.rawValue {
                //: writeOff()
                viewToMessage()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == KeyRepresentable.SetPassword.rawValue {
                //: let mobile = RecordAppManager.share.loginUserMode.mobile ?? ""
                let mobile = RecordAppManager.share.loginUserMode.mobile ?? ""
                //: let email = RecordAppManager.share.loginUserMode.email
                let email = RecordAppManager.share.loginUserMode.email
                //: if !mobile.isEmpty, !email.isEmpty {
                if !mobile.isEmpty, !email.isEmpty { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = TagDataSource(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: str_selectTitle.map{$0^39}, encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.moveDismiss(fontSize: 12)
                    vc.titleFont = UIFont.moveDismiss(fontSize: 12)
                    //: vc.titleColor = UIColor.pullUpColor()
                    vc.titleColor = UIColor.pullUpColor()
                    //: vc.cellTitleFont = UIFont.cutis(fontSize: 16)
                    vc.cellTitleFont = UIFont.cutis(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.toCurrent(cellTitleList: [(String(str_needInfoAcrossTitle.prefix(5)) + "ied " + String(str_floorData.prefix(4)) + str_makeData.replacingOccurrences(of: "border", with: "h")).localized, (String(str_backValue)).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.toSms()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.pushBack()
                        }
                    }

                    //: } else if !mobile.isEmpty {
                } else if !mobile.isEmpty {
                    //: req_sendModifyPwdSms()
                    toSms()

                    //: } else if !email.isEmpty {
                } else if !email.isEmpty {
                    //: req_sendModifyPwdEmail()
                    pushBack()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension TipContextRecognizerDelegate {
    /// 注销
    //: private func writeOff() {
    private func viewToMessage() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard HiddenToolReactiveCompatible.equalAdd() == false else { return }
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ManagingDirectorReactiveCompatible.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.talkTask(showMsg: kLet_meetingValue)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: str_appValue.map{extraVideo(succeed: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.exMatchStrRange((String(str_greetName)))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.imageColor(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        BackgroundAlertShow.startMessage(title: (String(str_toolIndexTitle.suffix(4)) + "te Acc" + str_labelData.replacingOccurrences(of: "view", with: "un")).localized, attributedMessage: string, leftBtnTitle: (String(str_rowTitle) + str_timeData.replacingOccurrences(of: "event", with: "l")).localized, rightBtnTitle: (String(str_responseName.prefix(6))).localized) {
            //: TalkingAlertShow.hideAlert()
            BackgroundAlertShow.everySize()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            BackgroundAlertShow.everySize()

            //: self.LockAccount()
            self.positionAccount()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                BackgroundAlertShow.statuteContentRightBussChange(title: (String(str_responseName.prefix(6))).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                BackgroundAlertShow.statuteContentRightBussChange(title: (String(str_responseName.prefix(6))).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func positionAccount() {
        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        FocusThen.ditheredColor(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.withOf()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.moveAir(showMsg: (String(str_editTitle.suffix(5)) + "t succ" + String(str_normalMakeText.prefix(6))).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func withOf() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ManagingDirectorReactiveCompatible.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.talkTask(showMsg: kLet_meetingValue)
            //: return
            return
        }

        //: ProgressHUD.show()
        TotalerpretationProgressHUD.alienation()
        //: TalkingLoginRequestTool.req_loginOut { t in
        ActualListThen.finish { t in
            //: ProgressHUD.dismiss()
            TotalerpretationProgressHUD.countimateEqualDismiss()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_domainChangeMessageValue, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension TipContextRecognizerDelegate {
    //: private func designView() {
    private func allowIt() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: "TalkingSettingCell")
        MainTable.register(SinceView.self, forCellReuseIdentifier: (String(str_limitData.suffix(5)) + "ngSett" + String(str_transformTitle.suffix(5)) + str_bottomSizeValue.replacingOccurrences(of: "color", with: "l")))
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: "TalkingSettingNotifCell")
        MainTable.register(SoundReactiveCompatible.self, forCellReuseIdentifier: (String(str_photoFatalValue.suffix(4)) + "ingS" + str_requestTitle.replacingOccurrences(of: "image", with: "n") + String(str_makeName)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
